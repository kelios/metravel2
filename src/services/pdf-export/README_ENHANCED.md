# Улучшенная система экспорта PDF

## Обзор

Реализована новая система экспорта PDF с улучшенным дизайном, поддержкой структурированных блоков контента и 4 новыми темами оформления.

## Основные улучшения

### 1. Система тем оформления

Созданы 4 новые темы оформления:

- **minimal** - Минималистичная: чистое и простое оформление с акцентом на контент
- **light** - Светлая: много воздуха, мягкие цвета, комфортное чтение
- **dark** - Темная: темное оформление, пригодное для печати и экрана
- **travel-magazine** - Travel Magazine: журнальная вёрстка с яркими акцентами

Каждая тема включает:
- Полную цветовую палитру (текст, фон, акценты, специальные блоки)
- Типографику (шрифты для заголовков и текста, размеры)
- Отступы и сетку
- Стили блоков (скругления, тени, границы)

### 2. Парсер контента

Создан `ContentParser`, который преобразует HTML/Markdown в структурированные блоки:

- Заголовки (h1-h6)
- Параграфы
- Списки (упорядоченные и неупорядоченные)
- Цитаты
- Изображения (с подписями)
- Галереи изображений
- Специальные блоки (Совет, Важно, Лайфхак, Предупреждение)
- Код
- Таблицы
- Разделители

### 3. Рендерер блоков

`BlockRenderer` рендерит структурированные блоки в HTML с применением темы оформления:

- Корректное применение стилей темы
- Поддержка специальных блоков с иконками
- Обработка изображений с fallback
- Поддержка галерей

### 4. Улучшенный генератор

`EnhancedPdfGenerator` генерирует PDF с:

- Титульным листом
- Оглавлением (опционально)
- Страницами путешествий с фото
- Страницами с контентом (парсинг HTML в структурированные блоки)
- Галереями фотографий (опционально)
- Картами маршрутов (опционально)
- Финальной страницей

### 5. Обработка краевых случаев

Реализована обработка:

- Путешествий без фотографий
- Путешествий без карты
- Очень длинного текста
- Длинных заголовков
- Переносов страниц (page-break-inside: avoid для заголовков, изображений, блоков)
- Защита от "висячих" строк (orphans/widows)

## Архитектура

```
PdfExportService
  ├── TravelDataTransformer (трансформация данных)
  ├── EnhancedPdfGenerator (новый генератор для новых тем)
  │   ├── ContentParser (парсинг HTML в блоки)
  │   ├── BlockRenderer (рендеринг блоков)
  │   └── PdfThemeConfig (конфигурация тем)
  └── Html2PdfRenderer (рендеринг в PDF)
```

## Использование

### Базовое использование

```typescript
import { PdfExportService } from '@/src/services/pdf-export/PdfExportService';
import { Html2PdfRenderer } from '@/src/renderers/pdf/Html2PdfRenderer';
import type { BookSettings } from '@/components/export/BookSettingsModal';

const renderer = new Html2PdfRenderer();
const service = new PdfExportService(renderer);

const settings: BookSettings = {
  title: 'Мои путешествия',
  template: 'minimal', // или 'light', 'dark', 'travel-magazine'
  // ... другие настройки
};

const result = await service.export(travels, settings);
```

### Откуда вызывается экспорт в приложении

На уровне UI используется **один общий хук** `usePdfExport` (`src/hooks/usePdfExport.ts`).

- **Со страницы путешествия** (travel-detail):
  - в хук передаётся массив из одного `Travel`;
  - по необходимости `usePdfExport` догружает недостающие поля (описание, рекомендации, плюсы, минусы, галерею, адреса/карту);
  - при нажатии на кнопку PDF открывается модалка `BookSettingsModal`, далее вызывается `openPrintBook(settings)` из хука.
- **Со страницы списка / конструктора экспорта**:
  - в хук передаётся массив выбранных `Travel` (несколько путешествий);
  - экспортируется одна книга со всеми выбранными маршрутами в порядке, заданном в `BookSettings.sortOrder`.

В обоих случаях используется **один и тот же пайплайн**:

`usePdfExport` → `BookHtmlExportService.generateTravelsHtml` → `EnhancedPdfGenerator` → `Html2PdfRenderer`.

### Выбор темы

Темы выбираются через параметр `template` в `BookSettings`:

- `'minimal'` - Минималистичная
- `'light'` - Светлая
- `'dark'` - Темная
- `'travel-magazine'` - Travel Magazine
- `'classic'` - Классическая
- `'modern'` - Современная
- `'romantic'` - Романтическая
- `'adventure'` - Приключенческая

Старые темы (`'classic'`, `'modern'`, `'romantic'`, `'adventure'`) продолжают работать и совместимы с текущей архитектурой экспорта.

## Специальные блоки в контенте

Парсер автоматически распознает специальные блоки по классам:

- `.tip`, `.совет`, `.лайфхак` → Блок совета (💡)
- `.warning`, `.предупреждение`, `.важно` → Блок предупреждения (⚠️)
- `.danger`, `.опасность` → Блок опасности (🚨)
- `.info`, `.информация` → Информационный блок (ℹ️)

Пример HTML:

```html
<div class="tip">
  <strong>Совет:</strong>
  Не забудьте взять с собой паспорт!
</div>
```

### Разделы книги путешествия

При экспорте одного или нескольких путешествий `EnhancedPdfGenerator` строит книгу из стандартных секций:

- **Обложка** – используется `BookSettings.title/subtitle`, автор (имя пользователя), обложка путешествия или градиент.
- **Оглавление** (если `includeToc = true`) – список путешествий с номерами страниц.
- Для каждого путешествия:
  - **Фото‑страница** – главное фото (`travel_image_url`/`thumb`), страна, год, количество дней.
  - **Текстовая страница**:
    - блок «Описание» (`travel.description` → HTML через `ContentParser` + `BlockRenderer`),
    - блок «Рекомендации» (`travel.recommendation`),
    - боксы «Плюсы» / «Минусы» (`travel.plus` / `travel.minus`).
  - **Галерея** – если в данных есть `gallery` и `settings.includeGallery = true`.
  - **Карта маршрута** – если есть `travelAddress` и `settings.includeMap = true`:
    - верхняя часть: картинка‑карта (скриншот Leaflet или SVG‑маршрут),
    - нижняя часть: карточки точек маршрута (фото точки, номер, название, категория, координаты).
- **Чек‑листы путешественника** – если включены в `BookSettings`.
- **Финальная страница** – благодарность и цитата о путешествиях.

Все эти секции доступны и при экспорте с отдельной страницы путешествия, и при экспорте из списка: разница только в количестве путешествий в книге.

## Тестирование

Созданы тесты в `__tests__/services/pdf-export/EnhancedPdfGenerator.test.ts`:

- Генерация HTML для разных сценариев
- Обработка краевых случаев
- Проверка всех тем оформления
- Тесты специальных блоков

Запуск тестов:

```bash
npm test -- EnhancedPdfGenerator.test.ts
```

## Миграция

Новая архитектура экспорта полностью совместима с существующими настройками. Новые темы автоматически используют улучшенный генератор.

Для использования новых тем просто измените `template` в `BookSettings`:

```typescript
// Старый способ (продолжает работать)
settings.template = 'classic';

// Новый способ
settings.template = 'minimal';
```

## Обратная совместимость

- Старые темы (`classic`, `modern`, `romantic`, `adventure`) продолжают работать
- Внешний API не изменился
- Все существующие настройки поддерживаются

## Будущие улучшения

- Поддержка кастомных тем через JSON-конфиги
- Экспорт тем в отдельные файлы
- Предпросмотр тем перед экспортом
- Дополнительные специальные блоки
- Поддержка Markdown в дополнение к HTML
