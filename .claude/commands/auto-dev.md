---
description: Одна итерация code-level разработка → проверка → фикс
---

Прочитай `.agents/skills/source-command-auto-dev/SKILL.md` и используй его как канонический workflow.

Выполни одну итерацию для `$ARGUMENTS`. После code-level checks организуй независимый review-and-fix; browser/API/device QA передавай в `testing` после pass. Не присваивай чужой незавершённый diff.

Аргументы: `$ARGUMENTS`
