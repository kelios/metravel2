---
name: android-qa-sweep
description: "QA-обход Android-приложения на устройстве через adb: скрины, доказательства дефектов и повторная проверка исправлений. Триггеры: «прогони QA на андроиде», «протестируй приложение на телефоне», «прокликай все экраны»."
---

# Android QA Sweep — проверка на устройстве (macOS)

## Предусловия

- Runtime QA начинается в `testing` после обязательного code review. QA не
  редактирует feature code; дефект передаётся исполнителю через
  implementation → review → testing по `docs/TASK_BOARD_MCP.md`.
- До первой пробы обнови и подними локальный бэкенд строго по
  `docs/WORKFLOW_OPERATIONS.md` → «3.0 Локальный стек и обновление бэкенда перед
  тестированием», включая применимые миграции/зависимости и проверку готовности.
  Локальный стек — таргет по умолчанию; dev/prod требуют явного запроса владельца.
- Физическое устройство по USB, отладка включена: сначала `adb devices -l`.
  Dev-client `by.metravel.app`, Metro, local build/install и port forwarding
  готовь по operation gate из `docs/WORKFLOW_OPERATIONS.md`.
- Устройство использует текущий LAN-IP мака для API. Перед запуском Metro задай
  `EXPO_PUBLIC_API_URL=http://<LAN-IP>:8000` и `EXPO_PUBLIC_IS_LOCAL_API=true`
  точечно в окружении процесса; после смены API перезапусти Metro и Reload бандла.
  Старые prod-фото могут отсутствовать в локальном бакете `metravellocal`;
  медиа проверяй на своей тестовой статье.
- DevTools button не должен перекрывать проверяемый UI.
- Логин тест-аккаунтом из `.env.e2e` допустим в рамках разрешённой QA-сессии;
  используй `E2E_EMAIL`/`E2E_PASSWORD` или вторую пару без вывода секретов.
  Account ID и права сверяй с текущим API, а не со старой заметкой. Вход выполняй
  доступным поддерживаемым способом; если обязательный авторизованный сценарий
  требует ручного логина, запроси ровно login/unlock и продолжи ту же проверку
  после ответа. До этого можно проверить независимые публичные сценарии.

## Инструменты — `artifacts/android-qa/qa.sh`
**Файл НЕ в git** (`/artifacts/` в `.gitignore`) — он машинно-локальный. Нет его на машине —
это environment blocker: скажи об этом и работай напрямую через `adb`, а не считай QA пройденной.
Source-аем и вызываем функции (Capitalized). Из Bash-тула:
`export QA_OUT="$PWD/.codex-temp/android-qa"` перед загрузкой helper; затем
`source artifacts/android-qa/qa.sh` и вызовы `Nav 'about' 6`, `Shot '20-about'`.

- `Shot "имя" [delay]` — screencap → даунскейл `sips -Z 1200` (>1200px = риск лимита Read-картинок). Карты-WebView грузятся 4–10с → delay 8+.
- `Crop "имя" [offY] [h]` — **полноразмерный** кроп (1080-wide, без даунскейла ширины) для ТОЧНОГО прицеливания по мелким маркерам Leaflet. Даунскейл-скрины врут по координатам.
- `Nav "route" [delay]` — deeplink `metravel://<route>`. Scheme из app.json (`metravel` с 2026-06-13; старее — `myapp`, `export QA_SCHEME=myapp`).
- `Tap x y`, `Swipe x1 y1 x2 y2 [ms]`, `Back`, `Menu` (RN dev-меню), `Pinch` (zoom-out двумя одновр. свайпами), `TypeText "латиница"` (кириллицу adb НЕ вводит).
- `Clickables [имя]` — uiautomator-дамп кликабельных, печатает label + bounds + **готовое `-> tap X Y`**. Координаты тапов брать ТОЛЬКО отсюда. Кириллица выводится корректно (UTF-8 из файла).
- `Logcat` — `ReactNativeJS:E` (краши/стектрейсы). `Focus` — текущий foreground-экран.
- Артефакты: `QA_OUT` обязательно указывает в ignored `.codex-temp/android-qa/`;
  не используй встроенный временный default. `QA_PKG` и `QA_SCHEME` сверяй с билдом.

## Ловушки (выучено на практике)
- **Лимит Read-картинок**: полноразмерные native-скрины (1080×2400) превышают лимит при многих картинках в истории. `Shot` даунскейлит до 1200; для прицеливания по маркерам — `Crop` (узкая полоса, остаётся <2000px). Серии до ~30 скринов или делегируй оценку субагенту (android-expert) со свежим контекстом.
- **Leaflet-WebView невидим для uiautomator**: маркеры/попапы внутри карты в дерево `Clickables` НЕ попадают — целиться только по `Crop`, проверять результат скрином, не дампом.
- **Тап по маркеру карты** (native) ОТКРЫВАЕТ нижнюю карточку места (`MapPlaceBottomCard`) — попадай по пину и ТЕСТИРУЙ карточку целиком (фото, действия Страница/Сохранить/♥/＋, навигация в Google/Organic/Waze, фуллскрин-фото, и обязательно — закрытие во всех состояниях, см. чеклист п.10-12). [ИСТОРИЧЕСКОЕ, перепроверять: старая заметка #223/F-46 утверждала «тап только рецентрит, карточку не открывает» — это УСТАРЕЛО, карточка открывается; именно эта стейл-заметка спрятала класс багов #497.]
- **АНТИ-СТЕЙЛ-ЗАМЕТКА (load-bearing):** заметка вида «известный баг: действие X ничего не делает / недоступно» — НЕ повод пропускать взаимодействие X. Заметки устаревают; «инертный» элемент мог стать активным и принести НОВЫЕ баги. Всегда выполняй взаимодействие сам и сверяй с реальностью, а не с заметкой. (Реально: вера в стейл-#223 «маркер карточку не открывает» → я не тапал маркеры → пропустил весь #497.)
- **Deeplink с query-параметрами** (`map?lat=&lng=&radius=`) применяется только на ПЕРВОМ монтировании экрана; на уже открытом табе игнорируется (mount-only capture). Чтобы применить — свежий старт приложения, не переход по табу.
- **force-stop роняет dev-client в лаунчер** (Metro-бандл не embedded). После `am force-stop` приложение НЕ откроется в прежнее место — нужно заново подключить бандл: запустить Metro → deeplink `metravel://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8081` или тапнуть сервер в «Recently opened». Не force-stop'ай без нужды.
- **Reload бандла** после правки `.env`/кода: `Menu` (`adb shell input keyevent 82`) → тап «Reload» (или Nav на dev-client URL). Deeplink на тот же экран JS НЕ перезагружает.
- **СТЕЙЛ-БАНДЛ — главная причина «фикс не сработал» при device-проверке.** Fast Refresh часто НЕ подхватывает НОВЫЕ файлы/импорты (а иногда и правки) — на устройстве крутится старый код, и баг «не исправлен», хотя в репо всё верно. Перед проверкой ЛЮБОГО фикса делать явный Reload (keyevent 82 → «Reload», ждать ~12с пересборки) и УБЕДИТЬСЯ скрином, что новый UI реально появился (изменённый текст/компонент), а не старый — только потом тестировать поведение. Не помечать native-фикс «verify pending», когда устройство подключено (см. правило 7 в `docs/NATIVE_COMPAT_RULES.md`).
- **Чипы-фильтры переупорядочиваются** при выборе (выбранные уходят в начало ряда) — позиции прыгают, перечитывай `Clickables` после каждого тапа.
- **Pinch** = два `adb input swipe ... &` + `wait` (одновременно). Иногда срабатывает как пан — повтори; для пана карты свайпь по ЧИСТОЙ области (не по строке поиска y≈700).
- **LogBox-тост** лежит поверх таб-бара и перехватывает тапы по нижним табам — закрывать перед тапами по табам. Деталь ошибки = тап по тосту → стектрейс с причиной (часто прямо называет проблему, напр. API URL).
- Радиокнопки/тогглы иногда срабатывают со 2-го тапа. `uiautomator dump` может вернуть stale-XML — перепроверять скрином.

## Надёжные хелперы qa.sh (P0) — предпочитать координатам
- `TapLabel "текст"` — тап по элементу по тексту/accessibilityLabel (центр bounds), БЕЗ ручных координат и путаницы tap/Tap. Для табов/кнопок/меню — всегда это.
- `DumpTexts` — весь видимый текст экрана (дешёвая текстовая альтернатива чтению картинки; для большинства проверок скрин не нужен).
- `WaitText "текст" [timeout]` / `AssertText "текст"` — дождаться/проверить текст (убивает тайминги с асинхронными данными — не снимай скрин до резолва).
- `Reload [sec]` — dev-меню → тап «Reload» по лейблу. `ColdStart [sec]` — force-stop → relaunch → выбрать dev-server (сбрасывает Zustand-сторы; нужен когда правка в сторах/контексте/persist не подхватывается Reload).

## Runtime-состояние без правок кода (P1) — `[QA-STATE]` / `__QA__()`
Dev-хук `utils/qaDebug.ts` (встроен в `app/_layout`) логирует в `ReactNativeJS` тег `[QA-STATE]` на каждой смене роута: `{route, isAuthenticated, userId, favorites, history, apiUrl}`. Читать: `adb logcat -d ReactNativeJS:V '*:S' | grep QA-STATE`. Это снимает необходимость вставлять временный `console.log` для диагностики (как было в саге с полками). Ручной дамп — глобал `__QA__()`. Если нужно новое поле — передай изменение `dumpQaState` исполнителю с повторным review; QA код не меняет.

## Карта входов в фичи (UI-навигация; deeplink `Nav` часто НЕ переключает с не-таб экрана)
- Нижние табы (TapLabel): «Маршруты» / «Карта» / «Квесты» / «Профиль» / «Ещё».
- «Ещё» (bottom-sheet, источник — `components/layout/bottomDockModel.ts`): Беларусь, Места, Статьи, Случайный маршрут, **«Вы смотрели»** (именно так, не «История просмотров»), Создать маршрут, Профиль + юр-страницы, Связаться с нами. «Книги путешествий» и «Попутчиков» в мобильном доке НЕТ: экспорт в PDF — только десктоп (`bottomDockModel.ts:78`), `/trips` живёт в гамбургере.
- Гамбургер (шапка ≡, TapLabel «Открыть меню», источник — `components/layout/CustomHeaderMobileMenu.tsx` + `constants/headerNavigation.ts`): Личный кабинет, Добавить путешествие, Мои путешествия, Мои точки, Сообщения, **Выход (НЕ тапать — роняет в лаунчер)**; «Экспорт в PDF» на мобильном скрыт (`CustomHeaderMobileMenu.tsx:173-174`). НАВИГАЦИЯ (`PRIMARY_HEADER_NAV_ITEMS`): Маршруты, Беларусь, Карта, Места, Попутчики, Случайный маршрут, Квесты.
- Профиль: кнопки Чаты / Мои точки / Календарь / Маршрут.
- **Квест офлайн-точки:** Квесты → открыть квест → топ-бар иконки ⬇ (скачать GPX) / ↗ (открыть в приложении) → системный share с `.gpx`.
- **Отзывы квеста:** ДЕТАЛЬ квеста (не список) → чип рейтинга `testID="quest-detail-rating"`
  (`app/(tabs)/quests/[city]/[questId].tsx:330-348`) → модалка читалки. Чип не рендерится вовсе,
  когда `ratingCount === 0`, — пустой экран здесь не баг, а отсутствие отзывов.
- **Карта квеста:** деталь квеста → скролл к блоку «Карта квеста».
- **Полки рекомендаций:** Маршруты → тап compass-иконку «Показать идеи путешествий» (данные favorites/history грузятся асинхронно — `WaitText`/`__QA__`, не снимай сразу).
- **GPX-трек travel:** деталь travel С треком → секция «Карта маршрута» → кнопка скачать (вход TBD: deeplink `travel/[id]` залипает, открывать через список; уточнить, у какого travel есть трек).
- **KML-экспорт точек:** «Мои точки» — кнопка экспорта (вход TBD: в списке точек её нет, искать на карте точек/в меню).

## Текущее состояние тест-аккаунта

Перед авторизованными действиями сверь `[QA-STATE]`/`__QA__()`: `apiUrl`,
`userId`, авторизацию и текущие данные. Зафиксируй target и выбранный тест-аккаунт
без секретов; прежние ID, email и счётчики не подтверждают состояние или права.
Разрешённые действия — по разделу ниже, сообщения имеют отдельный session gate.

## Карта экранов
Источник правды — `git ls-files 'app/*'`; сверяйся с ним, а не с этим списком.

`app/(tabs)/*.tsx`: index, map, search, travelsby, travels/[param], travel/[id],
travel/new (визард 6 шагов), quests (index, map, scenario, [city], [city]/[questId],
country/[country]), articles, article/[id], profile, user/[id], favorites, history,
subscriptions, messages, settings, export, userpoints, offline, login, registration,
set-password, accountconfirmation, about, cookies, privacy, terms, disclaimer,
community-rules, trip-rules, metravel, roulette, calendar, places.

**Планировщик поездок — отдельная ветка, её легко пропустить:** `trips/index`, `trips/my`,
`trips/community`, `trips/[id]`, `trips/plan/index`, `trips/plan/create`, `trips/plan/[id]`.

Вне `(tabs)`: `app/contact.tsx`, `app/[...missing].tsx` (404), `app/privacy-settings.tsx`,
`app/security-journal.tsx`, `app/register.tsx`, `app/error.tsx`.

## ПРАВИЛО №0 (load-bearing): СКРИН + UI/UX-оценка на КАЖДОМ экране и после КАЖДОГО действия
- На каждом экране и после каждого значимого действия — `Shot` (карты/тяжёлое — delay 8+), затем **критически оценить UI/UX по скрину**, а не только «открылось/нажалось».
- Оценивать: вёрстка/обрезки/пустые блоки (§9), иерархия и читаемость, контраст и тема (светлая+тёмная), touch-таргеты ≥44px, консистентность с дизайн-системой, понятность пользователю (нет «ступора»), состояния loading/empty/error, высота шапки ≤20% (§3), доступность последнего элемента списка (§2).
- **Каждую находку — с evidence:** сохрани воспроизведение, скрин и logcat;
  проверь Problem Memory и передай подтверждённый дефект через `ticket-board`
  профильному исполнителю. Изменённый код возвращается на QA после review.
- Серию скринов для UI/UX-оценки можно делегировать свежему субагенту (передать пути PNG) — он вернёт находки для отчёта и маршрутизации исполнителю.

## Обязательный чеклист интеракций (LOAD-BEARING — эти классы РЕАЛЬНО пропускались, проверять ВСЕГДА)
Эти проверки обязательны на КАЖДОМ релевантном экране — статичного скрина и «кнопка нажимается» НЕДОСТАТОЧНО:
1. **Toggle-действия в ОБЕ стороны.** Любой тоггл/статус тестировать И включением, И выключением — это РАЗНЫЕ кодовые пути, выключение падает независимо. Обязательно: избранное (добавить **и убрать**), подписка (follow **и unfollow**), план/статус поездки (поставить **и убрать**), лайк, mark/unmark. (Реальный баг: «не убирается из избранного, пишет ошибку» — add работал, remove падал.)
2. **Скролл КАЖДОГО списка до САМОГО конца + последний элемент.** Доскроллить до низа и убедиться, что ПОСЛЕДНЯЯ карточка/строка полностью видна НАД таб-баром (BottomDock ~56px+safe-area), а не спрятана за футером. Любой full-screen список на табе должен иметь `paddingBottom` под док. (Реальный баг: рулетка — последняя карточка за футером.)
3. **Высота шапок ≤ ~20% viewport + фильтры всегда доступны.** Фиксированная шапка (заголовок+фильтры+легенда) не должна занимать >20% высоты экрана; фильтры/контролы должны быть ДОСТУПНЫ всегда (sticky), а не уезжать со скроллом. Замерять по скрину (низ фикс-шапки vs высота экрана). (Реальные баги: календарь — шапка ~38%; Места — фильтры уезжают.)
4. **Фидбэк действия = смотреть на ошибку.** После КАЖДОГО действия читать `Logcat` (`ReactNativeJS:E`) и экран на error-тост/красный LogBox. Действие может «визуально пройти», но залогировать ошибку (успешный remove → тост «Ошибка» из-за упавшего refresh; автосейв формы → `ReferenceError: Property 'Event' doesn't exist`).
5. **Формы: реально ВВОДИТЬ и СОХРАНЯТЬ** (тест-аккаунт, латиница; пробелы в `adb input text` через `%s`). Краши web-глобалов (`Event`/`window`/`document` без `typeof`-гварда) и автосейв-падения вылезают ТОЛЬКО при реальном вводе/сохранении, не при просмотре. Подозреваемые места ищет скилл `android-native-audit`.
6. **Клавиатура.** Открыть клавиатуру на каждом тексто-вводе (особенно чат/комментарии/визард) и убедиться, что поле ввода видно НАД клавиатурой и не за таб-баром (adjustResize + абсолютный док взаимодействуют — поле уезжает).
7. **Реальные действия с тест-данными.** Выполняй разрешённые сценарием toggle,
   ввод/сохранение и создание тестовых сущностей; после проверки удали созданное
   и восстанови исходные toggles. Отправка сообщений допустима только при явной
   авторизации в текущей сессии на адресата и действие; наличие тест-аккаунта
   само по себе её не даёт. Без неё проверь ввод/отмену без отправки.
8. **Нажать КАЖДОЕ вторичное/icon-действие — топ-бары, overflow (⋯), плавающие контролы.** Самый пропускаемый класс: иконки в шапке деталки и под `⋯` — их легко не заметить, потому что они не главная кнопка. Обязательно нажимать: **скачать / экспорт / офлайн-точки / «открыть в приложении» / поделиться / печать / аудио-озвучка / сброс**. Native-only пути (файловая система, share-intent, Linking, expo-модули) часто падают там, где web-путь работает — поэтому «работает на web» НЕ закрывает сценарий, его надо нажать на устройстве и прочитать `Logcat`. (Реальный баг: квест → скачать/подготовить офлайн-точки → тосты «Не удалось подготовить/открыть точки» — legacy `expo-file-system` бросает в рантайме (проект уже на expo ~57.0.4); web-путь работал, поэтому отчёт «всё ок» был ложным.)
9. **Аудит целостности вёрстки на КАЖДОМ скрине — критически смотреть, не «нажимается ли», а «не развалилось ли».** Флагить как баг: текст усечён в мусор (локация → «.:..», «Ита.…», заголовок → двухстрочный перенос в узкой колонке); элемент обрезан краем экрана или перекрыт плавающим контролом (напр. аудио-плеер налезает на карточку); пустой переразмеренный блок (большая дыра без контента); карточка/контейнер шире вьюпорта или контент срезан фикс-высотой (`overflow:hidden`); FAB/док перекрывает метаданные нижней карточки; блок «разъезжается» при другой высоте/плотности. Оценивать по РЕАЛЬНОЙ ширине устройства; одного «кнопка есть» недостаточно. Эту визуальную оценку каждого скрина делать критическим взглядом (или делегировать свежему субагенту-ревьюеру по серии скринов) — именно она ловит то, что adb-дамп не видит.
10. **ЗАКРЫВАЕМОСТЬ каждого popup / sheet / modal / карточки / fullscreen-overlay — в КАЖДОМ его состоянии.** Любой не-инлайн оверлей ОБЯЗАН закрываться из UI. На каждом проверять ВСЕ способы дисмисса: кнопка ✕ И свайп-вниз И тап-вне (backdrop) И системный back. Системный back — это ФОЛБЭК, а НЕ «закрывается»: если убрать оверлей можно только аппаратным back — это баг (юзер не знает про back). И главное: сначала РАСКРЫТЬ все под-секции оверлея («Ещё»/expand/аккордеон), ПОТОМ пробовать закрыть в раскрытом виде — close-кнопка часто ломается/уезжает за пределы экрана, когда карточка растёт. (Реальный баг #497: ✕ карточки места — no-op на native, свайп отключён, фуллскрин-фото не закрывалось, и при «Ещё» ✕ уезжала за статус-бар → закрыть можно было только системным back.)
11. **ВСЕ СОСТОЯНИЯ компонента, не только дефолтное.** Каждый компонент гонять во всех состояниях: компакт/раскрыт, свёрнут/открыт, пусто/заполнено, обычный/fullscreen, мало контента/много. Фикс или фича, проверенные ТОЛЬКО в дефолтном состоянии, пропускают баги в остальных. Особенно после исправления — не «сработала ли моя кнопка», а «работает ли во всех состояниях, не сломалось ли соседнее».
12. **Overlay / z-order: при открытии оверлея проверить, что нижние плавающие контролы СКРЫТЫ и не просвечивают сквозь него.** Когда поверх карты/списка открывается карточка/sheet/modal — нижние тулбары/FAB/иконки контролов должны прятаться или быть перекрыты, а не «светить» поверх контента оверлея. Смотреть на скрин: нет ли иконок фона на карточке. (Реальный баг #497: верхний тулбар карты — фильтры/слои/список — рисовался поверх фото карточки места, потому что гейтился только на `sheetState!=='full'`, а не на `hasSelectedPlace`.)

## Пайплайн
1. **План**: список фаз (экраны → интеракции → темы → evidence → retest)
   по Task Contract, platform/localization impact и разрешённым действиям.
2. **Обход**: deeplink/таб на каждый роут → `Shot` → оценка (вёрстка/тёмная+светлая тема/контраст/обрезки/пустые блоки/счётчики/**высота шапки ≤20%**) → интеракции (каждая кнопка через `Clickables`+`Tap`, скроллы медленные 400–800мс И быстрые флики 150мс, **скролл до конца списка**, **тогглы в обе стороны**, **реальный ввод+сохранение форм с тест-аккаунтом**, после каждого действия — `Logcat` на ошибку). ПРОЙТИ «Обязательный чеклист интеракций» выше на каждом экране. Оценивать против эталонов (карта → Google Maps / Organic Maps).
3. **Отчёт**: `.codex-temp/android-qa/QA_REPORT_<ЭКРАН>.md` — таблица «Статус обхода» + находки `### F-NN (severity) Заголовок` (critical/major/minor/info) со скринами и (если есть) ссылкой на файл:строку root cause. Краш → сразу `Logcat` за стектрейсом, корень в коде (web-only импорты — главный подозреваемый, см. `docs/NATIVE_COMPAT_RULES.md`).
4. **Борд**: сначала `$metravel-problem-memory`, затем агент `ticket-board` — тикеты `[AND-NN] суть`,
   **area=front** с Android/device context в title, description, `Platform impact`
   и validation. В active workflow нет `area=android`; бэк не править —
   подтверждённый API/server defect оформляется как `area=back`. Привязывай
   задачу к текущему active sprint и указывай `Localization impact`.
5. **Передача дефекта**: native-поведение — `android-expert`, карта —
   `map-expert`; исполнитель исправляет код, получает code review и возвращает
   проверенный diff в `testing`. Только затем QA повторяет сценарий на устройстве
   и релевантную web-проверку; статус определяется evidence по Task Contract.
   **Пост-фикс верификация — НЕ happy-path (load-bearing, иначе фикс ложно «зелёный»):** после Reload (1) убедиться скрином, что новый код реально на устройстве; (2) проверить НЕ только тот путь, что менял, а ВСЕ состояния тронутого компонента (компакт/раскрыт/fullscreen/пусто-полно — чеклист п.11); (3) посмотреть на ВЕСЬ скрин критически, не «сработала ли моя кнопка» — не появилось ли наложений/просвечивания/смещений соседних элементов (п.9, п.12); (4) для оверлеев — перепройти закрываемость во всех состояниях (п.10). Реальный провал: #497 закрыли «по кнопке в компакте», а в раскрытом виде ✕ снова ломалась и контролы карты светили на карточку — потому что проверили узко. Узкая пост-фикс проверка = повторный пропуск.
6. **Регресс**: повтори затронутые runtime-сценарии и соседние состояния.
   Code-level checks выполняет исполнитель/reviewer; дополнительные тяжёлые
   проверки запускай только по Task Contract и operation gate.

## Что МОЖНО и что НЕЛЬЗЯ во время обхода

В разрешённой QA-сессии выполняй сценарии на текущем тест-аккаунте: создавать и
удалять только собственные тестовые сущности, переключать избранное/статусы,
проходить квесты, вводить и сохранять формы. После теста восстанови состояние.
Сообщения отправляй только при явной session authorization адресата и действия;
согласование, уже полученное в этой сессии, повторно не запрашивай.

Не редактируй feature code, не удаляй чужие/существующие prod-данные, не выполняй
платежи или массовую рассылку. Имя тест-аккаунта не разрешает внешние мутации;
разрешение на prod-таргет само по себе не разрешает отправку сообщений.

## Проверка по platform impact (обязательное правило)

Shared/common responsive UI проверяется на desktop web и mobile web (~390px, `isMobile`). Общий файл или компонент сам по себе не создаёт Android/iPhone device gate.

- **Native device validation только для platform-specific scope.** Android-specific поведение, конфигурацию или runtime проверяй на Android; iOS-specific — на требуемом simulator/physical iPhone/TestFlight layer. Parity остаётся архитектурным инвариантом, а не требованием прогонять common/shared задачу на всех устройствах.
- **Evidence по shared/common UI:** desktop web + mobile web screenshots. Native screenshots нужны только для затронутой Android- или iOS-specific поверхности.
- **Запрещены web-only визуальные ветвления в мобильном вьюпорте:** serif-шрифты и hover-only элементы — только desktop (`!isMobile`); контент-элементы (чипы, бейджи, кнопки) не скрывать через `Platform.OS === 'web'`, если на устройстве они видны.
- **Темизация:** для тематических поверхностей только `useThemedColors()` — `DESIGN_TOKENS.colors.*` на native это статичный светлый fallback, на web — живые CSS-переменные.
- **Попапы/карточки точек на картах** — один общий компонент на всех страницах и платформах (различия — только добавочный функционал), компактный, вся информация видна без обрезания по X и Y.
