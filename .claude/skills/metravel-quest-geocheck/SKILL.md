---
name: metravel-quest-geocheck
description: >-
  Гео-сверка точек городского квеста metravel.by: для каждой точки находит место
  на карте (OSM/Nominatim) и сверяет с сохранёнными координатами — ловит маркеры
  на парковке/остановке/дороге и точки не на том объекте, предлагает исправленные
  координаты. Работает по локальному data-файлу (при создании, до заливки) и по
  живому квесту с прода. Триггеры: «сверь точки квеста с картой», «проверь
  координаты квеста», «правильно ли стоят точки», «проверь гео перед заливкой».
---

# metravel-quest-geocheck

Регламент гео-сверки точек квеста. Цель — чтобы координаты КАЖДОЙ точки стояли
на самом объекте из задания, а не на соседней парковке, остановке, дороге или
одноимённом POI в другом месте. Запускай этот скилл при **создании** квеста
(до миграции на прод) и при **редактировании/ревизии** существующего.

Исполнитель — агент `quest-geo-verifier`. Контент заданий/подсказок — отдельно
(`quest-editor`); создание квеста — скилл `metravel-quest`.

## Принципы (ОБЯЗАТЕЛЬНО)

1. **Сверяй и forward, и reverse.** Недостаточно найти объект по имени
   (forward). Главную ошибку — «точка на парковке/остановке/дороге» — ловит
   только reverse-геокод самой точки (что РЕАЛЬНО стоит в этих координатах).
   Инструмент делает оба.
2. **Точка — на объекте из `story`/`task` или на его входе.** В плотном старом
   городе расхождение 20–30 м уже уводит человека не туда. Для входов/фасадов
   целься в ≤15 м.
3. **FAIL — это «проверь», а не «исправь».** Геокодер часто матчит не тот
   объект (одноимённая улица/рынок в другом городе). Финальное решение — только
   по подтверждённому OSM-объекту или reverse самой точки + веб-поиск.
4. **Берегись одноимённых остановок.** `highway/*`, `*bus_stop*`, `*tram_stop*`,
   `*/parking` в кандидатах — это НЕ твой объект (так ломались Полоцк-монастырь,
   Витебск-амфитеатр, Вроцлав-Hala). Бери ближайший реальный POI-кандидат.
5. **Опциональные точки-паузы не трогай.** Кафе/видовые (`type any`, пометки
   «по желанию») часто дают WARN «no result» — это норма, не ошибка.
6. **Сам на прод не пиши.** Скилл только находит и предлагает координаты.
   Применение: в локальный data-файл (новый квест) или патчем через
   `apply-quest-patches.js` / тикетом (квест на проде).
7. **Уважай Nominatim.** Публичный инстанс без ключа: `User-Agent` обязателен
   (скрипт шлёт), пауза ≥1.1 с между запросами (скрипт держит), без параллельных
   прогонов — иначе бан по IP.
8. **Проверяй не только ТОЧКИ, но и ПОРЯДОК маршрута (anti-backtracking).** Мало,
   чтобы каждая точка стояла на объекте — сам порядок шагов (`order`) должен быть
   связным пешим путём без возвратов. Выпиши координаты в порядке `order` и убедись,
   что движение монотонно по главной оси города (напр. широта север→юг) и ни один
   переход между соседними шагами не разворачивает игрока назад к уже пройденной
   зоне. АНТИПАТТЕРН: 1→2→3 на юг, потом 4 возвращает к точке 1, потом 5 снова на
   юг. Нашёл возврат — предложи новый `order` (greedy nearest-neighbor от старта).
   Реордер прод-квеста применяют, меняя только поле `order` (не `step_id`, прогресс
   сохраняется) — см. `metravel-quest` шаг 4b/4c.

## Инструмент

`scripts/quest-geocheck.js` (read-only). Для каждого шага печатает:
`stored` (текущие координаты + maps_url) · `here` (reverse: что в точке;
`⚠ infra` = инфраструктура) · `address` (геокод заявленного `location` + улица/дом
из reverse, `⚠ street mismatch`) · `search` + кандидаты с расстоянием и
`class/type` · `verdict` (OK / WARN / FAIL / SKIP) + причина · `suggest` (готовые
исправленные `lat, lng` + maps_url, когда вердикт не OK).

**Адресное измерение** (то, что просили: «адрес ul. Szeroka 40, а точка на
парковке»): скрипт геокодит `location` и сравнивает с точкой + сверяет улицу/дом
из reverse. FAIL по адресу — ТОЛЬКО при номере дома (улица без дома геокодится в
произвольную точку, ложняк). Ещё ловушка: если в точке reverse показывает не-infra
POI (костёл/музей), адресное расхождение часто ложное — у крупного здания № дома
стоит в стороне. Решай по совокупности reverse + forward + address.

`scripts/quest-poi-suggest.js --quest-id=<id>` — подсказка ОПЦИОНАЛЬНЫХ точек
рядом с маршрутом (Overpass/OSM): музеи (`★` notable — напр. Czartoryski с «Дамой
с горностаем»), кошерные/еврейские рестораны, кофейни, аттракции.
`--kinds=museum,kosher,cafe,gallery,attraction`, `--radius=<m>`, `--json`.
Read-only — редактор сам решает, что добавить как опц. точку (`type any`).

```bash
# Новый квест: по локальному файлу данных, до заливки
node scripts/quest-geocheck.js --source-file=scripts/<city>-quest-data.js --quest-id=<quest_id>

# Существующий квест: с прода
NODE_TLS_REJECT_UNAUTHORIZED=0 node scripts/quest-geocheck.js --quest-id=<quest_id>

# Все квесты с прода (длинно — Nominatim троттлинг)
NODE_TLS_REJECT_UNAUTHORIZED=0 node scripts/quest-geocheck.js

# Опции: --threshold=<m> (дефолт 60; плотный центр 80–100; точечный POI 40)
#        --limit=<n> кандидатов (дефолт 3) · --json машинный вывод
```

## Вердикты

| verdict | что значит | действие |
|---|---|---|
| OK | реальный POI в пределах порога, точка не на инфраструктуре | обычно не трогать |
| WARN infra | reverse: парковка/остановка/дорога в точке | разобрать, чаще всего перенести |
| WARN no result | геокодер не нашёл по запросу | основная точка — уточнить вручную; опциональная — норма |
| FAIL | ближайший объект дальше порога | проверить вручную, не доверять вслепую |
| SKIP | нет названия для поиска | intro/служебные — ок |

## Процесс

1. **Данные.** Создание — локальный `--source-file`. Ревизия — `--quest-id`
   с прода (актуальные данные, не старые копии).
2. **Прогон** `quest-geocheck.js`. Сохрани отчёт в `.quest-audit/` (gitignored).
3. **Разбор** каждого WARN/FAIL вручную: подтверди объект веб-поиском и прямым
   Nominatim (`/search?format=jsonv2&q=...`, `/reverse?format=jsonv2&zoom=18&lat=..&lon=..`,
   `User-Agent`, пауза ≥1.1 с). Смотри `class/type` — не остановка ли.
4. **Патчи** `{ step_db_id, lat, lng, maps_url, source, note }`: координаты —
   6 знаков, `maps_url = https://maps.google.com/?q=<lat>,<lng>`, на каждую —
   источник.
5. **Применение** (не в этом скилле):
   - новый квест → впиши `lat/lng/mapsUrl` в `scripts/<city>-quest-data.js`,
     перепрогони geocheck по файлу → 0 реальных проблем → дальше миграция
     (скилл `metravel-quest`);
   - квест на проде → `scripts/apply-quest-patches.js` (`PATCH /api/quest-steps/<id>/`).
     Токен из `~/.metravel_token`; если он невалиден (HTTP 401) — взять свежий
     логином e2e: `METRAVEL_TOKEN=$(node scripts/get-quest-token.js)
     node scripts/apply-quest-patches.js .quest-audit/patches-geo-<id>.json`
     (сначала `--dry-run`). Токен/пароль не логировать. Применяют
     `quest-editor`/владелец, либо тикет на борде при необходимости полевой
     проверки координат.
6. **Контроль** после правок: повторный прогон geocheck — целевые точки больше
   не WARN-infra/FAIL по реальной причине.

## Связанное

- `quest-geo-verifier` — агент-исполнитель этого скилла.
- `quest-editor` — качество заданий/подсказок/`answer_pattern` (не координаты).
- `metravel-quest` — создание квеста; вызывай geocheck перед заливкой (шаг 4).
- `scripts/audit-quest-coordinates.js` — более старый forward-only аудит
  (geocheck его расширяет reverse-геокодом и подсказками координат).
