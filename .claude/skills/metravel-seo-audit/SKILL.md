---
name: metravel-seo-audit
description: >-
  Аудит и улучшение SEO существующих путешествий на metravel.by: поиск статей,
  которые нужно довести до топа поисковиков. Используй, когда нужно найти статьи
  с проблемами SEO (длинные/слабые заголовки, пустые meta_description, тонкий
  текст, нет внутренних ссылок), приоритизировать их по трафику и дополнить текст
  достоверной информацией (история, легенды, практика) — БЕЗ замены фотографий.
  Триггеры: «проанализируй SEO статей», «какие статьи улучшить для поиска»,
  «допиши мету и историю», «вывести сайт в топ», «SEO-аудит автора».
---

# metravel-seo-audit

Регламент SEO-аудита и дотягивания существующих путешествий до топа поисковой
выдачи (Google + Yandex). Не путать с `metravel-travel-article` — там создание
статей из папок с фото; здесь — улучшение УЖЕ опубликованного контента.

## Жёсткие правила

1. **Фото не меняем.** Разрешено только ДОБАВЛЯТЬ достоверную информацию в текст
   (история места, легенды, топонимика, факты, практика). Существующие фото и
   обложку не трогаем; можно лишь добавлять фото при явном запросе.
2. **Только достоверное.** Даты, имена, цены, часы — проверяй веб-поиском. Где не
   уверен — формулируй как «по местным преданиям…», а не как факт. Цены/часы
   помечай «актуально на <год>, уточняйте».
3. **Текст не выхолащивай.** Дополняй живой авторский текст, не переписывай его в
   шаблон. Запрещённые секции-маркеры генерации: «Вступление / Что увидели /
   Итог / Точки / Описание». Заголовки — осмысленные (`<h2>`): названия мест или
   тематические («Что посмотреть», «Как добраться», «История», «Легенды и факты»,
   «Практическая информация», «Маршрут в цифрах»).

## Шаг 1 — Аудит (найти проблемы)

```bash
npm run seo:audit                 # автор по умолчанию user_id=1 (Julia)
node scripts/seo-audit.js --user-id 1 --json /tmp/seo-report.json
node scripts/seo-audit.js --user-id 1 --limit 50 --min-words 500
```

Скрипт тянет все опубликованные путешествия автора и проверяет каждое:

| Проблема | Порог | Почему важно |
|---|---|---|
| `title-too-long` | > 60 симв. | заголовок обрезается в выдаче |
| `title-too-short` | < 25 симв. | нет поискового ключа в заголовке |
| `meta-missing` | пусто | сниппет генерится поисковиком как попало |
| `thin-content` | < 400 слов | слабая релевантность и время на странице |
| `no-headings` | 0 × h2/h3 | нет структуры под подзапросы |
| `no-internal-links` | 0 ссылок на `/travels/` | теряется ссылочный вес и обход |

`priority` = число проблем × вес трафика (страницы с трафиком, но тонкие/без меты —
самый высокий ROI). Бери worklist из вывода или из `--json`.

## Шаг 2 — Улучшить текст (по одной статье)

Для каждой статьи из worklist:

1. Получи текущий текст: `GET https://metravel.by/api/travels/{id}/` (поля
   `name`, `description`, `meta_description`).
2. Реши, что добавить под её проблемы:
   - `thin-content` / `no-headings` → дополни блоки `<h2>`: **История/происхождение**
     (кто/когда построил, геология озёр и гор), **Легенды и факты** (предание,
     топонимика, цифры: высота, глубина, год), **Практическая информация**
     (координаты, как добраться, парковка, билеты/часы на текущий год, длина и
     сложность маршрута, сезон), **Что рядом** (2–4 внутренние ссылки на соседние
     путешествия автора).
   - `no-internal-links` → добавь ссылки `<a href="/travels/{slug}">…</a>` на
     релевантные статьи (узнай slug через `seo:audit --json` или список API).
   - `title-too-long` → сократи `name` до ≤ 60 симв., главный ключ в начало;
     хвост перенеси в meta_description.
   - `meta-missing` → напиши `meta_description` 80–160 симв. с ключом и зацепкой.
3. Применяй изменения через движок публикации (PUT сохраняет name/description/year):
   `python3 .claude/skills/metravel-travel-article/scripts/metravel_publish.py`
   (см. `_put_with_desc` / `upsert`). Токен — из окружения, секреты не логируй.

## Шаг 3 — Опорные (pillar) статьи для топа

Чтобы вытянуть кластер в топ, добавляй хабовые статьи (1500–2500 слов) со
ссылками на 8–15 существующих материалов. Сильные кластеры автора: Беларусь
(усадьбы, озёра, экотропы, веломаршруты), Польша/Краков (окрестности, купание,
Татры/Закопане), сезонные (Energylandia/парки). Создание pillar-статьи — через
скилл `metravel-travel-article`.

## Шаг 4 — Проверка

- `npm run seo:audit -- --limit <N>` повторно — проблемы по обновлённым статьям
  должны уйти.
- `npx jest __tests__/scripts/seo-audit.test.ts` — зелёные тесты аудита.
- После релиза статичные страницы `dist/prod/travels/{slug}.html` обязаны нести
  свой `<title>`, `meta description`, `og:image` и текст в HTML (генерит
  `npm run seo:generate-pages`); проверь `npm run test:seo:prod`.

## Важно про прод

Per-page SEO собирается на сборке (`generate-seo-pages.js`) в статические HTML.
Если прод отдаёт ботам генерик `Путешествие | Metravel` и `og:url=.../[param]` —
значит статические страницы не передеплоены или новые статьи добавлены после
последней сборки. Лечится пересборкой+деплоем (`build:web:prod` →
`seo:generate-pages`), не правкой кода рантайма.
