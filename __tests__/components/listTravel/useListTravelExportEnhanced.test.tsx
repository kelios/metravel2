// Устаревшие тесты для useListTravelExportEnhanced.
// Хук удалён и заменён на useListTravelExport, поэтому этот набор тестов помечаем как пропущенный,
// чтобы он не ломал общий прогон Jest, но при этом сохранял исторический контекст.

describe.skip('useListTravelExportEnhanced (deprecated)', () => {
  it('deprecated test skipped', () => {
    // intentionally empty
  });
});

