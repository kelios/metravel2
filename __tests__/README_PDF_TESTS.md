# Тесты для PDF экспорта

## 📋 Обзор

Создан полный набор тестов для проверки всех элементов в экспортируемом PDF и превью.

## 📁 Структура тестов

```
__tests__/
├── services/pdf-export/
│   ├── TravelDataTransformer.test.ts    # Тесты трансформации данных
│   ├── PdfExportService.test.ts        # Интеграционные тесты сервиса
│   └── EnhancedPdfGenerator.test.ts    # Тесты улучшенного генератора книги
├── utils/
│   └── pdfContentValidation.test.ts    # Полная проверка содержимого HTML
└── hooks/
    └── usePdfExport.test.tsx           # Тесты React hook
```

## 🧪 Что проверяют тесты

### 1. TravelDataTransformer.test.ts
- ✅ Валидация данных (пустые массивы, невалидные типы)
- ✅ Трансформация путешествий в формат для PDF
- ✅ Обработка галереи (массивы строк и объектов)
- ✅ Фильтрация пустых значений
- ✅ Преобразование travelAddress
- ✅ Обработка null/undefined значений

### 2. Тесты генерации HTML книги
- ✅ **Обложка**: заголовок, подзаголовок, количество путешествий, год, имя пользователя, дата создания, брендинг
- ✅ **Оглавление**: наличие, названия путешествий, страна, год, миниатюры
- ✅ **Страницы путешествий**: фото, название, мета-информация, описание, рекомендации, плюсы, минусы, QR-коды, ссылки
- ✅ **Галерея**: все фотографии, название, количество
- ✅ **Обработка пустых данных**: без описания, без галереи, без изображений
- ✅ **Множественные путешествия**: правильная обработка нескольких путешествий
- ✅ **Сортировка**: по дате (desc/asc), по стране, по алфавиту
- ✅ **Заключительная страница**: наличие и содержимое
- ✅ **Структура HTML**: валидность, стили, экранирование

### 3. pdfContentValidation.test.ts
- ✅ **Обязательные элементы**: обложка, оглавление, страницы путешествий, галерея, заключительная страница
- ✅ **Содержимое обложки**: все элементы присутствуют
- ✅ **Содержимое оглавления**: все элементы присутствуют
- ✅ **Содержимое страниц путешествия**: все элементы на странице с фото и текстом
- ✅ **Содержимое галереи**: все фотографии присутствуют
- ✅ **QR-коды и ссылки**: наличие и правильность
- ✅ **Номера страниц**: присутствуют на всех страницах
- ✅ **Стили и форматирование**: все необходимые стили
- ✅ **Валидация данных**: обработка пустых значений
- ✅ **Полная проверка структуры**: правильная структура HTML документа

### 4. PdfExportService.test.ts
- ✅ Успешный экспорт PDF
- ✅ Вызов progress callback
- ✅ Обновление прогресса от 0 до 100
- ✅ Обработка ошибок (пустые массивы, невалидные данные)
- ✅ Создание превью
- ✅ Подписка на прогресс

### 5. usePdfExport.test.tsx
- ✅ Инициализация с правильными значениями
- ✅ Успешный экспорт PDF
- ✅ Обновление isGenerating во время экспорта
- ✅ Обновление прогресса
- ✅ Обработка ошибок
- ✅ Создание превью (iframe и кнопка закрытия)
- ✅ Использование конфигурации

## 🚀 Запуск тестов

### Все тесты PDF экспорта
```bash
npm test -- pdf
```

### Конкретный файл
```bash
npm test -- TravelDataTransformer
npm test -- EnhancedPdfGenerator
npm test -- pdfContentValidation
npm test -- PdfExportService
npm test -- usePdfExport
```

### С покрытием
```bash
npm test -- --coverage pdf
```

## ✅ Проверяемые элементы

### Обложка
- [x] Заголовок
- [x] Подзаголовок
- [x] Имя пользователя
- [x] Количество путешествий
- [x] Год/диапазон лет
- [x] Дата создания
- [x] Брендинг MeTravel
- [x] Фоновое изображение или градиент

### Оглавление
- [x] Заголовок "Содержание"
- [x] Названия путешествий
- [x] Страна
- [x] Год
- [x] Миниатюры
- [x] Номера страниц

### Страницы путешествий
- [x] Страница с фото
  - [x] Название
  - [x] Страна, город
  - [x] Год, месяц
  - [x] Количество дней
  - [x] Обложка путешествия
- [x] Страница с текстом
  - [x] Описание
  - [x] Рекомендации
  - [x] Плюсы
  - [x] Минусы
  - [x] QR-код
  - [x] Ссылка на онлайн-версию

### Галерея
- [x] Заголовок "Фотогалерея"
- [x] Название путешествия
- [x] Все фотографии из галереи
- [x] Количество фотографий
- [x] Номера фотографий

### Заключительная страница
- [x] Текст благодарности
- [x] Брендинг MeTravel

### Технические элементы
- [x] Валидная структура HTML
- [x] Стили CSS
- [x] Размеры страниц (210mm x 297mm)
- [x] Разрывы страниц
- [x] Экранирование HTML
- [x] Атрибуты изображений (crossorigin)
- [x] Номера страниц

## 📊 Покрытие

Тесты покрывают:
- ✅ Все обязательные элементы PDF
- ✅ Все опциональные элементы (галерея, оглавление)
- ✅ Обработку пустых данных
- ✅ Множественные путешествия
- ✅ Различные настройки (includeToc, includeGallery)
- ✅ Сортировку путешествий
- ✅ Валидацию данных
- ✅ Обработку ошибок
- ✅ Прогресс экспорта

## 🔍 Примеры использования

### Проверка наличия всех элементов
```typescript
const html = await buildPhotoBookHTML(travels, settings);
const parsed = parseHTML(html);

expect(parsed.hasCover).toBe(true);
expect(parsed.hasToc).toBe(true);
expect(parsed.hasTravelPages).toBe(true);
expect(parsed.hasGallery).toBe(true);
expect(parsed.hasFinalPage).toBe(true);
```

### Проверка содержимого
```typescript
expect(html).toContain('Мои путешествия'); // Заголовок
expect(html).toContain('TestUser'); // Имя пользователя
expect(html).toContain('2024'); // Год
expect(html).toContain('https://example.com/gallery1.jpg'); // Фото
```

### Проверка структуры
```typescript
expect(html).toMatch(/<!doctype html>/i);
expect(html).toMatch(/<html[^>]*>/i);
expect(html).toMatch(/<head>/i);
expect(html).toMatch(/<body>/i);
```

## 🐛 Известные ограничения

1. **Типы Jest**: TypeScript может показывать ошибки для `describe`, `it`, `expect` - это нормально, тесты работают при запуске через Jest
2. **DOMParser**: В тестовом окружении используется упрощенный парсер на основе регулярных выражений
3. **Моки**: Некоторые зависимости (html2pdf, qrcode) замокированы для тестов

## 🧱 Актуальная архитектура экспорта PDF

После рефакторинга в проекте используется **единый путь экспорта**:

- UI-кнопки экспорта (список путешествий, деталка, шэринг)
- → `BookSettingsModal` / `BookSettingsModalLazy`
- → `useListTravelExport` / `useSingleTravelExport`
- → `usePdfExport`
- → `BookHtmlExportService` + `EnhancedPdfGenerator`
- → HTML-превью в новом окне браузера
- → стандартная печать/"Сохранить как PDF" через браузер

Article-PDF экспорт и связанные с ним сервисы/тесты **удалены**. Все новые сценарии экспорта должны строиться поверх `usePdfExport` и `BookHtmlExportService`.

Основные точки входа, которые важно учитывать при написании тестов:

- `usePdfExport` — единый хук для экспорта набора путешествий
- `useSingleTravelExport` — тонкая обёртка над `usePdfExport` для одиночного travel
- `useListTravelExport` — управляющая логика выбора путешествий + интеграция с `usePdfExport`
- `EnhancedPdfGenerator` — генерация HTML-книги по данным и настройкам

## 📝 Как писать новые тесты при доработках

При добавлении новых возможностей экспорта (новые поля `BookSettings`, новые блоки в PDF, новые темы и т.п.) рекомендуется:

1. **Сервисы и генератор**
   - Добавить юнит-тесты в `__tests__/services/pdf-export/EnhancedPdfGenerator.test.ts` или соседние файлы
   - Проверить, что новые поля/блоки корректно попадают в HTML
   - Для новых тем — проверить ключевые элементы оформления (цвета, шрифты, блоки)

2. **Хуки экспорта**
   - Дополнить `__tests__/hooks/usePdfExport.test.tsx`, если логика прогресса/ошибок/конфигурации меняется
   - При изменениях в `useSingleTravelExport` или `useListTravelExport` — добавить отдельные тесты для них (инициализация, вызов `openPrintBook`, работа с настройками)

3. **Интеграционные проверки HTML**
   - Для новых блоков/секций в книге — обновить или добавить кейсы в `__tests__/utils/pdfContentValidation.test.ts`
   - Проверять не только наличие текста, но и структуру (class-ы, порядок блоков, наличие критических атрибутов)

4. **Регрессия по существующим сценариям**
   - После существенных изменений в экспорте всегда запускать полный набор pdf-тестов:

     ```bash
     npm test -- pdf
     ```

   - Следить, чтобы не ломались ключевые сценарии: обложка, оглавление, страницы путешествий, галерея, финальная страница.

## 📝 Примечания

- Тесты используют моки для внешних зависимостей
- Регулярные выражения используются для проверки HTML (более надежно, чем DOMParser в тестах)
- Все тесты асинхронные, так как генерация HTML и экспорт PDF асинхронны
- Тесты проверяют как структуру, так и содержимое


