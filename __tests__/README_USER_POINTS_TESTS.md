# Тесты функционала "Мои точки"

Этот документ описывает тесты для нового функционала импорта и управления пользовательскими точками.

## 📋 Покрытие тестами

### 1. Парсеры (`__tests__/parsers/`)

#### `googleMapsParser.test.ts`
Тестирует парсинг данных из Google Maps:
- ✅ Парсинг JSON формата (Google Takeout)
- ✅ Парсинг KML формата
- ✅ Обработка невалидных данных
- ✅ Автоопределение категорий по ключевым словам
- ✅ Обработка отсутствующих координат

#### `osmParser.test.ts`
Тестирует парсинг данных из OpenStreetMap:
- ✅ Парсинг GeoJSON формата
- ✅ Парсинг GPX формата
- ✅ Автоопределение категорий по тегам
- ✅ Обработка невалидных данных
- ✅ Фильтрация точек без координат

### 2. Компоненты (`__tests__/components/UserPoints/`)

#### `PointCard.test.tsx`
Тестирует отображение карточки точки:
- ✅ Рендеринг названия, описания, адреса
- ✅ Отображение категории и статуса
- ✅ Отображение рейтинга
- ✅ Цветовой индикатор
- ✅ Обработка отсутствующих полей

#### `PointFilters.test.tsx`
Тестирует фильтры точек:
- ✅ Отображение всех цветовых фильтров
- ✅ Добавление/удаление фильтров
- ✅ Визуальное выделение выбранных фильтров
- ✅ Callback при изменении фильтров

#### `ImportWizard.test.tsx`
Тестирует мастер импорта:
- ✅ Выбор источника данных (Google Maps/OSM)
- ✅ Выбор формата файла
- ✅ Выбор цвета для точек
- ✅ Навигация между шагами
- ✅ Индикаторы прогресса
- ✅ Кнопки отмены и возврата

### 3. Страница (`__tests__/app/`)

#### `userpoints.test.tsx`
Тестирует главную страницу:
- ✅ Проверка авторизации
- ✅ Редирект на страницу входа
- ✅ Отображение списка точек для авторизованных
- ✅ FAB кнопка для импорта
- ✅ Открытие модального окна импорта

### 4. Типы (`__tests__/types/`)

#### `userPoints.test.ts`
Тестирует TypeScript типы:
- ✅ Enum значения для цветов
- ✅ Enum значения для категорий
- ✅ Enum значения для статусов
- ✅ Маппинг цветов на метки
- ✅ Маппинг категорий на метки
- ✅ Маппинг статусов на метки

## 🚀 Запуск тестов

### Все тесты
\`\`\`bash
npm test
# или
yarn test
\`\`\`

### Только тесты пользовательских точек
\`\`\`bash
npm test -- __tests__/parsers/
npm test -- __tests__/components/UserPoints/
npm test -- __tests__/app/userpoints
npm test -- __tests__/types/userPoints
\`\`\`

### Конкретный файл
\`\`\`bash
npm test -- __tests__/parsers/googleMapsParser.test.ts
\`\`\`

### С покрытием кода
\`\`\`bash
npm test -- --coverage
\`\`\`

### В watch режиме
\`\`\`bash
npm test -- --watch
\`\`\`

## 📊 Что тестируется БЕЗ бэкенда

### ✅ Можно протестировать:
1. **Парсеры** - полностью работают без бэкенда
   - Парсинг JSON/KML/GeoJSON/GPX
   - Валидация данных
   - Автоопределение категорий

2. **UI компоненты** - рендеринг и взаимодействие
   - Отображение карточек точек
   - Фильтры
   - Навигация в мастере импорта

3. **Типы и константы** - проверка структуры данных
   - Enum значения
   - Маппинги меток

4. **Логика авторизации** - проверка доступа к странице

### ⚠️ Требует бэкенда:
1. **API запросы**
   - Импорт точек на сервер
   - Получение списка точек
   - CRUD операции
   - Построение маршрутов

2. **Интеграционные тесты**
   - E2E тесты с реальным API
   - Тесты загрузки файлов

## 🧪 Примеры использования

### Тестирование парсера
\`\`\`typescript
import { GoogleMapsParser } from '@/src/api/parsers/googleMapsParser';

const jsonData = {
  features: [{
    geometry: { coordinates: [30.5, 50.4] },
    properties: { Title: 'Test Place' }
  }]
};

const result = (GoogleMapsParser as any).parseJSON(JSON.stringify(jsonData));
expect(result).toHaveLength(1);
expect(result[0].name).toBe('Test Place');
\`\`\`

### Тестирование компонента
\`\`\`typescript
import { render, screen } from '@testing-library/react-native';
import { PointCard } from '@/components/UserPoints/PointCard';

const mockPoint = {
  id: '1',
  name: 'Test',
  latitude: 50.4,
  longitude: 30.5,
  // ...
};

render(<PointCard point={mockPoint} />);
expect(screen.getByText('Test')).toBeTruthy();
\`\`\`

## 📝 Добавление новых тестов

1. Создайте файл с суффиксом `.test.ts` или `.test.tsx`
2. Поместите в соответствующую директорию `__tests__/`
3. Используйте Jest и React Testing Library
4. Следуйте существующим паттернам

## 🐛 Отладка тестов

### Просмотр вывода
\`\`\`bash
npm test -- --verbose
\`\`\`

### Отладка конкретного теста
\`\`\`bash
npm test -- --testNamePattern="should parse valid JSON"
\`\`\`

### Обновление снапшотов
\`\`\`bash
npm test -- -u
\`\`\`

## 📚 Дополнительная информация

- [Jest Documentation](https://jestjs.io/)
- [React Testing Library](https://testing-library.com/react)
- [Testing Library React Native](https://callstack.github.io/react-native-testing-library/)

## ✨ Статус

- ✅ Парсеры: 100% покрытие основных сценариев
- ✅ Компоненты: Основные UI тесты
- ✅ Типы: Полная валидация
- ⚠️ API: Требует моки или реальный бэкенд
- ⚠️ E2E: Требует Playwright и запущенное приложение
