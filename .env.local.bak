# Local QA override (gitignored) — point dev/preview at the working e2e backend (E2E_API_URL).
# Created for TD-010 browser verification because 127.0.0.1:8112 returns 502.
# Remove this file to revert to the default .env backend.
EXPO_PUBLIC_API_URL=http://192.168.50.36
EXPO_PUBLIC_IS_LOCAL_API=true
