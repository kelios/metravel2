# Анализ системы создания и редактирования путешествий

## Дата анализа: 3 января 2026

---

## 🔍 Обзор архитектуры

### Основные компоненты:
1. **UpsertTravel.tsx** - главный компонент мастера создания/редактирования
2. **useTravelFormData.ts** - хук для управления данными формы и автосохранения
3. **useTravelWizard.ts** - хук для управления шагами мастера
4. **useImprovedAutoSave.ts** - улучшенный автосейв с дебаунсом
5. **TravelWizardStep*.tsx** - компоненты для каждого шага
6. **travelsApi.ts** - API для работы с путешествиями
7. **misc.ts** - API для сохранения данных формы

---

## ⚠️ НАЙДЕННЫЕ ПРОБЛЕМЫ И УЯЗВИМОСТИ

### 🔴 КРИТИЧНЫЕ

#### 1. Race Condition при множественных автосохранениях
**Файл:** `hooks/useTravelFormData.ts`, строки 220-240
**Проблема:** 
- При быстрых изменениях формы могут запуститься несколько параллельных автосохранений
- Отсутствует механизм отмены предыдущего запроса при старте нового
- Может привести к потере данных или сохранению устаревшей версии

**Решение:**
```typescript
// Добавить AbortController для отмены предыдущих запросов
const saveAbortControllerRef = useRef<AbortController | null>(null);

const cleanAndSave = useCallback(async (data: TravelFormData) => {
  // Отменяем предыдущий запрос
  if (saveAbortControllerRef.current) {
    saveAbortControllerRef.current.abort();
  }
  
  // Создаём новый контроллер
  saveAbortControllerRef.current = new AbortController();
  
  // ... существующий код сохранения
}, []);
```

#### 2. Утечка памяти при размонтировании компонента
**Файл:** `hooks/useImprovedAutoSave.ts`, строки 200-260
**Проблема:**
- setState может вызываться после размонтирования компонента
- Таймеры не всегда очищаются корректно

**Решение:** Добавлен mountedRef, но нужна дополнительная проверка перед каждым setState

#### 3. Небезопасная обработка маркеров с изображениями
**Файл:** `hooks/useTravelFormData.ts`, строки 140-170
**Проблема:**
- Отправка пустой строки `image: ""` вместо `null` вызывает ошибку бэкенда
- Некорректная обработка может привести к отказу в сохранении

**Уже исправлено в коде:**
```typescript
const imageValue = typeof image === 'string' ? image.trim() : '';
const marker = {
  ...rest,
  image: imageValue && imageValue.length > 0 ? imageValue : null,
};
```

### 🟡 ВАЖНЫЕ

#### 4. Отсутствие валидации на клиенте перед отправкой
**Файл:** `src/api/misc.ts`, строка 63
**Проблема:**
- Недостаточная валидация полей формы перед отправкой на сервер
- Санитизация применяется только к feedback, но не к travel данным

**Решение:** Добавить валидацию в saveFormData

#### 5. Конфликт при одновременном редактировании
**Файл:** `hooks/useTravelFormData.ts`
**Проблема:**
- Отсутствует механизм обнаружения конфликтов версий
- Если два пользователя (например, владелец и админ) редактируют одно путешествие, изменения одного перезапишут изменения другого

**Решение:** Добавить версионирование или оптимистичную блокировку

#### 6. Отсутствие rate limiting на автосохранение
**Проблема:**
- Пользователь может генерировать много запросов при быстром вводе
- Нагрузка на сервер может быть избыточной

**Решение:** Уже реализован debounce 5000ms, но можно добавить дополнительное ограничение

#### 7. Небезопасное хранение черновиков
**Файл:** `hooks/useTravelFormData.ts`, строки 70-130
**Проблема:**
- Draft placeholder содержит ID в открытом виде: `__draft_placeholder__${id}`
- Потенциальная утечка информации об ID

**Решение:** Использовать более безопасный placeholder без ID

### 🟢 УЛУЧШЕНИЯ

#### 8. Отсутствие оптимистичных обновлений UI
**Проблема:** 
- UI ждёт ответа сервера для обновления статуса автосохранения
- Можно улучшить UX через оптимистичное обновление

#### 9. Недостаточная обработка оффлайн режима
**Файл:** `hooks/useImprovedAutoSave.ts`
**Проблема:**
- Автосохранение не работает оффлайн
- Нет механизма восстановления данных при восстановлении связи

**Решение:** Добавить локальное хранилище для черновиков

#### 10. Отсутствие дедупликации стран из маркеров
**Файл:** `utils/travelFormUtils.ts`, строка 235
**Проблема:**
- Функция syncCountriesFromMarkers может создавать дубликаты
- Уже используется Set, но проверка типов может быть строже

---

## ✅ ЧТО УЖЕ РЕАЛИЗОВАНО ХОРОШО

1. ✅ **Автосохранение с debounce** - предотвращает спам запросов
2. ✅ **Безопасное хранилище токенов** - используется SecureStore
3. ✅ **Race condition защита в API клиенте** - refresh token lock
4. ✅ **Валидация изображений** - размер и формат проверяются
5. ✅ **Санитизация input** - XSS защита для feedback
6. ✅ **Проверка доступа** - checkTravelEditAccess перед редактированием
7. ✅ **Нормализация данных** - cleanEmptyFields, normalizeTravelId
8. ✅ **Обработка сетевых ошибок** - retry логика в API

---

## 🔧 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1 (Критичные)
1. ✅ Исправить race condition при автосохранении - добавить AbortController
2. ✅ Усилить защиту от утечки памяти - проверки mountedRef перед setState
3. ✅ Улучшить валидацию перед отправкой на сервер

### Приоритет 2 (Важные)
4. ✅ Добавить версионирование для предотвращения конфликтов
5. ✅ Улучшить обработку draft placeholder
6. ✅ Добавить rate limiting на уровне API

### Приоритет 3 (Улучшения)
7. ⚠️ Реализовать оффлайн поддержку (требует больших изменений)
8. ✅ Добавить логирование ошибок для мониторинга
9. ✅ Улучшить типизацию в критичных местах

---

## 📊 МЕТРИКИ БЕЗОПАСНОСТИ

- **XSS защита:** ✅ Реализована в security.ts
- **SQL Injection:** ✅ N/A (используется API, не прямые SQL запросы)
- **CSRF защита:** ✅ Токены через безопасное хранилище
- **Rate Limiting:** ⚠️ Только debounce на клиенте
- **Версионирование:** ❌ Отсутствует
- **Аудит действий:** ⚠️ Частичный (analytics события)

---

## 🎯 РЕКОМЕНДАЦИИ

### Немедленно:
1. Исправить race condition при автосохранении
2. Добавить проверки mountedRef во всех setState
3. Усилить валидацию перед saveFormData

### Скоро:
1. Реализовать версионирование данных
2. Добавить мониторинг ошибок (Sentry/LogRocket)
3. Улучшить оффлайн поддержку

### В будущем:
1. Реализовать collaborative editing с WebSocket
2. Добавить undo/redo для действий пользователя
3. Оптимизировать размер bundle через code splitting

---

## 📝 ЗАМЕТКИ ДЛЯ РАЗРАБОТЧИКОВ

### Ключевые моменты при работе с формой:
1. Всегда используйте `cleanAndSave` вместо прямого вызова API
2. Не забывайте вызывать `autosave.cancelPending()` перед ручным сохранением
3. Используйте `normalizeDraftPlaceholders` перед отображением данных пользователю
4. Проверяйте `hasAccess` перед разрешением редактирования

### Тестирование:
- Все критичные сценарии покрыты тестами в `__tests__/components/travel/`
- Обязательно тестируйте race conditions и edge cases
- Используйте моки для API запросов

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

**Core:**
- `components/travel/UpsertTravel.tsx`
- `hooks/useTravelFormData.ts`
- `hooks/useTravelWizard.ts`
- `hooks/useImprovedAutoSave.ts`

**API:**
- `src/api/travelsApi.ts`
- `src/api/misc.ts`
- `src/api/client.ts`

**Utils:**
- `utils/travelFormUtils.ts`
- `utils/travelWizardValidation.ts`
- `src/utils/security.ts`
- `src/utils/validation.ts`

**Tests:**
- `__tests__/components/travel/UpsertTravel.*.test.tsx`
- `__tests__/utils/travelFormUtils.test.ts`


