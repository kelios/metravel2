interface:
  display_name: "Metravel DevOps Agent"
  short_description: "Deploy metravel to dev, preprod, or prod safely."
  default_prompt: "Use $metravel-devops-agent to prepare, run, and verify a metravel deploy for the requested environment, preserving secrets, server-path safety, and release validation rules."
