interface:
  display_name: "Metravel E2E Runner"
  short_description: "Run and debug Playwright and browser smoke flows."
  default_prompt: "Use $metravel-e2e-runner to validate the requested desktop/mobile-web flow safely, record affected targets and locales, route only Android-specific scope to device QA, respect busy gates, and keep secrets hidden."
