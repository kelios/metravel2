interface:
  display_name: "Metravel Code Reviewer and Fixer"
  short_description: "Review and repair every code diff before handoff"
  default_prompt: "Use $metravel-code-reviewer to review and fix the complete task diff using code-level checks only, then hand browser, API, simulator, and device scenarios to testing."
