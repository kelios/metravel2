/node_modules/
/.secrets/
*.gcp.json
gcp-service-account*.json
/dist/
/dist-stub/
/.prod-build-tmp/
/dist-web-analyze/
/dist-web-analyze-sm/
/dist-dev-diag/
/dist-prod-diag/
/dist-*-diag/
/web-build/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
settings.json
.prettierrc
.DS_Store
**/.DS_Store
.vscode/
.env*
!.env.example
!.env.*.example
.secrets/
.expo
.expo-shared/
.idea
env.sh
/assets/quests/

# Test / coverage artifacts
coverage/
coverage-*/
.nyc_output/
*.lcov
junit.xml
/coverage/
jest-results.json
test-output.txt
*.log
.eslintcache
.tsbuildinfo
eslint-report.json

# Playwright artifacts
/.playwright-mcp/
/test-results/
/playwright-report/
.playwright-cli/
.playwright-report-extracted/
/e2e/.auth/
/e2e/_flicker-shots/
.pwtrace_tmp*/
/playwright-cli-*.tgz

.pwtrace_tmp/
**/*-snapshots/

# Android sensitive files
google-services.json
google-play-service-account.json
android-keystore.jks
*.keystore
*.jks
upload-keystore.jks

# Local tooling artifacts
/artifacts/
/e2e-results.json
/jest-smoke-results.json
/*-review-*.png
lighthouse-report*.json
lighthouse*.json
# Committed Lighthouse budget config (not a transient report) — keep tracked (#816)
!config/lighthouse-budget-mobile.json
lighthouse-reports/
lighthouse-reports/local-*.json
lighthouse-reports/local-*.html
lighthouse-reports/local-*.png
tmp-*.png
tmp-*.jpg
tmp-*.jpeg
tmp-*.webp
tmp-*.gif
yarn-error.log
output/
tmp/
*.tmp

# Android build files
android/
.gradle/
build/
*.apk
*.aab

# Next.js / bundler caches (web)
.next/
.swc/
.turbo/
.parcel-cache/
.cache/
/playwright-screenshots/
nginx/ssl/
/nginx/ssl/fullchain.pem

# Claude Code AI session artifacts
.claude/settings.local.json
# Общий project-конфиг с хуками (код-ревью гейт) должен быть в репозитории,
# несмотря на глобальное правило `settings.json` выше.
!.claude/settings.json
.claude/worktrees/
.claude/sessions/
.claude/tmp/
.claude/scheduled_tasks.lock
.codex-temp/
.codex-debug/
.tmp/

# iOS signing & certificates (never commit)
*.cer
*.certSigningRequest
*.p12
*.p8
*.mobileprovision
AuthKey_*.p8

# Additional env files
.env.local
.env.production
.env.*.local
*.bak

# e2e generated artifacts
/e2e/__screenshots__/

# EAS auto-versioning
eas-build-auto-version.txt
eas-update-auto-version.txt

# macOS system files
.AppleDouble
.LSOverride
Icon$
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# Editor swap/backup files
*.swp
*.swo
*~
.#*

# Sentry (if used)
sentry.properties
.sentryclirc

# scratch/debug content (not part of app)
travel-drafts/

# seo-edit.js article backups (local rollback snapshots)
scripts/.seo-backups/

# Local scratch caches/reports from one-off SEO/quest/workflow scripts
scripts/.seo-*/
scripts/.seo-*.json
scripts/.fix-*/
scripts/.workflows/
scripts/.tmp-*

# migrate-description-images.js: бэкапы payload статей, инвентарь и журнал прогона
# (#1245). Это снимки ЖИВОГО контента прода, в репозиторий им нельзя.
scripts/.migrate-description-images/

# update-quest-content.js: снимки контента квеста для ревью и архив применённых
# (#1448). Та же причина, что у #1245 — это снимок ЖИВОГО прода, он протухает
# сразу после следующей правки, а применение протухшего файла откатывает прод.
scripts/.quest-review/

# Ad-hoc tmp dumps at repo root
/tmp_*.json

# Локальные бэкапы медиа-хранилища (scripts/backup_media_storage.py — ~11 ГиБ)
# и оригиналы, снятые перед перезаливкой (scripts/normalize_quest_covers_prod.py)
.backup/
/tmp_*.txt

# Index-status кэш (не коммитим)
scripts/.index-cache/
/dump/
/.quest-audit/

# Build-check artifacts (export-проверки web/android) и отладочные дампы
/.chk-web/
/.chk-android/
/.chk-e2e/
/.tmp-article/
/.tmp-pdf-preview/

# Каталог-артефакт: создаётся, когда путь вывода собрался из undefined-переменной
# (скриншоты/дампы улетают в ./undefined вместо реального каталога).
/undefined/

# Дампы прод-базы (scripts/backup-prod-db.sh). Внутри персональные данные
# пользователей, а репозиторий публичный — под git не попадают никогда.
/backup/

# Отладка native (adb-скриншоты, логи dev-серверов) — не коммитить
.debug-shots/
metro-*.log
.claude/play-testing/evidence/
.quest-snapshots/

# QA-доказательства по тикетам, которые агенты складывают в КОРЕНЬ репозитория:
# скриншот прогона (`task-1623-desktop.png`, `1536-minsk-step2-mobile.png`) и
# текстовый снимок страницы рядом с ним (`*-snapshot.md`). Это evidence под
# конкретный прогон: протухает сразу после следующей правки и в репозиторий ему
# нельзя. Каталог `tasks/` под правило не попадает — нужен дефис после `task`.
/task-*.png
/task-*.md
/[0-9][0-9][0-9][0-9]-*.png
/[0-9][0-9][0-9][0-9]-*.md

# рабочие брифы задач в корне (см. #1772: файл уехал в чужой коммит свипом)
/brief.md
