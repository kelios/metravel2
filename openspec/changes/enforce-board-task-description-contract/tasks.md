## 1. Замер и калибровка

- [x] 1.1 Выгрузить все открытые карточки борда и проверить их против контракта из `docs/TASK_BOARD_MCP.md` — получить число, а не впечатление
- [x] 1.2 Откалибровать проверку на живых данных: устранить ложные срабатывания (разбор значения поля, `none` как законное значение, эвристика блокера)
- [x] 1.3 Отделить дефекты правил от дефектов исполнения: зафиксировать противоречие `needs_human` против обязательного `Task Contract` и неопределённость формата поля

## 2. Валидатор контракта

- [x] 2.1 Создать `scripts/lib/boardTaskContract.mjs` как единственный источник правды: семь разделов и их порядок, строки лида, поля `Problem History` и `Task Contract`
- [x] 2.2 Реализовать разбор значения поля в каноническом формате `Имя:` и отдельное распознавание оформленного варианта (`**Scope.**`) как отсутствующего поля
- [x] 2.3 Реализовать проверки согласованности карточки: спринт, `area`, статус `blocked_by` против названного блокера и против текста раздела «Что блокирует»
- [x] 2.4 Реализовать правила значений: `none` как законное значение, заглушки, `Regression control` для `kind=bug`, наборы поверхностей и локалей
- [x] 2.5 Реализовать ветку карточки `needs_human=true`: разделы человеческого шаблона обязательны, `Task Contract` запрещён
- [x] 2.6 Разделить находки на `error` и `warn`, а ошибки — на `text` и `field` по способу починки

## 3. Гейт и аудит

- [x] 3.1 Создать `.claude/hooks/task-quality-gate.mjs` по механике `review-gate.mjs`: PreToolUse на `metravel_task_create` и `metravel_task_update`
- [x] 3.2 Ограничить проверку тремя событиями: создание, запись `description`, перевод в `todo`; прочие обновления пропускать
- [x] 3.3 Блокировать запись описания только ошибками рода `text`, а ошибки рода `field` сообщать
- [x] 3.4 Сделать отказ самодостаточным: перечислить нарушения и назвать шаблон, порядок разделов, формат поля и команду проверки черновика
- [x] 3.5 Реализовать явное сообщение при невозможности проверки и видимый аварийный обход `BOARD_TASK_GATE_BYPASS=1`
- [x] 3.6 Добавить режим `check --file` и `check --task` для проверки до отправки
- [x] 3.7 Создать `scripts/audit-board-tasks.mjs` и команду `npm run board:audit` с фильтрами `--task`, `--status`, `--json`, `--strict`
- [x] 3.8 Зарегистрировать хук в `.claude/settings.json` рядом с существующим гейтом код-ревью

## 4. Правила

- [x] 4.1 Развести `needs_human` и `Task Contract` в `docs/TASK_BOARD_MCP.md`: взаимоисключающи, реализация владельцем бэкенда флага не несёт
- [x] 4.2 Объявить имя поля контракта идентификатором и зафиксировать законность `none` там же
- [x] 4.3 Описать гейт качества карточки отдельным разделом документа: модуль, хук, команды, обход
- [x] 4.4 Перенести те же правила в `.claude/agents/ticket-board.md`, `.codex/skills/metravel-ticket-board/SKILL.md`, `.codex/skills/metravel-task-contract/SKILL.md`
- [x] 4.5 Добавить шаг проверки черновика в скилл `metravel-issue` и его шаблоны, включая зеркала в `.agents/skills/`
- [x] 4.6 Добавить строку про гейт в матрицу triage `docs/CODEX.md`

## 5. Регрессионный контроль

- [x] 5.1 Написать `__tests__/scripts/board-task-quality-gate.test.ts`: поведение валидатора на эталонной карточке и на каждом классе нарушения
- [x] 5.2 Закрепить тестом разделение `text`/`field` — случай, который на практике заставил исполнителя обходить инструмент
- [x] 5.3 Закрепить тестом регистрацию хука и наличие команды аудита
- [x] 5.4 Закрепить тестом присутствие формулировки границы `needs_human` во всех файлах, откуда заводят карточку
- [x] 5.5 Прогнать jest по новому тесту — 17 из 17 зелёные
- [x] 5.6 Починить загрузку ESM-модуля в jest: пресет трансформировал только `.[jt]sx?`, из-за чего `.mjs` падал с `SyntaxError: Unexpected token 'export'`; в `jest.config.js` добавлен transform для `.mjs` с теми же опциями babel-jest
- [x] 5.7 Подтвердить, что правка transform не задела существующие тесты (`exifGps`, governance-parity, problem-memory — 13 из 13 зелёные)

## 6. Нормализация открытых карточек

- [x] 6.1 Снять ошибочный `needs_human` с карточек, которые являются инженерной работой владельца
- [x] 6.2 Проставить спринт карточкам без спринта и исполнителя карточкам в работе
- [x] 6.3 Привести описания открытых карточек к контракту, не теряя фактов и не выдумывая причин
- [x] 6.4 Исправить статусы карточек, стоящих в `blocked_by` без блокера, по фактическому состоянию работы (7 карточек → `todo`)
- [x] 6.5 Прогнать `npm run board:audit` и подтвердить результат нормализации числом (78 открытых карточек: 0 нарушений, 0 замечаний против 46 нарушений из 80 на старте)

## 7. Приведение процесса в соответствие

- [x] 7.1 Установить OpenSpec-скиллы для Claude Code (`openspec init --tools claude`) — раньше они стояли только для Codex, из-за чего Claude-сессии обходили обязательный цикл
- [x] 7.2 Обновить `docs/spec-driven-development.md`: skills существуют для обоих агентов, правило одинаково обязательно
- [x] 7.3 Оформить эту работу change-артефактами OpenSpec
- [x] 7.4 Провалидировать change (`openspec validate --strict` → valid)
