## 1. Отбор безопасного текста

- [x] 1.1 В `scripts/generate-seo-pages.js` добавить чистую функцию разбора описания точки на
      предложения (`\n\n` схлопывается, границы по `.!?…`)
- [x] 1.2 Добавить чистую функцию извлечения конкретных ожидаемых ответов из `answer_pattern`:
      список `exact_any`, значение `exact`, все числа диапазона `range`; `any`/`any_text`/`any_number`
      дают пустой список
- [x] 1.3 Добавить чистую функцию отбора безопасных предложений: выкинуть обращённые к игроку
      (императив, второе лицо) и содержащие значение ответа; применить лимит 2 предложения / 60 слов
      с обрывом по границе предложения
- [x] 1.4 Добавить сборку модели маршрута из бандла: по точке — название, место, отобранное описание,
      часы работы и стоимость из `poi_info`; точка без уцелевших предложений остаётся с названием и
      местом

## 2. Блок маршрута в статическом HTML

- [x] 2.1 Отрисовать блок маршрута в `injectQuestIntroSection` между вступлением и перелинковкой,
      в стилистике существующей секции (`h2`, список точек, `h3` на точку)
- [x] 2.2 Практическую справку показывать только при наличии значений, без пустых подписей
- [x] 2.3 Проверить, что при отсутствии бандла и при пустом списке точек блок не рендерится, а
      остальная секция собирается как прежде

## 3. Гейт на раскрытие игровой части

- [x] 3.1 Добавить функцию сверки итогового HTML страницы с сырыми полями бандла: текст `task`,
      текст `hint`, значения ответов
- [x] 3.2 Вызвать сверку в цикле генерации квестов до записи файла; при находке — прервать сборку с
      указанием квеста и точки
- [x] 3.3 В конце блока квестов вывести число проверенных квестов

## 4. Тесты

- [x] 4.1 В `__tests__/scripts/generate-seo-pages.test.ts` покрыть отбор предложений: наводка
      выкинута, описательное предложение сохранено, дословность сохранённого
- [x] 4.2 Покрыть извлечение ответов по всем типам `answer_pattern` и выброс предложения с ответом
      (в том числе числом из диапазона)
- [x] 4.3 Покрыть лимиты: обрыв по границе предложения, лимит по словам
- [x] 4.4 Покрыть рендер блока: точки на месте, `task`/`hint`/ответа нет, точка без `poi_info` и
      точка без уцелевших предложений
- [x] 4.5 Покрыть гейт: HTML с текстом задания даёт ошибку, чистый HTML проходит

## 5. Проверка на живом каталоге

- [x] 5.1 Прогнать генерацию квестовых страниц на живом каталоге всех 165 квестов и убедиться, что
      гейт зелёный
- [x] 5.2 Замерить объём уникального текста до и после на шести URL из #1763 и по разделу целиком,
      цифры зафиксировать в тикете
- [x] 5.3 Выборочно прочитать итоговый текст нескольких квестов и убедиться, что пересказ читается
      как описание мест, а не как инструкция игроку

## 6. Регрессии и review

- [ ] 6.1 `npm run test:i18n` и релевантные static/unit checks на изменённом scope
- [ ] 6.2 Code-only review-and-fix полного diff независимым ревьюером
- [ ] 6.3 `openspec validate quest-ssg-route-digest --strict`

## 7. Приёмка в браузере (после перехода тикета в `testing`)

- [ ] 7.1 Статический срез страницы квеста без исполнения JS: точки видны, заданий и ответов нет
- [ ] 7.2 Desktop web и mobile web: блок до гидратации не ломает вёрстку, после гидратации скрыт,
      экран квеста работает как прежде
