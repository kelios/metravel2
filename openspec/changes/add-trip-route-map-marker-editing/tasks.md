## 1. Общий контракт карты и владелец черновика

- [x] 1.1 Добавить в `tripPlanRouteMap.types.ts` тип `RoutePointMove` и описать, что оба платформенных файла обязаны нести `onMovePoint`/`onDeletePoint`.
- [x] 1.2 Добавить в `RouteBuilder` обработчик перемещения точки: округление до 6 знаков, точечная замена координат, синхронизация полей открытого редактора этой точки, защита от нефинитных чисел.
- [x] 1.3 Прокинуть `onMovePoint`/`onDeletePoint` в карту только для владельца; в readonly-ветке оставить прежний набор пропов.

## 2. Web-карта

- [x] 2.1 Сделать маркер перетаскиваемым (`draggable={!readonly}` + `dragend`), убрать координаты из ключа маркера.
- [x] 2.2 Добавить в popup кнопку удаления рядом с существующей кнопкой редактирования.
- [x] 2.3 Поставить защёлку авто-подгонки кадра после первого перетаскивания.
- [x] 2.4 Снимать защёлку, когда маршрут заменён целиком (шаблон, импорт трека):
      иначе новый маршрут остаётся за пределами кадра до размонтирования карты.
      Ключ — `id` точки: координаты дропа округляются уже в `RouteBuilder`.

## 3. Native-карта

- [x] 3.1 Заменить `L.circleMarker` точек маршрута на `L.marker` с divIcon той же геометрии; включить `draggable`/`interactive` по флагу payload.
- [x] 3.2 Добавить сообщения `ROUTE_POINT_MOVED` и `ROUTE_POINT_TAP` в инлайновый скрипт и в типизированный парсер моста; гасить всплытие тапа по маркеру.
- [x] 3.3 Прокинуть `onRoutePointMove`/`onRoutePointPress`/`routePointsInteractive` через `Map.ios.tsx`.
- [x] 3.4 Показать карточку действий точки поверх карты в `TripPlanRouteMap.tsx` и закрывать её после действия и при исчезновении точки.
- [x] 3.5 Поставить защёлку авто-подгонки кадра в route-ветке WebView.
- [x] 3.6 Снимать её по тому же правилу, что и на web. Ключ координатный
      (id в payload нет), огрублённый `toFixed(6)` с обеих сторон и нумерованный
      по полному набору точек.

## 4. Локализация

- [x] 4.1 Добавить ключи `plan.map.editPoint`, `plan.map.deletePoint`, `plan.map.pointActions`, `plan.map.markerHint` в статические ресурсы RU/BE/UK/PL/EN.
- [x] 4.2 Прогнать `npm run test:i18n`.

## 5. Regression control

- [x] 5.1 Тест web-карты: `dragend` отдаёт индекс и координаты дропа; кнопка удаления отдаёт индекс; у гостя маркер не перетаскивается и кнопок нет.
- [x] 5.2 Тест `RouteBuilder`: перемещение с карты меняет координаты ровно одной точки, сохраняет порядок, синхронизирует открытый редактор и поднимает флаг несохранённых изменений.
- [x] 5.3 Тест парсера моста: `ROUTE_POINT_MOVED`/`ROUTE_POINT_TAP` разбираются и отбраковываются на нефинитных значениях.
- [x] 5.4 Тесты защёлки на обеих картах: оптовая замена маршрута кадр возвращает,
      перетаскивание единственной точки — нет.

## 6. Проверка

- [x] 6.1 Static/unit: typecheck, lint по изменённым файлам, focused jest.
- [ ] 6.2 Browser QA на локальном стеке (desktop web + mobile web) — на стадии `testing`.
- [ ] 6.3 iOS simulator и Android device QA — на стадии `testing`.
