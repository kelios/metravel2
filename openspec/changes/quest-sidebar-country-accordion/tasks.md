## 1. Модель выбора страны

- [x] 1.1 В `utils/questCatalogSelection.ts` добавить префикс `__country__:` и два помощника: id из кода страны и код страны из id (`null` для любого другого значения); unit-тест на оба направления, регистр кода и чужие id (`__all__`, id города)
- [x] 1.2 Создать `screens/tabs/useQuestCountrySelection.ts`: из слота выбора, `citiesByCountry` и `cityQuests` вывести `activeCountryCode` (страна выбранного города или странового id), квесты выбранной страны (объединение квестов её городов с дедупликацией по ключу квеста), имя страны и признак валидности странового id; unit-тест хука на эти четыре вывода, включая группу `OTHER`

## 2. Экран каталога

- [x] 2.1 Подключить хук в `screens/tabs/QuestsScreen.tsx`: сетка при страновом id берёт квесты страны, эффект валидности пропускает валидный страновой id и сбрасывает на «Все квесты» несуществующий; `QuestsScreen.tsx` не растёт сверх текущих 845 строк
- [x] 2.2 `selectedCityName` для странового id отдаёт имя страны; добавить ключ описания каталога для страны в пять локалей и выбрать его в `descText`
- [x] 2.3 Убрать `countryLandingHref` и вызов `buildQuestCountryLandingGroups` из построения `citiesByCountry`
- [x] 2.4 Раскрытие: неизвестная группа в `collapsedCountryCodes` свёрнута; эффект по смене `activeCountryCode` раскрывает страну активного выбора один раз; кнопка «Развернуть/Свернуть все» работает как раньше

## 3. Сайдбар

- [x] 3.1 В `screens/tabs/QuestsSidebar.tsx` заголовок страны — один `Pressable`-тумблер с `accessibilityRole="button"`, `aria-expanded` и `accessibilityState.expanded`; удалить ветку `Link`, импорт `Link`/`Href`, поле `countryLandingHref` в типе группы и `onCloseDrawer` у страны
- [x] 3.2 Первым пунктом раскрытой страны с двумя городами и больше выводить «Все квесты страны» в форме строки города (иконка `globe`, счётчик = сумма квестов городов), выбор через `onSelectCity(<страновой id>)`, отметка выбора; подпись скринридера — ключ `countryLandingA11y`, видимая подпись — новый ключ в пяти локалях; у группы `OTHER` пункта нет
- [x] 3.3 Маркер активного выбора на свёрнутой стране: имя фирменным цветом и точка 8 px из `useThemedColors`, подпись тумблера для скринридера дополняется «выбрано» (новый ключ в пяти локалях)
- [x] 3.4 Ритм в `screens/tabs/QuestsScreen.styles.ts`: снять `paddingVertical` секции страны и `marginTop` заголовка, привести шаг свёрнутых стран к шагу строк фильтров при зоне нажатия ≥ 44 px, только существующими токенами
- [x] 3.5 Переписать `__tests__/screens/QuestsSidebar.countryLink.test.tsx` и `__tests__/screens/QuestsScreen.countryLink.test.tsx` под новый контракт: нажатие по имени страны раскрывает её и не ведёт на лендинг, ссылки `quests-country-link-*` нет, `aria-expanded` меняется, пункт «Все квесты страны» есть при ≥2 городах и нет при одном, маркер на свёрнутой стране с выбором, тумблер не закрывает drawer, выбор страны закрывает

## 4. Документация

- [x] 4.1 `docs/features/quests.md`: во флоу каталога описать фильтр «вся страна», раскрытие стран и маркер; в точках входа отметить, что сайдбар на лендинг страны не ведёт, а перелинковка на него живёт в статическом HTML

## 5. Проверки до review

- [x] 5.1 `npx tsc --noEmit`, `npx eslint` по изменённым файлам, `npx jest` по новым и затронутым сьютам каталога квестов, `npm run test:i18n`
- [x] 5.2 Стражи затронутой зоны: `guard:touch-targets`, `guard:themed-colors`, `guard:text-row-sizing`, `guard:file-complexity:changed`, `guard:type-debt`
- [ ] 5.3 Обязательный review-and-fix по полному diff (`review-auditor`), затем `code-review-gate` перед `testing`
- [x] 5.4 `openspec validate quest-sidebar-country-accordion --strict`

## 6. Testing и выкат (пайплайн задачи — постоянное правило владельца от 20.09.2026, `CLAUDE.md`)

- [ ] 6.1 Карточка на MCP-борде ведётся от `in_progress` до `done`; после pass гейта — коммит явными путями задачи и push в `main`
- [ ] 6.2 Выкат web на прод через `frontend-deployer` без параллельного деплоя
- [ ] 6.3 Приёмка на `https://metravel.by/quests` по матрице `design.md`: desktop 1280 px и mobile 375 px, RU и EN — раскрытие по имени/счётчику/шеврону без смены адреса, `aria-expanded`, пункт страны и сетка страны, заголовок и сброс, маркер, шаг строк, drawer, восстановление выбора после перезагрузки, регрессия города/«Рядом»/личных срезов/поиска/карты; скриншоты и консоль без ошибок
