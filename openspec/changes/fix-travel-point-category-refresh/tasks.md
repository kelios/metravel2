## 1. Воспроизведение до исправления

- [ ] 1.1 После отдельного согласования apply сверить текущую #1518, `main`, task-owned diff и свободный operation gate; сохранить чужие изменения.
- [ ] 1.2 В существующем hook-тесте воспроизвести новый ответ после ухода/возврата на неизменном шаге 2; показать отсутствие новой категории до исправления и здоровый контроль обновления при переходе шага.
- [ ] 1.3 На локальном стеке воспроизвести последовательность «открыть редактор → создать собственную активную категорию через локальную админку → вернуться без смены шага», записать новую id/name, GET и отсутствие/наличие пункта. Если сам свежий API не содержит запись, остановить frontend-исправление до уточнения владельца причины.

## 2. Обновление и ограничение запросов

- [ ] 2.1 Добавить в `hooks/useTravelFilters.ts` web-only возврат с объединением focus/visibility после ухода, актуальным шагом и cleanup; выполнить требования `Fresh categories after returning to the editor` и `Requests are bounded and correspond to the return`.
- [ ] 2.2 Сохранить один запрос на возврат независимо от 60-секундного step-троттлинга; не потерять отложенный возврат за старой загрузкой, не запускать его после ухода/размонтирования. Проверить helper-level старый forced-запрос; при необходимости узко дополнить существующий `api/miscOptimized.ts` без второго кэша.
- [ ] 2.3 Сохранить ревизионную защиту, silent-обновление только категорий, выбранные id/поиск/поля и поведение ошибок; не менять правила создания короткого префикса, текущие фото и native lifecycle.

## 3. Регрессионные проверки

- [ ] 3.1 Расширить существующие hook/API suites: быстрый возврат <60 с, оба порядка focus/visibility и завершение ответа между ними, медленный старый forced-запрос, дубли событий, другой шаг/unmount, отсутствие DOM, error/offline recovery и empty без цикла. Прогнать целевые Jest из design через общий quality wrapper.
- [ ] 3.2 Расширить `e2e/travel-wizard.spec.ts` именованным сценарием возврата без смены шага для 1440×900 и 390×844: уникальный новый id в результатах ≤5 с после ответа, один новый GET, сохранение выбранного id/поиска/адреса/фото, полный и неполный запрос имени. Управляемые ответы использовать для гонки; не считать их живой приёмкой.
- [ ] 3.3 Выполнить отдельную живую локальную проверку с собственной категорией и черновиком без подмены API на обоих viewport; снять network/console и скриншоты, затем удалить только созданные тестовые записи по сохранённым id.
- [ ] 3.4 Повторить соседние query/cache/prefetch-тесты, 2 → 3 → 2 внутри и после 60 секунд, существующий exact-match тест селектора; выполнить `npm run check:fast`, проверить отсутствие новых строк RU/BE/UK/PL/EN и native runtime-изменений.

## 4. Ревью и завершение реализации

- [ ] 4.1 Провести независимый `$metravel-code-reviewer` review-and-fix по полному task diff, исправить подтверждённые замечания и повторить затронутые проверки без рекурсивного ревьюера.
- [ ] 4.2 Актуализировать причину и постоянный контроль в `docs/PROBLEM_MEMORY.md` с реальным результатом локальной проверки; не выдавать предположение о production-кэше за измерение.
- [ ] 4.3 Выполнить `openspec validate fix-travel-point-category-refresh --type change --strict` и `git diff --check`; перед возможным archive выполнить `openspec validate --all`. Передать точные результаты и оставшийся production-recheck без заявления о выкладке.

Commit, push, deploy, production mutations и archive не входят в текущее разрешение. Apply начинается только отдельным запросом после представления этих артефактов. Пока runtime-проверки выше не выполнены, ни спецификация, ни задача #1518 не считаются реализованными.
