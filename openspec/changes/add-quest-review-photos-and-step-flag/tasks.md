## 1. Клиентский контракт загрузки

- [ ] 1.1 Завести адаптер загрузки фото отзыва поверх существующего `uploadImage` (`api/misc.ts:438`): собирает multipart с `id` (PK отзыва), `collection='questReviewPhoto'`, `file` и необязательным `step_id`; на web не кладёт в FormData RN-объект `{uri,name,type}` (ловушка из `hooks/useAvatarUpload.ts:188`).
- [ ] 1.2 Написать тест реальной сериализации запроса (не мок примитива): проверяет присутствие `collection`, корректный `id`, наличие `step_id` только при выбранной точке и его отсутствие при непривязанном фото.
- [ ] 1.3 Зафиксировать предел в три файла одной константой, разделяемой пикером и адаптером, чтобы клиентский лимит не мог разойтись с серверным.

## 2. Пикер в шаге «Как прошло?»

- [ ] 2.1 Добавить выбор до трёх изображений в `components/quests/QuestReviewSection.tsx` с превью по уменьшенному представлению; подключение — через `components/quests/questWizardSections.tsx`.
- [ ] 2.2 Реализовать порядок «подтверждённый отзыв → последовательная загрузка»: `id` берётся из ответа `submitQuestReview` (`api/questReview.ts:68`), до подтверждения не грузится ничего.
- [ ] 2.3 Реализовать частичный успех: сохранённый отзыв остаётся сохранённым, неуспешные файлы называются игроку поимённо, успешные не теряются и не грузятся повторно.
- [ ] 2.4 Показать состояние «фото отправлено на проверку», чтобы задержка модерации не читалась как потеря файла.
- [ ] 2.5 Компонентные тесты: без фото поведение прежнее; четвёртый файл не принимается и три выбранных сохраняются; провал одной загрузки не откатывает отзыв.

## 3. Показ промодерированных фото

- [ ] 3.1 Отрисовать поле `photos` публичного списка отзывов в `components/quests/QuestReviewsModal.tsx`.
- [ ] 3.2 Отрисовать те же фото в блоке отзывов на странице квеста.
- [ ] 3.3 Тест: отзыв без фото рендерится прежним образом, без пустого места на месте галереи.

## 4. Аналитика

- [ ] 4.1 Добавить событие `quest_photo_upload` в `utils/questReviewAnalytics.ts` рядом с существующим `quest_review_submit`.
- [ ] 4.2 Тест: событие уходит один раз на подтверждённую загрузку, не уходит при ошибке и отмене выбора, существующее `quest_review_submit` не дублируется.

## 5. Отметка «точка изменилась»

- [ ] 5.1 Добавить клиент структурной отметки `POST /api/quest-steps/{id}/inaccuracy-reports/` с разбором 201 (создано) и 200 (повтор).
- [ ] 5.2 Добавить действие на точке маршрута рядом со свободнотекстовой формой; подписи обеих точек входа должны различать назначение.
- [ ] 5.3 Показать «уже отмечено» на ответ 200 вместо второго подтверждения.
- [ ] 5.4 Гарантировать, что отказ отправки не меняет состояние шага и не прерывает прохождение.
- [ ] 5.5 Тест: неавторизованный посетитель не отправляет отметку и получает существующий путь входа; ошибка сети не ломает прохождение.
- [ ] 5.6 Убедиться, что `components/quests/QuestInaccuracyReportModal.tsx` не изменён.

## 6. Локализация

- [ ] 6.1 Провести все новые подписи (добавить фото, предел трёх, ошибки загрузки, «фото на проверке», «точка изменилась», «уже отмечено») через `@/i18n` в RU/BE/UK/PL/EN.
- [ ] 6.2 Прогнать `npm run test:i18n`.

## 7. Доступность и целевые проверки

- [ ] 7.1 Проставить accessibilityLabel и размер цели нажатия по правилам проекта для пикера и действия отметки; статус загрузки объявлять текстом, а не только цветом.
- [ ] 7.2 Прогнать `node scripts/guard-text-row-sizing.js` и остальные релевантные guard-скрипты по затронутой зоне.
- [ ] 7.3 Прогнать целевые Jest-наборы по quests и adapter-тест загрузки; затем `npm run check:fast`.

## 8. Review

- [ ] 8.1 Независимый review-and-fix полного diff задачи, повтор code-level проверок после правок.
- [ ] 8.2 Коммит явными путями задачи и push в `main`, sha — в тикет #1579; затем перевод в `testing`.

## 9. Runtime и device acceptance (после перехода в `testing`)

- [ ] 9.1 Браузер: загрузка одного и трёх фото, отказ на четвёртом, частичный провал, показ промодерированных фото; console и network как evidence.
- [ ] 9.2 Физический Android: выбор из галереи, съёмка камерой, отказ в разрешении, отмена выбора.
- [ ] 9.3 iPhone (simulator либо устройство): те же четыре сценария; hardware-разрешения симулятором не доказываются.
- [ ] 9.4 Отметка точки на локальном стеке: первая отметка создаёт, повтор идемпотентен, ошибка не ломает прохождение. Прод-часть держать связанной с закрытием #1577.
- [ ] 9.5 Записать evidence в #1579, тестовые фото и отметки после проверки убрать.
