## 1. Поколение в снапшоте и слиянии

- [x] 1.1 Добавить в `QuestProgressSnapshot` поле `serverId` (0 — поколение неизвестно): нормализация, `snapshotFromServerProgress` берёт `id` строки, серверный payload поле не получает
- [x] 1.2 В `mergeQuestProgress` при разных ненулевых поколениях вернуть целиком снапшот с большим `serverId`, при равных или одном нулевом — прежнее объединение с ненулевым поколением; `serverId` — в локальном отпечатке, не в серверном
- [x] 1.3 Тесты слияния: разные поколения, усвоение 0 → S, неизменность `serverNeedsPush` от одного поколения, прежние сценарии объединения зелёные

## 2. Единый писатель и доставка

- [x] 2.1 `withQuestProgress(questId, task, { lineageId })`: при заданном поколении строка обязана существовать с этим `id`, иначе `QuestProgressLineageMismatch` с текущей строкой или `null`, без `POST` и без вызова задачи
- [x] 2.2 `pushQuestProgressSnapshot` передаёт поколение снапшота писателю; миграция гостевого прогресса и `fetchOrCreateProgress` — без опции
- [x] 2.3 Проход очереди снимает запись при несовпадении поколения и продолжает с остальными квестами; `deliverOrEnqueueQuestProgress` при несовпадении снимает запись и не кладёт её обратно
- [x] 2.4 Тесты писателя и очереди: совпадение → PATCH как раньше; строки нет → ни `POST`, ни `PATCH`, запись снята; другая строка → запись снята, строка не изменена; снапшот без поколения → создание строки (#1803)

## 3. Экран квеста и хук синхронизации

- [x] 3.1 `useQuestProgressSync`: состояние `progressMissing` (404 чтения, подтверждённое удаление, несовпадение без строки) отдельно от упавшего чтения; поколение в отложенном снапшоте
- [x] 3.2 Флаш экрана при несовпадении: выбросить отложенный снапшот, снять запись с очереди, не планировать ретрай, перейти на строку из ошибки или `null`, освежить отметку каталога
- [x] 3.3 Экран передаёт визарду `serverId: null` при подтверждённом отсутствии строки и снапшот без поколения при неизвестном состоянии; типы пропсов `QuestWizard`
- [x] 3.4 Тесты хука: 404 → подтверждено; сбой чтения → не подтверждено; несовпадение при флаше → без ретрая и с переходом состояния

## 4. Визард

- [x] 4.1 Запись хранилища и payload `onProgressChange` несут `serverId`; ref поколения засевается из сида и сбрасывается `resetProgress`
- [x] 4.2 Первое согласование: копия с поколением + подтверждённое отсутствие → пустое состояние; + другая строка → состояние сервера; без отправки и со сбросом `completionFinishedAt`
- [x] 4.3 Обновления в сессии: то же правило против живого снапшота с курсором сервера; усвоение поколения сохраняет курсор игрока и пишет поколение в хранилище
- [x] 4.4 Тесты визарда: регресс #2033 (сброс на другом устройстве не доливается), #1803 (офлайн без строки доливается), упавшее чтение не стирает копию, поколение попадает в хранилище после первой отправки

## 5. Документация и проверка

- [x] 5.1 Запись в `docs/PROBLEM_MEMORY.md` рядом с QUEST-PROGRESS-CARRYOVER-001 и строка structural backlog про единый контракт согласования прогресса
- [x] 5.2 Узкие jest-наборы прогресса квеста, `npm run check:fast`, typecheck затронутых файлов
- [x] 5.3 Независимое код-ревью полного diff (code-review-gate) и исправление находок
- [x] 5.4 Browser evidence после выката: два контекста одного аккаунта на desktop web и mobile web — сброс во втором не воскресает из первого, сеть без `POST`/`PATCH`, консоль без ошибок
- [x] 5.5 `openspec validate quest-progress-reset-lineage --strict`
