# Анализ прод-сервера metravel.by — 2026-06-13

**Сервер:** `sx3@178.172.137.129` (server-dtwewu, 15 GB диск, 1867 MB RAM, 1 core, uptime 3 дня)
**Источник данных:** `~/metravel-monitor/metrics.jsonl` (cron `collect-metrics.sh`, 1 замер/мин)
**Окно анализа:** 2026-06-09T12:20Z → 2026-06-12T22:05Z (4871 замер)

## Вердикт
Сайт жив и отвечает (home/api 200, фронт ~40 мс). Найдены 3 деградации; одна имеет ясную корневую причину.

## 🔴 Корень: Django-app работает с `DEBUG=True` в проде
В логах `metravel_app_1` — построчный дамп каждого SQL (`DEBUG django.db.backends`). При `DEBUG=True` Django копит все запросы в `connection.queries` без очистки → утечка памяти.
- Память app_1: **665 → 874 MB** (лимит 900) — монотонный рост в потолок.
- **23 рестарта** контейнера за 3.4 дня (пик mem 100%, cpu 99%) — OOM-перезапуски по лимиту.
- Каждый рестарт = всплеск **502 на /api** (кластеры 09-го 18:32–36, 10-го 18:53–19:01) и подвисания api до **14–18 с**.

Связь: prod outage 2026-06-09 (SECRET_KEY не прокинут при DEBUG=False) — `DEBUG=True`, видимо, оставлен как обход и теперь даёт утечку/рестарты.
**Backend → не правим, нужен тикет:** выключить DEBUG, прокинуть SECRET_KEY/ALLOWED_HOSTS, снять логгер `django.db.backends`.

## 🟠 Диск рос быстро — почищено
Было `13G/15G, 91%`, рост 72% → 91% за 3 дня (~6%/день; алерт `monitor.sh` на 95%).
**Действие 2026-06-13:** `docker image prune -f` + `docker builder prune -f` → освобождено **1.62 GB**, стало **79% (свободно 3.1 GB)**. Основной прирост диска — логи DEBUG; уйдёт вместе с фиксом выше.

## 🟡 Память хоста тесная
RAM 1867 MB. Доступно **700 → 423 MB** за период, swap used **9 → 171 MB** (пик 954 MB). OOM-киллов ядра нет (`oom_total=0`), но запас на нуле.

## ✅ В норме
- Фронт (home): p95 0.069 с, max 0.41 с.
- nginx / redis / redis-images / gis — 0 рестартов, памяти с запасом.
- api: p50 0.144 с, p95 0.936 с (вне рестарт-окон).
- Мониторинг работает; alert-спам по CPU в `monitor.sh` уже починен.

## HTTP-сбои за период (22 не-200)
- api 502 ×N — во время рестартов app (09 18:32–36, 10 18:53–19:01), 1× api 500.
- home 404 ×2 (10-го 09:33, 12-го 10:27) — вероятно во время деплоя.

## Приоритет действий
1. **P1 backend-тикет:** выключить `DEBUG` + убрать SQL-логгер — лечит утечку/рестарты/502/рост диска разом.
2. ✅ **Сделано:** очистка docker-образов (+1.62 GB диска).

## Инфра-справка
- Скрипты на сервере: `monitor.sh` (alert по почте, */5 мин), `metravel-monitor/collect-metrics.sh` (метрики, 1 мин), `report.sh` (10:00), `restart_metravel_app.sh` (04:00), `seo_report.sh` (09:00).
- Контейнеры: `metravel_app_1` (Django, лимит 900 MB), `metravel_nginx_1`, `metravel_redis_1`, `metravel_redis-images_1`, `metravel_metravel-gis_1`.
