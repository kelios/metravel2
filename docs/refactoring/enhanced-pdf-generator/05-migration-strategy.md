# Миграционная стратегия

## 🎯 Цели миграции

1. **Безопасность** - не сломать существующую функциональность
2. **Постепенность** - внедрять изменения итерациями
3. **Обратная совместимость** - старый код продолжает работать
4. **Прозрачность** - пользователи не замечают изменений
5. **Откат** - возможность вернуться к старой версии

## 📅 График миграции

### Этап 1: Подготовка (неделя 1)

#### Задачи:
- [x] Создать документацию текущего состояния
- [ ] Зафиксировать текущее поведение тестами
- [ ] Создать бенчмарки производительности
- [ ] Настроить feature flags

#### Checklist:
```typescript
// 1. Все существующие тесты проходят
npm run test:pdf -- --coverage

// 2. Baseline производительности
npm run benchmark:pdf

// 3. Feature flags готовы
// config/features.ts
export const features = {
  useNewPdfGenerator: false,
  useImageCache: false,
  useParallelGeneration: false
};
```

#### Результат:
- ✅ 100% тестов проходят
- ✅ Baseline метрик зафиксирован
- ✅ Feature flags работают

### Этап 2: Создание новой архитектуры (неделя 2-3)

#### Задачи:
- [ ] Создать новые компоненты
- [ ] Написать unit тесты
- [ ] Интегрировать с feature flags
- [ ] Добавить fallback на старый код

#### Структура:
```
src/services/pdf-export/
├── generators/
│   ├── EnhancedPdfGenerator.ts (старый)
│   └── v2/
│       ├── EnhancedPdfGenerator.ts (новый)
│       ├── PageGenerators/
│       ├── Processors/
│       └── Builders/
```

#### Пример с feature flag:
```typescript
// BookHtmlExportService.ts
import { features } from '@/config/features';
import { EnhancedPdfGenerator as V1Generator } from './generators/EnhancedPdfGenerator';
import { EnhancedPdfGenerator as V2Generator } from './generators/v2/EnhancedPdfGenerator';

class BookHtmlExportService {
  async generateTravelsHtml(travels, settings) {
    const Generator = features.useNewPdfGenerator 
      ? V2Generator 
      : V1Generator;
    
    const generator = new Generator(settings.template);
    return generator.generate(travels, settings);
  }
}
```

#### Результат:
- ✅ Новая архитектура работает рядом со старой
- ✅ Можно переключаться через feature flag
- ✅ Тесты проходят для обеих версий

### Этап 3: Внутреннее тестирование (неделя 4)

#### Задачи:
- [ ] Включить новую версию для команды
- [ ] Собрать метрики
- [ ] Найти и исправить баги
- [ ] Сравнить производительность

#### A/B тестирование:
```typescript
// Рандомно выбираем версию
const useV2 = features.useNewPdfGenerator || Math.random() > 0.5;

// Логируем метрики
telemetry.track('pdf-generation', {
  version: useV2 ? 'v2' : 'v1',
  duration: endTime - startTime,
  travelCount: travels.length,
  success: true
});
```

#### Метрики для сравнения:
- Время генерации
- Размер HTML
- Использование памяти
- Количество ошибок
- Удовлетворенность пользователей

#### Результат:
- ✅ V2 работает стабильно
- ✅ Производительность не хуже V1
- ✅ Нет критичных багов

### Этап 4: Beta релиз (неделя 5)

#### Задачи:
- [ ] Включить для beta-пользователей
- [ ] Собрать обратную связь
- [ ] Исправить найденные проблемы
- [ ] Обновить документацию

#### Beta программа:
```typescript
// Проверяем есть ли пользователь в beta-программе
const isBetaUser = await userService.isBetaTester(userId);
const useV2 = features.useNewPdfGenerator || isBetaUser;
```

#### Сбор feedback:
```typescript
// Показываем опрос после генерации PDF
if (usedV2) {
  showSurvey({
    question: 'Как вам новый генератор PDF?',
    options: ['Отлично', 'Хорошо', 'Нормально', 'Плохо', 'Ужасно'],
    freeForm: 'Расскажите подробнее (опционально)'
  });
}
```

#### Результат:
- ✅ Beta-пользователи довольны
- ✅ Критичные баги исправлены
- ✅ Документация обновлена

### Этап 5: Постепенный rollout (неделя 6-7)

#### Задачи:
- [ ] 10% пользователей на V2
- [ ] 25% пользователей на V2
- [ ] 50% пользователей на V2
- [ ] 75% пользователей на V2
- [ ] 100% пользователей на V2

#### Стратегия rollout:
```typescript
class RolloutStrategy {
  shouldUseV2(userId: string): boolean {
    // Детерминированно на основе userId
    const hash = this.hashUserId(userId);
    const percentage = this.getCurrentRolloutPercentage();
    return (hash % 100) < percentage;
  }
  
  private getCurrentRolloutPercentage(): number {
    // Возвращает текущий процент rollout из конфига
    return config.get('pdf.v2.rollout.percentage');
  }
}
```

#### Мониторинг:
```typescript
// Отслеживаем ключевые метрики
dashboard.monitor({
  errorRate: 'pdf.generation.errors',
  performance: 'pdf.generation.duration',
  successRate: 'pdf.generation.success',
  userSatisfaction: 'pdf.generation.satisfaction'
});

// Автоматический откат при проблемах
if (errorRate > 5% || performance > baseline * 1.5) {
  rollback();
  alert('PDF generator rollback triggered!');
}
```

#### Результат:
- ✅ V2 используется всеми пользователями
- ✅ Метрики стабильны
- ✅ Нет увеличения ошибок

### Этап 6: Удаление старого кода (неделя 8)

#### Задачи:
- [ ] Пометить V1 как deprecated
- [ ] Обновить документацию
- [ ] Удалить feature flags
- [ ] Удалить V1 код

#### Deprecation:
```typescript
// EnhancedPdfGenerator.ts (V1)
/**
 * @deprecated Use v2/EnhancedPdfGenerator instead
 * This will be removed in version 3.0
 */
export class EnhancedPdfGenerator {
  constructor() {
    console.warn(
      'EnhancedPdfGenerator V1 is deprecated. ' +
      'Please migrate to v2/EnhancedPdfGenerator'
    );
  }
}
```

#### Удаление:
```bash
# 1. Удалить feature flags
git rm src/config/features.ts

# 2. Переместить V2 на место V1
mv src/services/pdf-export/generators/v2/* src/services/pdf-export/generators/

# 3. Удалить старый код
git rm src/services/pdf-export/generators/EnhancedPdfGenerator.old.ts

# 4. Обновить импорты
find . -type f -name "*.ts" -exec sed -i 's/generators\/v2\//generators\//g' {} +
```

#### Результат:
- ✅ Старый код удален
- ✅ Кодовая база чище
- ✅ Нет технического долга

## 🔄 План отката

### Сценарии для отката:

1. **Критический баг** - мгновенный откат
2. **Деградация производительности** - откат в течение часа
3. **Рост ошибок** - откат в течение дня
4. **Негативный feedback** - обсуждение и решение

### Процедура отката:

```typescript
// 1. Изменить feature flag
config.set('pdf.v2.enabled', false);

// 2. Перезапустить сервисы
pm2 restart all

// 3. Верифицировать
curl https://api.metravel.by/health

// 4. Мониторинг
watch -n 5 'curl -s https://api.metravel.by/metrics | grep pdf'

// 5. Анонсировать
slack.notify('PDF generator rolled back to V1');
```

### Checklist отката:

- [ ] Feature flag выключен
- [ ] Сервисы перезапущены
- [ ] Health checks проходят
- [ ] Метрики вернулись к норме
- [ ] Пользователи уведомлены (при необходимости)
- [ ] Post-mortem запланирован

## 📊 Метрики успеха

### Технические метрики:

| Метрика | Baseline (V1) | Target (V2) | Статус |
|---------|--------------|-------------|--------|
| Время генерации (1 travel) | 500ms | < 400ms | 🔄 |
| Время генерации (10 travels) | 3s | < 2s | 🔄 |
| Размер кода | 2111 строк | < 500 строк | 🔄 |
| Test coverage | 40% | > 80% | 🔄 |
| Error rate | 2% | < 1% | 🔄 |
| Memory usage | 150MB | < 100MB | 🔄 |

### Бизнес метрики:

| Метрика | Baseline | Target | Статус |
|---------|----------|--------|--------|
| User satisfaction | 4.2/5 | > 4.5/5 | 🔄 |
| PDF completion rate | 85% | > 90% | 🔄 |
| Support tickets | 10/week | < 5/week | 🔄 |
| Time to add feature | 8 hours | < 2 hours | 🔄 |

## 🧪 Тестовый план миграции

### Pre-migration тесты:

```bash
# 1. Все тесты проходят
npm run test

# 2. E2E тесты
npm run test:e2e

# 3. Performance тесты
npm run test:performance

# 4. Visual regression
npm run test:visual
```

### During migration тесты:

```bash
# Тесты для обеих версий
npm run test -- --testPathPattern="(v1|v2)"

# Сравнение output
npm run test:compare-output
```

### Post-migration тесты:

```bash
# Smoke тесты
npm run test:smoke

# Regression тесты
npm run test:regression

# Load тесты
npm run test:load
```

## 📝 Коммуникация

### Команде:

1. **Кикофф встреча** - презентация плана миграции
2. **Еженедельные sync'и** - статус и блокеры
3. **Demo** - показ новой версии
4. **Retrospective** - что прошло хорошо/плохо

### Пользователям:

1. **Анонс beta** - приглашение протестировать
2. **Release notes** - описание изменений
3. **Migration guide** - если API изменился
4. **Support** - помощь при проблемах

### Стейкхолдерам:

1. **Status reports** - еженедельно
2. **Risk assessment** - при появлении рисков
3. **Go/no-go meeting** - перед финальным rollout
4. **Post-mortem** - после завершения

## ⚠️ Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Критический баг в продакшене | Средняя | Высокое | Feature flags, быстрый откат |
| Деградация производительности | Низкая | Среднее | A/B тестирование, мониторинг |
| Конфликты в команде | Низкая | Низкое | Четкие роли, регулярная коммуникация |
| Технический долг в V2 | Средняя | Среднее | Code review, рефакторинг по ходу |
| Пользователи недовольны | Низкая | Высокое | Beta программа, сбор feedback |
| Задержка сроков | Высокая | Среднее | Буфер времени, приоритизация |

## ✅ Критерии завершения миграции

- [x] Документация создана
- [ ] V2 реализован и протестирован
- [ ] Beta тестирование прошло успешно
- [ ] 100% пользователей на V2
- [ ] Метрики успеха достигнуты
- [ ] Старый код удален
- [ ] Команда обучена
- [ ] Документация обновлена
- [ ] Post-mortem проведен
- [ ] Lessons learned задокументированы

## 🎓 Обучение команды

### Материалы:

1. **Видео** - Overview новой архитектуры (30 мин)
2. **Документация** - Подробное описание компонентов
3. **Code walkthrough** - Разбор ключевых частей
4. **Workshop** - Практика написания нового кода

### Workshop план:

```
День 1 (4 часа):
- 09:00 - Презентация новой архитектуры
- 10:00 - Демо основных компонентов
- 11:00 - Перерыв
- 11:15 - Hands-on: создание PageGenerator
- 12:30 - Обед
- 13:30 - Hands-on: тестирование компонентов
- 15:00 - Q&A

День 2 (4 часа):
- 09:00 - Code review лучших практик
- 10:00 - Типичные ошибки и как их избежать
- 11:00 - Перерыв
- 11:15 - Hands-on: интеграция нового компонента
- 12:30 - Обед
- 13:30 - Финальный проект
- 15:00 - Презентация результатов
```

## 📚 Документация для миграции

Создать:

1. **MIGRATION.md** - Пошаговый guide
2. **API_CHANGES.md** - Изменения в API
3. **BREAKING_CHANGES.md** - Что сломалось
4. **FAQ.md** - Частые вопросы
5. **TROUBLESHOOTING.md** - Решение проблем

## 🎉 Празднование успеха

После успешной миграции:

1. **Team lunch** - отметить достижение
2. **Blog post** - поделиться опытом
3. **Presentation** - рассказать на встрече компании
4. **Lessons learned** - что узнали

