# Задачи по улучшению UI/UX создания/редактирования путешествий

## 🎯 Приоритизированный список задач

---

## Фаза 1: Quick Wins (1-2 недели)

### Задача 1.1: Онбординг-экран для новых пользователей
**Priority:** 🔥🔥🔥 HIGH  
**Effort:** 🔧🔧 Medium (3 дня)  
**Impact:** +15% completion rate

**Описание:**
Создать модальное окно с 3 слайдами, которое показывается при первом создании путешествия.

**Требования:**
- 3 слайда с иллюстрациями и текстом
- Кнопка "Начать" для закрытия
- Checkbox "Больше не показывать"
- Сохранение в localStorage
- Адаптивный дизайн (mobile/desktop)

**Файлы для создания:**
```
components/travel/wizard/TravelWizardOnboarding.tsx
hooks/useTravelWizardOnboarding.ts
```

**Интеграция:**
```tsx
// UpsertTravel.tsx
const { shouldShow, handleDismiss } = useTravelWizardOnboarding();

return (
  <>
    <TravelWizardOnboarding visible={shouldShow} onDismiss={handleDismiss} />
    {/* existing wizard */}
  </>
);
```

**Acceptance Criteria:**
- [ ] Modal показывается только при первом создании путешествия
- [ ] Работает на web, iOS, Android
- [ ] Сохраняется выбор "Больше не показывать"
- [ ] Анимация перехода между слайдами
- [ ] Адаптируется под размер экрана

---

### Задача 1.2: Рекомендации по загрузке медиа
**Priority:** 🔥🔥 MEDIUM  
**Effort:** 🔧 Easy (1 день)  
**Impact:** +20% качество фото

**Описание:**
Добавить info-карточку с рекомендациями по формату и размеру обложки/фото.

**Требования:**
- Info-карточка над компонентом загрузки
- Текст рекомендаций (16:9, мин. 1200×675px)
- Иконка "lightbulb"
- Статистика ("Путешествия с обложкой: +300% кликов")

**Файлы для изменения:**
```
components/travel/TravelWizardStepMedia.tsx (добавить карточку)
```

**Код (концепция):**
```tsx
<View style={styles.tipsCard}>
  <Icon name="lightbulb" size={20} color={colors.warning} />
  <View style={styles.tipContent}>
    <Text style={styles.tipTitle}>Совет по обложке</Text>
    <Text style={styles.tipBody}>
      Горизонтальные фото 16:9 (минимум 1200×675px) выглядят лучше всего.
      Избегайте коллажей и текста на изображении.
    </Text>
    <Text style={styles.tipStats}>
      📊 Путешествия с обложкой получают в 3 раза больше просмотров
    </Text>
  </View>
</View>
```

**Acceptance Criteria:**
- [ ] Карточка видна на шаге 3
- [ ] Стили соответствуют дизайн-системе
- [ ] Работает темная тема
- [ ] Адаптивный текст на мобильных

---

### Задача 1.3: Группировка дополнительных параметров
**Priority:** 🔥🔥 MEDIUM  
**Effort:** 🔧 Easy (2 дня)  
**Impact:** Улучшение UX шага 5

**Описание:**
Разбить длинный список фильтров на шаге 5 на группы с аккордеонами.

**Требования:**
- 4 группы: "Основное", "Время и сложность", "Стиль путешествия", "Практическая информация"
- Аккордеоны (collapsible sections)
- По умолчанию открыта только "Основное"
- Иконки для каждой группы
- Счетчик заполненных полей в заголовке (например: "Основное (2/3)")

**Файлы для изменения:**
```
components/travel/TravelWizardStepExtras.tsx
components/travel/FiltersUpsertComponent.tsx (добавить props для видимости полей)
```

**Код (концепция):**
```tsx
const sections = [
  {
    id: 'main',
    title: 'Основное',
    icon: 'star',
    fields: ['categories', 'transports'],
  },
  // ...
];

return (
  <ScrollView>
    {sections.map((section) => (
      <Collapsible
        key={section.id}
        title={`${section.title} (${filledCount}/${totalCount})`}
        icon={section.icon}
        defaultExpanded={section.id === 'main'}
      >
        <FiltersUpsertComponent visibleFields={section.fields} ... />
      </Collapsible>
    ))}
  </ScrollView>
);
```

**Acceptance Criteria:**
- [ ] 4 секции с аккордеонами
- [ ] Анимация открытия/закрытия
- [ ] Счетчик заполненных полей
- [ ] Работает на всех платформах
- [ ] Сохранение состояния (какие секции открыты)

---

### Задача 1.4: Разделение чеклиста на шаге 6
**Priority:** 🔥🔥 MEDIUM  
**Effort:** 🔧 Easy (1 день)  
**Impact:** Снижение демотивации

**Описание:**
Разделить список готовности на "Обязательно" и "Рекомендуем".

**Требования:**
- 2 секции: "Обязательно для публикации" (3 пункта) и "Рекомендуем заполнить" (3 пункта)
- Разные иконки (❌/✅ для обязательных, ⚠️/💡 для рекомендуемых)
- Статистика пользы ("Путешествия с фото: +300% кликов")

**Файлы для изменения:**
```
components/travel/TravelWizardStepPublish.tsx
```

**Код (концепция):**
```tsx
const requiredChecklist = [
  { key: 'name', label: 'Название (мин. 3 символа)', ok: hasName },
  { key: 'description', label: 'Описание (мин. 50 символов)', ok: hasDescription },
  { key: 'route', label: 'Маршрут на карте (мин. 1 точка)', ok: hasRoute },
];

const recommendedChecklist = [
  { key: 'countries', label: 'Страны маршрута', ok: hasCountries, benefit: 'Для фильтров' },
  { key: 'categories', label: 'Категории маршрута', ok: hasCategories, benefit: '+40% в поиске' },
  { key: 'photos', label: 'Фото или обложка', ok: hasPhotos, benefit: '+300% кликов' },
];

return (
  <>
    <View style={styles.section}>
      <Text style={styles.sectionTitle}>✅ Обязательно для публикации</Text>
      {requiredChecklist.map(item => (
        <ChecklistItem key={item.key} {...item} required />
      ))}
    </View>
    
    <View style={styles.section}>
      <Text style={styles.sectionTitle}>💡 Рекомендуем заполнить</Text>
      {recommendedChecklist.map(item => (
        <ChecklistItem key={item.key} {...item} showBenefit />
      ))}
    </View>
  </>
);
```

**Acceptance Criteria:**
- [ ] Две четкие секции
- [ ] Разные стили для обязательных и рекомендуемых
- [ ] Показ пользы для рекомендуемых полей
- [ ] Клик по пункту → переход к полю
- [ ] Работает на всех платформах

---

### Задача 1.5: Улучшенный прогресс-бар с милестонами
**Priority:** 🔥 LOW  
**Effort:** 🔧 Easy (1 день)  
**Impact:** Визуальное улучшение

**Описание:**
Добавить точки-милестоны для каждого шага над прогресс-баром.

**Требования:**
- 6 точек над прогресс-баром
- Активные точки (уже пройденные) — закрашены
- Неактивные — пустые
- Клик по точке → переход к шагу
- Анимация заполнения

**Файлы для изменения:**
```
components/travel/TravelWizardHeader.tsx
```

**Код (концепция):**
```tsx
<View style={styles.progressWrapper}>
  <View style={styles.milestones}>
    {[1, 2, 3, 4, 5, 6].map((step) => (
      <Pressable
        key={step}
        onPress={() => onStepSelect?.(step)}
        style={[
          styles.milestone,
          step <= currentStep && styles.milestoneActive,
        ]}
      >
        <View style={styles.milestoneCircle}>
          {step <= currentStep && <View style={styles.milestoneDot} />}
        </View>
        <Text style={styles.milestoneNumber}>{step}</Text>
      </Pressable>
    ))}
  </View>
  <View style={styles.progressBarTrack}>
    <Animated.View style={[styles.progressBarFill, { width: progressAnimation }]} />
  </View>
</View>
```

**Acceptance Criteria:**
- [ ] 6 точек над прогресс-баром
- [ ] Клик по точке → переход к шагу
- [ ] Анимация заполнения
- [ ] Адаптивный размер на мобильных
- [ ] Работает с темной темой

---

## Фаза 2: Major Improvements (2-3 недели)

### Задача 2.1: Quick Mode (быстрый черновик)
**Priority:** 🔥🔥🔥 HIGH  
**Effort:** 🔧🔧 Medium (2 дня)  
**Impact:** -30% time to first save

**Описание:**
Добавить кнопку "Быстрый черновик" на шаге 1 для мгновенного сохранения идеи.

**Требования:**
- Кнопка в footer шага 1
- Валидация только название (мин. 3 символа)
- Сохранение с минимальными данными
- Toast "Черновик сохранен"
- Редирект в /metravel

**Файлы для изменения:**
```
components/travel/TravelWizardStepBasic.tsx
components/travel/TravelWizardFooter.tsx (добавить prop для extraActions)
hooks/useTravelFormData.ts (добавить метод quickSave)
```

**Код (концепция):**
```tsx
// TravelWizardStepBasic.tsx
const handleQuickSave = async () => {
  if (!formData.name || formData.name.length < 3) {
    Toast.show({
      type: 'error',
      text1: 'Заполните название',
      text2: 'Минимум 3 символа',
    });
    return;
  }
  
  await onManualSave();
  
  Toast.show({
    type: 'success',
    text1: 'Черновик сохранен',
    text2: 'Вы можете вернуться к нему позже',
  });
  
  router.push('/metravel');
};

return (
  <>
    {/* existing content */}
    <TravelWizardFooter
      extraActions={
        <Button
          mode="outlined"
          onPress={handleQuickSave}
          disabled={!formData.name || formData.name.length < 3}
        >
          💾 Быстрый черновик
        </Button>
      }
    />
  </>
);
```

**Acceptance Criteria:**
- [ ] Кнопка видна на шаге 1
- [ ] Валидация названия перед сохранением
- [ ] Сохранение и редирект работают
- [ ] Toast показывается
- [ ] Аналитика: trackEvent('quick_draft_saved')

---

### Задача 2.2: Поиск мест по названию на карте
**Priority:** 🔥🔥🔥 HIGH  
**Effort:** 🔧🔧🔧 Hard (5 дней)  
**Impact:** Значительное упрощение работы с картой

**Описание:**
Интеграция поиска мест через Nominatim Search API.

**Требования:**
- Поле поиска над картой
- Debounced поиск (500ms)
- Dropdown с результатами (до 5)
- Клик по результату → центровка карты + добавление маркера
- Показ адреса и страны в результатах
- Индикатор загрузки

**Файлы для создания:**
```
components/travel/map/MapSearchBar.tsx
hooks/useMapSearch.ts
utils/nominatimApi.ts
```

**API:**
```typescript
// utils/nominatimApi.ts
export async function searchPlaces(query: string): Promise<SearchResult[]> {
  const response = await fetch(
    `https://nominatim.openstreetmap.org/search?` +
    `q=${encodeURIComponent(query)}&format=json&limit=5&addressdetails=1`
  );
  return await response.json();
}
```

**Интеграция:**
```tsx
// TravelWizardStepRoute.tsx
import MapSearchBar from '@/components/travel/map/MapSearchBar';

const handlePlaceSelect = (place: SearchResult) => {
  const newMarker: MarkerData = {
    lat: Number(place.lat),
    lng: Number(place.lon),
    address: place.display_name,
    country: matchCountryId(place.address.country, countries),
    categories: [],
  };
  
  setMarkers([...markers, newMarker]);
  
  // Центровка карты
  mapRef.current?.setCenter({ lat: newMarker.lat, lng: newMarker.lng });
};

return (
  <>
    <MapSearchBar onPlaceSelect={handlePlaceSelect} />
    <WebMapComponent ... />
  </>
);
```

**Acceptance Criteria:**
- [ ] Поиск работает с debounce 500ms
- [ ] Показываются до 5 результатов
- [ ] Клик по результату → добавление маркера
- [ ] Карта центрируется на выбранном месте
- [ ] Показывается индикатор загрузки
- [ ] Обработка ошибок сети
- [ ] Работает на всех платформах

---

### Задача 2.3: Превью карточки путешествия
**Priority:** 🔥🔥 MEDIUM  
**Effort:** 🔧🔧 Medium (3 дня)  
**Impact:** Понимание результата

**Описание:**
Добавить возможность увидеть превью карточки путешествия в процессе заполнения.

**Требования:**
- Кнопка "👁️ Превью" в header
- Modal с превью карточки
- Использование существующего компонента TravelCard
- Трансформация TravelFormData → Travel
- Адаптивный размер modal

**Файлы для создания:**
```
components/travel/wizard/TravelWizardPreview.tsx
utils/travelFormUtils.ts (добавить transformFormDataToTravel)
```

**Интеграция:**
```tsx
// TravelWizardHeader.tsx
import TravelWizardPreview from './wizard/TravelWizardPreview';

const [previewVisible, setPreviewVisible] = useState(false);

return (
  <>
    <View style={styles.headerRightRow}>
      <IconButton
        icon="eye"
        onPress={() => setPreviewVisible(true)}
        size={20}
        accessibilityLabel="Показать превью"
      />
      {/* existing elements */}
    </View>
    
    <TravelWizardPreview
      visible={previewVisible}
      formData={formData}
      onClose={() => setPreviewVisible(false)}
    />
  </>
);
```

**Acceptance Criteria:**
- [ ] Кнопка превью в header
- [ ] Modal с карточкой путешествия
- [ ] Показываются актуальные данные из формы
- [ ] Работает на всех шагах
- [ ] Адаптивный дизайн
- [ ] Можно закрыть (X или клик вне)

---

### Задача 2.4: Умные подсказки (contextual tips)
**Priority:** 🔥🔥 MEDIUM  
**Effort:** 🔧🔧 Medium (2 дня)  
**Impact:** Guidance в процессе

**Описание:**
Показывать подсказки на основе текущего контекста (например, количества заполненных полей).

**Требования:**
- Баннер с иконкой, текстом и тоном (info/success/warning)
- Разные подсказки для каждого шага
- Динамическое отображение на основе данных
- Анимация появления/исчезновения

**Файлы для создания:**
```
components/travel/wizard/ContextualTips.tsx
hooks/useContextualTips.ts
constants/wizardTips.ts
```

**Конфигурация подсказок:**
```typescript
// constants/wizardTips.ts
export const WIZARD_TIPS: Record<number, TipConfig[]> = {
  1: [
    {
      condition: (ctx) => !ctx.name,
      icon: 'edit',
      text: 'Начните с названия — это поможет сформулировать идею',
      tone: 'info',
    },
  ],
  2: [
    {
      condition: (ctx) => ctx.markers.length === 0,
      icon: 'map-pin',
      text: 'Кликните на карту или используйте поиск, чтобы добавить первую точку',
      tone: 'info',
    },
    {
      condition: (ctx) => ctx.markers.length === 1,
      icon: 'trending-up',
      text: 'Отлично! Добавьте еще 2-5 ключевых точек для полного маршрута',
      tone: 'success',
    },
    {
      condition: (ctx) => ctx.markers.length >= 7,
      icon: 'alert-circle',
      text: 'Много точек может усложнить восприятие. Оставьте только ключевые.',
      tone: 'warning',
    },
  ],
  // ...
};
```

**Интеграция:**
```tsx
// TravelWizardStepRoute.tsx
import ContextualTips from './wizard/ContextualTips';
import { useContextualTips } from '@/hooks/useContextualTips';

const activeTip = useContextualTips(currentStep, { markers });

return (
  <>
    {activeTip && <ContextualTips tip={activeTip} />}
    {/* existing content */}
  </>
);
```

**Acceptance Criteria:**
- [ ] Подсказки показываются на основе контекста
- [ ] Разные подсказки для каждого шага
- [ ] Анимация появления
- [ ] Адаптивный дизайн
- [ ] Работает с темной темой

---

### Задача 2.5: Анимация переходов между шагами
**Priority:** 🔥 LOW  
**Effort:** 🔧🔧 Medium (2 дня)  
**Impact:** Визуальное улучшение

**Описание:**
Добавить плавную анимацию при переходе между шагами визарда.

**Требования:**
- Slide-анимация (влево при "Далее", вправо при "Назад")
- Fade-анимация для прогресс-бара
- Haptic feedback на мобильных
- 300ms duration

**Файлы для изменения:**
```
components/travel/UpsertTravel.tsx (добавить Animated.View)
components/travel/TravelWizardHeader.tsx (анимация прогресс-бара)
```

**Код (концепция):**
```tsx
// UpsertTravel.tsx
import { useSharedValue, useAnimatedStyle, withTiming } from 'react-native-reanimated';

const slideOffset = useSharedValue(0);

useEffect(() => {
  slideOffset.value = withTiming(currentStep * -100, { duration: 300 });
  
  // Haptic feedback
  if (Platform.OS !== 'web') {
    Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
  }
}, [currentStep]);

const animatedStyle = useAnimatedStyle(() => ({
  transform: [{ translateX: slideOffset.value }],
}));

return (
  <Animated.View style={animatedStyle}>
    {/* step components */}
  </Animated.View>
);
```

**Acceptance Criteria:**
- [ ] Плавная анимация между шагами
- [ ] Haptic feedback на мобильных
- [ ] Анимация прогресс-бара
- [ ] Не ломает функционал
- [ ] Производительность не страдает

---

## Фаза 3: Advanced Features (4-6 недель)

### Задача 3.1: Упрощенный ввод плюсов/минусов
**Priority:** 🔥🔥 MEDIUM  
**Effort:** 🔧🔧 Medium (3 дня)  
**Impact:** Упрощение заполнения

**Описание:**
Заменить rich-text editor на упрощенный ввод списков для плюсов/минусов.

**Требования:**
- Input + кнопка "Добавить" (или Enter)
- Список добавленных пунктов
- Возможность удаления
- Drag-and-drop для сортировки (опционально)
- Сохранение в формате массива

**Файлы для создания:**
```
components/travel/SimplifiedListInput.tsx
```

**Интеграция:**
```tsx
// TravelWizardStepDetails.tsx
<SimplifiedListInput
  label="Плюсы"
  value={formData.plus || []}
  onChange={(items) => handleChange('plus', items)}
  placeholder="Например: Красивые пейзажи"
  icon="plus-circle"
  maxItems={10}
/>
```

**Acceptance Criteria:**
- [ ] Input с кнопкой "Добавить"
- [ ] Enter также добавляет пункт
- [ ] Список с возможностью удаления
- [ ] Валидация (не более 10 пунктов)
- [ ] Сохранение в массив
- [ ] Работает на всех платформах

---

### Задача 3.2: Встроенный кроп изображений
**Priority:** 🔥 LOW  
**Effort:** 🔧🔧🔧 Hard (5 дней)  
**Impact:** Качество медиа

**Описание:**
Добавить возможность кропа изображений перед загрузкой.

**Требования:**
- Интерфейс кропа (перетаскивание, масштабирование)
- Предустановленные соотношения (16:9, 4:3, 1:1)
- Превью результата
- Сохранение кропнутого изображения

**Библиотека:**
- Web: `react-easy-crop`
- Native: `react-native-image-crop-picker`

**Файлы для создания:**
```
components/travel/media/ImageCropper.tsx
hooks/useImageCrop.ts
```

**Acceptance Criteria:**
- [ ] Кроп работает на web и native
- [ ] Предустановленные соотношения
- [ ] Превью результата
- [ ] Сохранение кропнутого изображения
- [ ] Не ломает текущий функционал загрузки

---

### Задача 3.3: Шаблоны маршрутов
**Priority:** 🔥 LOW  
**Effort:** 🔧🔧🔧 Hard (5 дней)  
**Impact:** Ускорение создания

**Описание:**
Добавить готовые шаблоны для популярных типов маршрутов.

**Требования:**
- Библиотека шаблонов (Weekend в Европе, Roadtrip, Хайкинг и т.д.)
- Предзаполнение полей на основе шаблона
- Возможность кастомизации после выбора

**Файлы для создания:**
```
components/travel/wizard/TemplateSelector.tsx
constants/travelTemplates.ts
```

**Acceptance Criteria:**
- [ ] Библиотека из 5-10 шаблонов
- [ ] Превью шаблона перед выбором
- [ ] Предзаполнение полей
- [ ] Возможность изменения после выбора
- [ ] Аналитика использования шаблонов

---

## Аналитика и метрики

### События для отслеживания

```typescript
// Новые события
trackWizardEvent('onboarding_viewed', { slide: number });
trackWizardEvent('onboarding_dismissed', { dont_show_again: boolean });
trackWizardEvent('quick_draft_saved', { travel_id: string });
trackWizardEvent('map_search_used', { query: string, result_selected: boolean });
trackWizardEvent('preview_opened', { step: number });
trackWizardEvent('contextual_tip_shown', { step: number, tip_key: string });
trackWizardEvent('template_selected', { template_id: string });
```

### Метрики для дашборда

```typescript
// Конверсия по шагам
const stepConversion = {
  step_1_to_2: percentage,
  step_2_to_3: percentage,
  // ...
  step_6_submission: percentage,
};

// Время на шагах
const timePerStep = {
  step_1: avgSeconds,
  step_2: avgSeconds,
  // ...
};

// Использование функций
const featureUsage = {
  quick_draft: percentage,
  map_search: percentage,
  preview: percentage,
  templates: percentage,
};
```

---

## Тестирование

### Unit-тесты

```typescript
// Новые тесты
describe('TravelWizardOnboarding', () => {
  it('should show onboarding for first-time users', () => {});
  it('should save "don\'t show again" preference', () => {});
  it('should support slide navigation', () => {});
});

describe('MapSearchBar', () => {
  it('should debounce search requests', () => {});
  it('should display search results', () => {});
  it('should handle place selection', () => {});
});

describe('ContextualTips', () => {
  it('should show correct tip based on context', () => {});
  it('should hide when condition no longer met', () => {});
});
```

### E2E-тесты (Playwright)

```typescript
test('complete travel creation with quick draft', async ({ page }) => {
  await page.goto('/travel/new');
  await page.fill('[name="name"]', 'Test Travel');
  await page.click('text=Быстрый черновик');
  await expect(page).toHaveURL('/metravel');
  await expect(page.locator('text=Черновик сохранен')).toBeVisible();
});

test('use map search to add marker', async ({ page }) => {
  await page.goto('/travel/new');
  await page.click('text=Далее');
  await page.fill('[placeholder*="Поиск места"]', 'Paris');
  await page.click('text=Париж, Франция');
  await expect(page.locator('text=Париж')).toBeVisible();
});
```

---

## Документация

### Обновить файлы:

- [ ] `UPSERT_TRAVEL_DOCUMENTATION.md` — добавить новые функции
- [ ] `UPSERT_TRAVEL_QUICK_REFERENCE.md` — обновить quick reference
- [ ] `README.md` — добавить ссылку на новую документацию
- [ ] Inline-комментарии в коде

---

## Чек-лист перед релизом

### Фаза 1
- [ ] Все задачи выполнены
- [ ] Unit-тесты написаны и прошли
- [ ] E2E-тесты прошли
- [ ] Ревью кода
- [ ] Тестирование на всех платформах (web/iOS/Android)
- [ ] Проверка темной темы
- [ ] Проверка адаптивности
- [ ] Аналитика настроена
- [ ] Документация обновлена

### Фаза 2
- [ ] Все задачи выполнены
- [ ] Unit-тесты написаны и прошли
- [ ] E2E-тесты прошли
- [ ] Ревью кода
- [ ] Тестирование интеграции с Nominatim API
- [ ] Проверка производительности анимаций
- [ ] Аналитика работает корректно
- [ ] Документация обновлена

### Фаза 3
- [ ] Все задачи выполнены
- [ ] Unit-тесты написаны и прошли
- [ ] E2E-тесты прошли
- [ ] Ревью кода
- [ ] Проверка работы кропа изображений
- [ ] Тестирование шаблонов
- [ ] Аналитика работает
- [ ] Документация обновлена

---

## Контакты

**Разработчик:** TBD  
**Ревьюер:** TBD  
**Дата начала:** TBD  
**Дата релиза:** TBD

---

**Версия:** 1.0  
**Дата создания:** 01.01.2026  
**Последнее обновление:** 01.01.2026

