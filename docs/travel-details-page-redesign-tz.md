# ТЗ: переработка страницы «Путешествие» (Travel Details) — UX/UI + Performance + Responsive

## Быстрые прыжки под hero

- Добавлен набор из кнопок быстрого перехода (`Карта`, `Описание`, `Координаты`) сразу под hero-блоком; каждая кнопка повторно использует секцию `sectionLinks`, показывает соответствующую иконку и вызывает обновлённый `scrollToWithMenuClose`, чтобы плавно добраться до раздела без лишних действий по открытию меню.

## Запланированные задачи

- [x] Performance: аналогично карте — лениво подключить/проверить тяжёлые секции `NearTravelList` / `PopularTravelList` (и любые большие списки), чтобы не тянуть лишние запросы/рендер до скролла.
- [x] Reserved space / CLS: убедиться, что у секций ниже первого экрана (карта/рядом/популярное) есть стабильные размеры в loading-состоянии (skeleton/reserved height), без «прыжков».
- [x] Mobile smoke-check:
  - [x] iOS Safari / Android Chrome: sticky-tabs, кнопки, кликабельные зоны.
  - [x] Проверка что cookie-баннер не перекрывает bottom-nav при разных высотах экрана.
- [ ] Метрики/гейты: зафиксировать проверку CLS/LCP/INP для travel details (минимум ручной чеклист + e2e если возможно).
- [ ] Удалить/упростить mobile-ветки логики, которые больше не используются:
  - [ ] Убедиться, что нет «мертвых» стилей/компонентов, которые не отображаются на mobile.
- [ ] Decision summary (короткое резюме 2–4 строки) и/или «ключевые точки» в структурированном виде (если данные есть в API).

## Метрики/гейты (ручной чеклист)

### CLS (Cumulative Layout Shift)

- [ ] Открыть страницу `/travels/:slug` в Chrome (Desktop + Mobile emulation) и прокрутить до секций `Карта`, `Рядом`, `Популярные`, `Экскурсии`.
- [ ] Убедиться, что при появлении карты/списков/виджета **контент ниже не прыгает** (есть reserved height / skeleton).
- [ ] В DevTools → Performance insights / Lighthouse: CLS не деградировал относительно текущей версии.

### LCP (Largest Contentful Paint)

- [ ] Проверить, что hero-картинка/галерея грузится без заметной задержки (нет блокирующих запросов от секций ниже первого экрана).
- [ ] Lighthouse: LCP в зелёной зоне на типовом маршруте (desktop + mobile).

### INP (Interaction to Next Paint)

- [ ] На mobile: клики по `sticky-tabs`, `ShareButtons`, быстрым прыжкам под hero — без залипаний.
- [ ] Скролл страницы плавный (без jank при появлении секций ниже первого экрана).

### E2E (опционально)

- [ ] Playwright: открыть travel details, дождаться hero, проскроллить до `near`/`popular`, проверить что секции появились и страница не падает.
