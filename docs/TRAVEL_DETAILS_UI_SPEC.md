# ТЗ: UI/UX-редизайн страницы путешествия (Travel Details)

> Дата: 6 февраля 2026
> Область: только UI/UX, без изменений бэкенда
> Основа: анализ 15+ компонентов страницы, стилей, документации и QA-отчёта

---

## Содержание

1. [Текущая структура страницы](#1-текущая-структура-страницы)
2. [Критические проблемы](#2-критические-проблемы)
3. [Что добавить](#3-что-добавить)
4. [Что изменить](#4-что-изменить)
5. [Что удалить / убрать](#5-что-удалить--убрать)
6. [Приоритизация](#6-приоритизация)

---

## 1. Текущая структура страницы

### Desktop (≥ 1024px): sidebar + scroll

```
┌──────────────────────────────────────────────────┐
│  [Sticky Sidebar]  │  [Main Content Scroll]      │
│  - Аватар автора   │  1. Hero (Slider/LCP Image) │
│  - Навигация       │  2. QuickFacts              │
│  - Секции          │  3. QuickJump чипы          │
│  - Кнопка «Ред.»   │  4. AuthorCard              │
│                    │  5. Description (collapsible)│
│                    │  6. Video (YouTube)          │
│                    │  7. Recommendation           │
│                    │  8. Plus / Minus             │
│                    │  9. Comments                 │
│                    │ 10. Excursions (web only)    │
│                    │ 11. Map (toggleable)         │
│                    │ 12. PointList (collapsed)    │
│                    │ 13. NearTravelList           │
│                    │ 14. NavigationArrows         │
│                    │ 15. PopularTravelList        │
│                    │ 16. Telegram Discussion      │
│                    │ 17. ShareButtons             │
│                    │ 18. CTA Section              │
│                    │ 19. [ScrollToTop]            │
└──────────────────────────────────────────────────┘
```

### Mobile (< 1024px): single column

```
┌────────────────────────────┐
│ 1. Hero (Slider/LCP Image) │
│ 2. QuickFacts              │
│ 3. WeatherWidget           │
│ 4. QuickJump чипы          │
│ 5. ShareButtons            │  ← до контента!
│ 6. AuthorCard              │  ← до контента!
│ 7. [Sections Sheet trigger]│
│ 8. Description             │
│ 9. Video                   │
│ 10. Insight tabs           │
│ 11. Recommendation         │
│ 12. Plus / Minus           │
│ 13. Comments               │
│ 14. Excursions             │
│ 15. Map                    │
│ 16. PointList              │
│ 17. NearTravelList         │
│ 18. NavigationArrows       │
│ 19. PopularTravelList      │
│ 20. Telegram Discussion    │
│ 21. CTA Section            │
│ 22. [ScrollToTop]          │
└────────────────────────────┘
```

### Ключевые файлы

| Файл | Роль |
|---|---|
| `components/travel/details/TravelDetailsContainer.tsx` | Главный контейнер, layout, SEO, scroll |
| `components/travel/details/TravelDetailsHero.tsx` | Hero-блок: LCP-изображение, slider, QuickFacts, Author, Share |
| `components/travel/details/TravelDetailsDeferred.tsx` | Оркестратор отложенных секций |
| `components/travel/details/sections/TravelDetailsContentSection.tsx` | Описание, видео, рекомендации, плюсы/минусы |
| `components/travel/details/sections/TravelDetailsMapSection.tsx` | Карта, координаты, экскурсии |
| `components/travel/details/sections/TravelDetailsSidebarSection.tsx` | Рядом, популярное, навигация |
| `components/travel/details/sections/TravelDetailsFooterSection.tsx` | Telegram, Share, CTA |
| `components/travel/details/sections/CollapsibleSection.tsx` | Раскрывающаяся секция |
| `components/travel/details/TravelDetailsStyles.ts` | Все стили страницы |
| `components/travel/CompactSideBarTravel.tsx` | Боковое меню (desktop) |
| `components/travel/sectionLinks.ts` | Генерация списка секций |
| `components/travel/CommentsSection.tsx` | Комментарии |
| `components/travel/QuickFacts.tsx` | Быстрые факты |
| `components/travel/AuthorCard.tsx` | Карточка автора |
| `components/travel/ShareButtons.tsx` | Кнопки «Поделиться» |
| `components/travel/CTASection.tsx` | Призыв к действию |
| `components/travel/PointList.tsx` | Список координат |
| `components/travel/NavigationArrows.tsx` | Навигация пред./след. |
| `components/travel/TravelSectionsSheet.tsx` | Мобильный sheet секций |
| `components/travel/TelegramDiscussionSection.tsx` | Обсуждение в Telegram |
| `components/travel/TravelDescription.tsx` | Рендер HTML-описания |

---

## 2. Критические проблемы

### 2.1. Нет видимого заголовка (P0)

**Проблема:** `<h1>` скрыт через `clip: rect(0,0,0,0)` (sr-only). Пользователь видит только фото — **не понимает, что за путешествие** до скролла к описанию.

**Файл:** `TravelDetailsHero.tsx:399-413`

**Решение:** Показать название путешествия поверх hero-изображения (overlay) или сразу под ним. Стиль: крупный белый текст с text-shadow на фоне изображения (как в travel-блогах).

---

### 2.2. ShareButtons и AuthorCard до контента (P0, mobile)

**Проблема:** На мобильном «Поделиться» и «Автор» показываются **до описания**. Пользователь ещё не прочитал контент, но ему уже предлагают поделиться. Это нарушает логику потребления контента.

**Файлы:** `TravelDetailsHero.tsx:554-583`

**Решение:**
- **ShareButtons** — перенести **после описания** (или сделать sticky-bar внизу экрана)
- **AuthorCard** — перенести **после описания**, перед комментариями. Автор — контекст для контента, но не приоритетнее самого контента.

---

### 2.3. CommentsSection не поддерживает тёмную тему (P0)

**Проблема:** `CommentsSection.tsx` использует `DESIGN_TOKENS.colors.*` напрямую (статические значения) вместо `useThemedColors()`. В тёмной теме комментарии будут со светлым фоном на тёмной странице.

**Файл:** `CommentsSection.tsx:470-665` — все стили через `StyleSheet.create` с хардкодом `DESIGN_TOKENS.colors.*`

**Решение:** Мигрировать на `createStyles(colors)` паттерн (как в `AuthorCard`, `QuickFacts`, `ShareButtons`). Использовать `useThemedColors()` внутри компонента.

---

### 2.4. Вложенный ScrollView в комментариях (P1)

**Проблема:** `CommentsSection` оборачивает список комментариев в `<ScrollView>` с `RefreshControl`. Но сам компонент уже находится внутри главного `<ScrollView>` страницы (`TravelDetailsContainer.tsx:397`). Вложенные ScrollView вызывают проблемы со скроллом на мобильных.

**Файл:** `CommentsSection.tsx:369-465`

**Решение:** Заменить внутренний `ScrollView` на обычный `View`. `RefreshControl` убрать — обновление через кнопку «Обновить» (уже есть `handleRefresh`).

---

## 3. Что добавить

### 3.1. Видимый заголовок в hero-блоке (P0)

**Текущее:** Заголовок скрыт (sr-only). Пользователь видит только фото.

**Решение:**
- Показать `travel.name` **поверх hero-изображения** (нижняя часть, gradient overlay)
- Стиль: `fontSize: 28` (mobile) / `32` (desktop), `fontWeight: 800`, белый цвет, `textShadow: '0 2px 8px rgba(0,0,0,0.6)'`
- Под заголовком: компактная строка `countryName · number_days дней · monthName year`
- Скрытый `<h1>` оставить для SEO

**Затрагиваемые файлы:** `TravelDetailsHero.tsx`

---

### 3.2. Breadcrumbs (P1)

**Текущее:** Нет навигации «назад». Пользователь не может вернуться к списку путешествий по стране/городу.

**Решение:**
- Формат: `Главная > Путешествия > {countryName} > {travel.name}`
- Расположение: над hero-блоком
- Только web (на мобильном — скрывать)
- Переиспользовать `useBreadcrumbModel` (уже есть в проекте)

**Затрагиваемые файлы:** `TravelDetailsContainer.tsx`, новый `components/travel/details/TravelBreadcrumbs.tsx`

---

### 3.3. Кнопка «В избранное» в hero-блоке (P1)

**Текущее:** Кнопка «В избранное» только в CTA-секции внизу страницы. Пользователь должен проскроллить всю страницу, чтобы сохранить путешествие.

**Решение:**
- Добавить иконку-сердечко в правый верхний угол hero-изображения (overlay)
- При нажатии — toggle избранного + toast «Добавлено в избранное»
- Использовать существующий `useFavorites` из `FavoritesContext`
- Стиль: полупрозрачный круг 44×44, иконка `heart` / `heart` (filled)

**Затрагиваемые файлы:** `TravelDetailsHero.tsx`

---

### 3.4. Счётчик фотографий в галерее (P2)

**Текущее:** Slider показывает стрелки (desktop) или точки (mobile), но нет индикатора «3 / 12».

**Решение:**
- Добавить badge `currentIndex / totalCount` в правом нижнем углу slider
- Стиль: полупрозрачный фон, белый текст, `fontSize: 13`, `borderRadius: 8`

**Затрагиваемые файлы:** `components/travel/Slider.tsx`

---

### 3.5. Оценка времени чтения (P2)

**Текущее:** Нет индикации длины описания. Пользователь не знает, сколько времени займёт чтение.

**Решение:**
- Рассчитать: `Math.ceil(wordCount / 200)` минут (200 слов/мин для русского текста)
- Показать в `descriptionIntroText`: `5 дней · Беларусь · ~7 мин чтения`
- Считать слова из `travel.description` (strip HTML tags)

**Затрагиваемые файлы:** `TravelDetailsContentSection.tsx`

---

### 3.6. Sticky-bar действий на мобильном (P2)

**Текущее:** На мобильном кнопки действий (избранное, поделиться) разбросаны по странице.

**Решение:**
- Sticky-bar внизу экрана (над BottomDock) с 3 кнопками-иконками:
  - ❤ В избранное
  - ↗ Поделиться (native share или copy link)
  - 💬 К комментариям (scroll to comments)
- Появляется после скролла > 300px (когда hero уходит из viewport)
- Скрывается при скролле вниз, появляется при скролле вверх

**Затрагиваемые файлы:** новый `components/travel/details/TravelStickyActions.tsx`, `TravelDetailsContainer.tsx`

---

### 3.7. Fullscreen-просмотр галереи (P2)

**Текущее:** Нажатие на фото в slider ничего не делает. Нет возможности рассмотреть фото крупнее.

**Решение:**
- По нажатию на фото — открыть fullscreen lightbox
- Свайп влево/вправо для навигации
- Кнопка закрытия (X) в правом верхнем углу
- Счётчик фото
- На web: overlay с `position: fixed`, на native: Modal

**Затрагиваемые файлы:** `TravelDetailsHero.tsx`, новый `components/travel/GalleryLightbox.tsx`

---

### 3.8. Skeleton для комментариев (P2)

**Текущее:** `ActivityIndicator` при загрузке комментариев.

**Решение:** Заменить на skeleton-лоадер (3 блока: аватар + 2 строки текста). Использовать существующий `SkeletonLoader`.

**Затрагиваемые файлы:** `CommentsSection.tsx`

---

## 4. Что изменить

### 4.1. Порядок секций на мобильном (P0)

**Текущий порядок:** Hero → QuickFacts → Weather → QuickJump → **ShareButtons** → **AuthorCard** → Sections Sheet → Description → ...

**Новый порядок:**
```
Hero (с видимым заголовком + кнопка ❤)
QuickFacts
QuickJump чипы
Sections Sheet trigger
Description
Video
Recommendation / Plus / Minus
AuthorCard                          ← перенесён сюда
Comments
Map
PointList
Excursions
NearTravelList
NavigationArrows
PopularTravelList
ShareButtons                        ← перенесён сюда
Telegram Discussion
CTA Section
```

**Логика:** Контент → Автор → Обсуждение → Действия. Пользователь сначала читает, потом видит кто написал, потом решает что делать.

**Затрагиваемые файлы:** `TravelDetailsHero.tsx` (убрать ShareButtons и AuthorCard из hero), `TravelDetailsDeferred.tsx` (добавить AuthorCard перед комментариями)

---

### 4.2. QuickJump чипы — расширить набор (P1)

**Текущее:** Только `map`, `description`, `points` (захардкожено в `HERO_QUICK_JUMP_KEYS`).

**Решение:**
- Показывать все доступные секции (из `sectionLinks`), но ограничить 5-6 чипами
- Приоритет: `description`, `map`, `points`, `comments`, `video`, `recommendation`
- На мобильном: горизонтальный скролл (уже реализован)

**Затрагиваемые файлы:** `TravelDetailsHero.tsx:35` — изменить `HERO_QUICK_JUMP_KEYS`

---

### 4.3. WeatherWidget — унифицировать поведение (P2)

**Текущее:** На мобильном показывается автоматически, на web — кнопка «Показать погоду». Виджет показывается только на мобильном (`isMobile && travel.travelAddress && extrasReady`).

**Решение:**
- Показывать на всех устройствах
- Всегда через кнопку «Показать погоду» (lazy load)
- Перенести в секцию карты (рядом с географическими данными)

**Затрагиваемые файлы:** `TravelDetailsHero.tsx:480-513`, `TravelDetailsMapSection.tsx`

---

### 4.4. CollapsibleSection — улучшить индикацию (P2)

**Текущее:** Иконка `expand-less` / `expand-more` — не самая очевидная. Badge «Опыт автора» мелкий.

**Решение:**
- Заменить иконки на `chevron-up` / `chevron-down` (Feather, более узнаваемые)
- Добавить плавную анимацию раскрытия (высота: 0 → auto, через `LayoutAnimation` или `Animated`)
- Badge сделать крупнее и контрастнее

**Затрагиваемые файлы:** `CollapsibleSection.tsx`

---

### 4.5. Mobile Insight Tabs — переименовать и переосмыслить (P1)

**Текущее:** Блок «Быстрый доступ к разделам» с чипами «Советы», «Понравилось», «Не зашло». Название вводит в заблуждение — это не навигация по разделам, а переключатель между recommendation/plus/minus.

**Решение:**
- Переименовать: «Впечатления автора» вместо «Быстрый доступ к разделам»
- Визуально: сделать как tab-bar (подчёркивание активного, а не pill-стиль)
- Показывать количество символов в каждом разделе как индикатор объёма

**Затрагиваемые файлы:** `TravelDetailsContentSection.tsx:233-263`, `TravelDetailsStyles.ts`

---

### 4.6. TelegramDiscussionSection — скрывать без URL (P1)

**Текущее:** Если `EXPO_PUBLIC_TELEGRAM_DISCUSSION_URL` не задан, показывается disabled кнопка + подсказка для администратора. Обычный пользователь видит бесполезный блок.

**Решение:** Если `baseUrl` пустой — не рендерить компонент вообще (`return null`).

**Затрагиваемые файлы:** `TelegramDiscussionSection.tsx:40-71`

---

### 4.7. CTASection — упростить (P1)

**Текущее:** 3 кнопки для авторизованного пользователя: «В избранное», «Все путешествия автора», «Создать путешествие». Слишком много CTA — размывают фокус.

**Решение:**
- Оставить **одну primary CTA**: «В избранное» (или «Создать путешествие» если уже в избранном)
- «Все путешествия автора» — перенести в AuthorCard (там уже есть эта кнопка)
- Для неавторизованных: оставить как есть (призыв к регистрации)

**Затрагиваемые файлы:** `CTASection.tsx`

---

### 4.8. Типографика — убрать смешение шрифтов (P2)

**Текущее:** `fontFamily: 'Georgia'` используется в:
- `AuthorCard` (authorName, viewButtonInlineText, viewButtonBottomText)
- `ShareButtons` (title)
- `NavigationArrows` (navTitle)
- `CTASection` (title)
- `TravelDescription` (placeholder)

Остальные компоненты используют системный шрифт. Это создаёт визуальную несогласованность.

**Решение:**
- **Вариант A:** Убрать `fontFamily: 'Georgia'` из всех компонентов, использовать только системный шрифт
- **Вариант B:** Использовать Georgia только для заголовков секций (`sectionHeaderText`) — единообразно

**Затрагиваемые файлы:** `AuthorCard.tsx`, `ShareButtons.tsx`, `NavigationArrows.tsx`, `CTASection.tsx`, `TravelDescription.tsx`

---

### 4.9. PointList — улучшить UX раскрытия (P2)

**Текущее:** Длинный текст кнопки «Показать координаты мест (12)». Требует клика для раскрытия.

**Решение:**
- Показать **превью первых 2-3 точек** без раскрытия (миниатюры + названия)
- Кнопка «Показать все 12 точек» для полного списка
- Убрать `FlashList` для ≤6 точек — использовать обычный `View` (FlashList overhead для малого количества)

**Затрагиваемые файлы:** `PointList.tsx`

---

### 4.10. NavigationArrows — показывать чаще (P2)

**Текущее:** Показывается только если текущее путешествие найдено в `relatedTravels`. Часто не показывается.

**Решение:**
- Если текущее путешествие не в списке — показывать первые 2 из `relatedTravels` как «Похожие маршруты»
- Изменить label: «Предыдущее» / «Следующее» → «Похожий маршрут» если нет точного совпадения

**Затрагиваемые файлы:** `NavigationArrows.tsx`

---

### 4.11. Описание — убрать декоративный штамп (P1)

**Текущее:** `TravelDescription.tsx` рендерит декоративное изображение `travel-stamp.webp` с `opacity: 0.15` поверх контента. Это:
- Добавляет визуальный шум
- Загружает лишнее изображение
- Перекрывает контент (хоть и с `pointerEvents: 'none'`)

**Решение:** Удалить штамп. Описание должно быть чистым текстом без декоративных элементов.

**Затрагиваемые файлы:** `TravelDescription.tsx:321-332`

---

### 4.12. Описание — убрать дублирующую кнопку «Назад к началу» (P2)

**Текущее:** Внутри описания есть `Pressable` «Назад к началу страницы». Одновременно существует глобальный `ScrollToTopButton` на странице.

**Решение:** Убрать кнопку из описания. Глобальный `ScrollToTopButton` достаточен.

**Затрагиваемые файлы:** `TravelDetailsContentSection.tsx:201-209`

---

### 4.13. Spacing — вернуть воздух (P1)

**Текущее:** Недавний редизайн уменьшил все отступы на 15-25% (`COMPACT_SPACING`, `COMPACT_TYPOGRAPHY`). Страница выглядит «сжатой» — секции слипаются, текст тесный.

**Решение:**
- `sectionContainer.marginBottom`: увеличить с 18/22px до **24/28px**
- `descriptionContainer.padding`: увеличить с 14/18px до **18/24px**
- `sectionHeaderText.fontSize`: увеличить с 20/24px до **22/26px**
- Между hero и QuickFacts: добавить `marginTop: 16`

**Затрагиваемые файлы:** `TravelDetailsStyles.ts` — `COMPACT_SPACING`, `COMPACT_TYPOGRAPHY`, `sectionContainer`, `descriptionContainer`

---

## 5. Что удалить / убрать

### 5.1. Дублирование ShareButtons (P1)

**Текущее:** ShareButtons рендерится дважды:
- В hero-блоке (mobile only) — `TravelDetailsHero.tsx:554-567`
- В footer (desktop only) — `TravelDetailsFooterSection.tsx:27-35`

**Решение:** Один экземпляр ShareButtons — после описания/комментариев, перед CTA. Одинаковый на mobile и desktop.

---

### 5.2. Декоративный штамп в описании (P1)

Описано в 4.11. Удалить `ImageCardMedia` с `travel-stamp.webp` из `TravelDescription.tsx`.

---

### 5.3. Кнопка «Назад к началу» в описании (P2)

Описано в 4.12. Удалить `backToTopWrapper` / `backToTopText` из `TravelDetailsContentSection.tsx` и `TravelDetailsStyles.ts`.

---

### 5.4. Подсказка администратору в TelegramDiscussionSection (P1)

**Текущее:** Текст «Администратору: задайте EXPO_PUBLIC_TELEGRAM_DISCUSSION_URL...» виден всем пользователям.

**Решение:** Показывать только в `__DEV__` режиме. В production — `return null` если нет URL.

---

### 5.5. Inline стили (P3)

Множество inline-стилей в `TravelDetailsContainer.tsx` (строки 236-269, 280-300) для состояний ошибки и загрузки. Вынести в `TravelDetailsStyles.ts` (частично уже есть `errorContainer`, `errorTitle` и т.д.).

---

## 6. Приоритизация

### P0 — Блокеры UX (влияют на первое впечатление)

| # | Задача | Сложность | Файлы |
|---|---|---|---|
| 2.1 | Видимый заголовок в hero | Низкая | `TravelDetailsHero.tsx` |
| 2.2 | Перенести Share/Author после контента (mobile) | Средняя | `TravelDetailsHero.tsx`, `TravelDetailsDeferred.tsx` |
| 2.3 | Тёмная тема в CommentsSection | Средняя | `CommentsSection.tsx` |
| 4.1 | Новый порядок секций (mobile) | Средняя | `TravelDetailsHero.tsx`, `TravelDetailsDeferred.tsx` |

### P1 — Важно (улучшает навигацию и читаемость)

| # | Задача | Сложность | Файлы |
|---|---|---|---|
| 2.4 | Убрать вложенный ScrollView в комментариях | Низкая | `CommentsSection.tsx` |
| 3.2 | Breadcrumbs | Низкая | `TravelDetailsContainer.tsx`, новый файл |
| 3.3 | Кнопка «В избранное» в hero | Низкая | `TravelDetailsHero.tsx` |
| 4.2 | Расширить QuickJump чипы | Низкая | `TravelDetailsHero.tsx` |
| 4.5 | Переименовать Mobile Insight Tabs | Низкая | `TravelDetailsContentSection.tsx` |
| 4.6 | Скрыть Telegram без URL | Низкая | `TelegramDiscussionSection.tsx` |
| 4.7 | Упростить CTASection | Низкая | `CTASection.tsx` |
| 4.11 | Убрать штамп из описания | Низкая | `TravelDescription.tsx` |
| 4.13 | Вернуть воздух в spacing | Низкая | `TravelDetailsStyles.ts` |
| 5.1 | Убрать дублирование ShareButtons | Низкая | `TravelDetailsHero.tsx`, `TravelDetailsFooterSection.tsx` |
| 5.4 | Скрыть admin-подсказку в Telegram | Низкая | `TelegramDiscussionSection.tsx` |

### P2 — Улучшения (полировка UX)

| # | Задача | Сложность | Файлы |
|---|---|---|---|
| 3.4 | Счётчик фото в галерее | Низкая | `Slider.tsx` |
| 3.5 | Оценка времени чтения | Низкая | `TravelDetailsContentSection.tsx` |
| 3.6 | Sticky-bar действий (mobile) | Средняя | Новый файл, `TravelDetailsContainer.tsx` |
| 3.7 | Fullscreen галерея | Высокая | Новый файл, `TravelDetailsHero.tsx` |
| 3.8 | Skeleton для комментариев | Низкая | `CommentsSection.tsx` |
| 4.3 | WeatherWidget — унифицировать | Средняя | `TravelDetailsHero.tsx`, `TravelDetailsMapSection.tsx` |
| 4.4 | CollapsibleSection — анимация | Средняя | `CollapsibleSection.tsx` |
| 4.8 | Убрать смешение шрифтов | Низкая | 5 файлов |
| 4.9 | PointList — превью точек | Средняя | `PointList.tsx` |
| 4.10 | NavigationArrows — показывать чаще | Низкая | `NavigationArrows.tsx` |
| 4.12 | Убрать «Назад к началу» из описания | Низкая | `TravelDetailsContentSection.tsx` |
| 5.5 | Вынести inline-стили | Низкая | `TravelDetailsContainer.tsx`, `TravelDetailsStyles.ts` |

---

## Ограничения

- **Бэкенд не меняется** — все данные из существующих API
- **Дизайн-система** — все изменения через `DESIGN_TOKENS` и `useThemedColors()`
- **Иконки** — только `@expo/vector-icons/Feather` (согласно `docs/RULES.md`)
- **Изображения** — нейтральные плейсхолдеры без emoji и текста
- **Тестирование** — каждое изменение: `yarn lint` + `yarn test:run`
- **Компоненты** — переиспользовать `components/ui/*` (согласно `docs/RULES.md`)

---

*Документ составлен на основе анализа 15+ компонентов страницы путешествия, стилей, навигации и 13 файлов документации.*
