# 🚀 Быстрый старт: Использование новых стилей бокового меню

## 📋 Что изменилось (v3.1.0)

### Новые функции:
```
✅ FAB кнопка для открытия меню на мобильном
✅ Компактные размеры для отображения без скролла
✅ Тесты для веб-версии (100+ тестов)
```

### Файлы:
```
✅ docs/DESIGN_SYSTEM_RULES.md - Правила дизайн-системы
✅ components/travel/CompactSideBarTravel.tsx - Обновленный компонент
✅ components/travel/CompactSideBarTravel.web.css - Новые CSS стили
✅ hooks/useTravelDetailsMenu.ts - Хук с toggleMenu
✅ hooks/useMenuState.ts - Хук с openMenu/closeMenu
✅ __tests__/components/CompactSideBarTravel.web.test.tsx - Тесты
```

---

## 🆕 FAB кнопка для мобильного меню

### Как работает:
```typescript
// Кнопка автоматически показывается на мобильном
{isMobile && !isMenuOpen && (
  <FloatingActionButton
    label="Меню"
    icon="☰"
    onPress={openMenu}
    position="bottom-left"
    testID="mobile-menu-fab"
  />
)}
```

### Функциональность:
- ✅ Показывается только на мобильном
- ✅ Скрывается когда меню открыто
- ✅ Открывает боковое меню при клике
- ✅ Расположена внизу слева (bottom: 80px, left: 16px)
- ✅ z-index: 999 для отображения поверх контента

---

## 📏 Компактные размеры (без скролла)

### Что уменьшено:

#### Карточка автора:
```
padding: 18px → 14px      (-22%)
borderRadius: 16px → 14px (-12%)
marginBottom: 16px → 12px (-25%)
```

#### Аватарка:
```
size: 60px → 52px         (-13%)
borderWidth: 2.5px → 2px  (-20%)
```

#### Пункты меню:
```
paddingVertical: 12px → 9px  (-25%)
paddingHorizontal: 14px → 12px (-14%)
borderRadius: 12px → 10px   (-17%)
marginBottom: 4px → 3px     (-25%)
```

#### Текст и иконки:
```
fontSize: 15px → 14px      (-7%)
iconSize: 18px → 16px      (-11%)
lineHeight: 22px → 20px    (-9%)
```

#### Информационная секция:
```
marginTop: 14px → 10px     (-29%)
paddingTop: 14px → 10px    (-29%)
marginBottom: 8px → 6px    (-25%)
fontSize: 14px → 13px      (-7%)
```

### Результат:
- ✅ Меню умещается в высоту < 900px
- ✅ Не требует скролла на большинстве экранов
- ✅ Все элементы видны сразу
- ✅ Сохранена читаемость

---

## 🎨 Как использовать цветовую палитру

### ✅ Правильно:
```typescript
import { DESIGN_TOKENS } from '@/constants/designSystem';

// Используйте только цвета из DESIGN_TOKENS
backgroundColor: DESIGN_TOKENS.colors.primary
color: DESIGN_TOKENS.colors.text
borderColor: DESIGN_TOKENS.colors.borderLight
```

### ❌ Неправильно:
```typescript
// Не используйте хардкод цветов
backgroundColor: '#7a9d8f'  // ❌
color: '#3a3a3a'  // ❌
```

---

## 🎨 Основные цвета

### Светлая тема:
```css
--color-primary: #7a9d8f          /* Акценты, кнопки */
--color-primaryLight: #f0f5f3     /* Фоны */
--color-text: #3a3a3a             /* Основной текст */
--color-textMuted: #6a6a6a        /* Второстепенный текст */
--color-border: rgba(58, 58, 58, 0.06)  /* Границы */
```

### Темная тема:
```css
--color-primary: #8fb5a5          /* Акценты */
--color-text: #e8e8e8             /* Текст */
--color-textMuted: #b8b8b8        /* Второстепенный текст */
--color-background: #1a1a1a       /* Фон */
```

---

## 💻 Добавление data-атрибутов (для новых компонентов)

```typescript
// Только для web-платформы
<View 
  style={styles.card}
  {...(Platform.OS === 'web' ? { 'data-sidebar-card': true } : {})}
>
```

---

## 🎨 CSS стили (для новых компонентов)

### Hover эффекты:
```css
[data-my-element]:hover {
  background-color: var(--color-primarySoft);
  transform: translateY(-2px);
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}
```

### Темная тема:
```css
html[data-theme="dark"] [data-my-element] {
  background-color: rgba(96, 165, 250, 0.1);
}
```

---

## 📖 Полная документация

Читайте файлы в папке `docs/`:
- **DESIGN_SYSTEM_RULES.md** - Полное руководство
- **SIDEBAR_REDESIGN.md** - Описание изменений

---

**Дата**: 31 декабря 2025  
✅ Готово к использованию!
