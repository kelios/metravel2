# Архитектурный обзор и рекомендации по рефакторингу

> Дата: 6 февраля 2026
> Проект: metravel2 (Expo 54 / React Native 0.81.5 / React 19)

---

## 1. Общая оценка

| Аспект | Оценка | Комментарий |
|---|---|---|
| Стек | ✅ Современный | Expo 54, RN 0.81, React 19, TanStack Query 5, Zustand 5 |
| Тестирование | ✅ Хорошо | Jest + Playwright, 50+ e2e спеков, покрытие P1 сценариев |
| CI/Release | ✅ Есть | `release:check`, EAS builds, Lighthouse |
| Документация | ⚠️ Средне | 11 файлов в `docs/`, но есть устаревшие и дублирующие |
| Структура кода | ❌ Требует рефакторинга | Дублирование `src/` vs корень, размытые границы модулей |
| Дизайн-система | ⚠️ Миграция не завершена | Легаси файлы остаются, хотя импорты почти убраны |
| Зависимости | ⚠️ Избыточные | Неиспользуемые и дублирующие пакеты |

---

## 2. КРИТИЧЕСКИЕ проблемы (переделать)

### 2.1. Дублирование структуры: `src/` vs корень

**Проблема:** Проект имеет параллельные структуры на двух уровнях:

| Назначение | Корень | `src/` |
|---|---|---|
| Хуки | `hooks/` (50 файлов) | `src/hooks/` (5 файлов) |
| Утилиты | `utils/` (30 файлов) | `src/utils/` (42 файла) |
| Типы | `types/` (5 файлов) | `src/types/` (9 файлов) |
| API | — | `src/api/` (14 файлов) |
| Сервисы | — | `src/services/` (41 файл) |
| Экраны | — | `src/screens/` |
| UI | `components/ui/` | `src/ui/` (2 файла) |

**Рекомендация:** Консолидировать в **одну** структуру. Два варианта:

- **Вариант A (рекомендуемый):** Всё в корне — `hooks/`, `utils/`, `types/`, `api/`, `services/`. Удалить `src/` как промежуточный слой. Это соответствует Expo Router конвенции, где `app/` и `components/` уже в корне.
- **Вариант B:** Всё в `src/` — перенести `hooks/`, `utils/`, `types/`, `components/`, `stores/`, `context/` внутрь `src/`. Обновить алиас `@/` → `./src/`.

**Приоритет:** 🔴 Высокий — усложняет навигацию, создаёт путаницу при импортах.

### 2.2. Контексты разбросаны по двум папкам

**Проблема:**
- `context/` — `AuthContext`, `FavoritesContext`, `ThemeContext`
- `contexts/` — `FiltersContext`
- `providers/` — `FiltersProvider`

Три папки для одного паттерна.

**Рекомендация:** Объединить в одну папку `context/` (или `providers/`). Перенести `FiltersContext` + `FiltersProvider` туда же.

**Приоритет:** 🔴 Высокий

### 2.3. FavoritesContext — монолитный контекст (604 строки)

**Проблема:** `context/FavoritesContext.tsx` — 604 строк, совмещает:
- Избранное (CRUD + синхронизация с сервером)
- Историю просмотров
- Рекомендации
- Кеширование в AsyncStorage
- Очистку невалидных записей

**Рекомендация:**
1. Разделить на 3 отдельных хука/стора: `useFavorites`, `useViewHistory`, `useRecommendations`
2. Перевести на **Zustand** (уже в проекте) вместо Context — избавит от лишних ре-рендеров
3. Серверную синхронизацию вынести в **TanStack Query** мутации

**Приоритет:** 🔴 Высокий — влияет на производительность всего приложения

### 2.4. Babel alias конфликтует с tsconfig paths

**Проблема:** В `babel.config.js` определены алиасы:
```js
'@/components': './src/components',
'@/utils': './src/utils',
'@/hooks': './src/hooks',
```
Но в `tsconfig.json` алиас `@/*` → `*` (корень). Это значит:
- TypeScript считает `@/hooks` → `./hooks/` (корень)
- Babel в runtime считает `@/hooks` → `./src/hooks/`

Это **скрытый источник багов** при несовпадении файлов.

**Рекомендация:** Привести алиасы в соответствие. После консолидации структуры (п. 2.1) убрать переопределения из babel и оставить единый `@/ → ./`.

**Приоритет:** 🔴 Высокий

---

## 3. ВАЖНЫЕ проблемы (заменить/улучшить)

### 3.1. Избыточные зависимости

| Пакет | Статус | Рекомендация |
|---|---|---|
| `deprecated-react-native-prop-types` | Устаревший | Удалить — не импортируется в коде проекта |
| `react-native-multiple-select` | Не используется | Удалить — 0 импортов в коде |
| `react-native-responsive-screen` | 1 импорт | Заменить на `useResponsive` хук (уже есть в проекте) |
| `react-native-paper` | Минимальное использование | Используется только в `ThemedPaperProvider.native.tsx`. Заменить на собственные `components/ui/` |
| `lucide-react-native` | Запрещён в docs | Docs говорят использовать `@expo/vector-icons/Feather`. Осталось 1 использование + mock. Удалить |
| `html2canvas` | Не импортируется | Удалить — заменён на `pdf-lib` |
| `sharp-cli` | Не импортируется | Удалить — CLI-утилита не нужна в runtime |
| `formik` | 3 файла | Рассмотреть замену на React Hook Form (легче, лучше перформанс) или собственные хуки (уже есть `useOptimizedFormState`) |
| `react-native-flash-message` | Легаси | Заменить на `react-native-toast-message` (уже в проекте) — унифицировать |
| `react-native-image-picker` | Дублирует | `expo-image-picker` уже в проекте — убрать дубль |
| `react-native-document-picker` | Дублирует | `expo-document-picker` уже в проекте — убрать дубль |
| `json5` | Утилитарный | Проверить использование — возможно заменить на `JSON.parse` |
| `ms` | Микро-пакет | Заменить на inline-функцию (3 строки кода) |

**Приоритет:** 🟡 Средний — уменьшит бандл, упростит поддержку

### 3.2. Peer dependency конфликты

Документировано в `docs/TESTING.md`:
- `react-leaflet@5.0.0` — peer dep на React 18, проект на React 19
- `@react-pdf/renderer@3.x` — peer dep на React 18

**Рекомендация:**
- `react-leaflet` — следить за выходом v5.x с поддержкой React 19
- `@react-pdf/renderer` — проект уже использует `pdf-lib` параллельно. Рассмотреть полный переход на `pdf-lib` и удаление `@react-pdf/renderer`

**Приоритет:** 🟡 Средний

### 3.3. Дизайн-система: легаси файлы

Миграция на `DESIGN_TOKENS` почти завершена, но легаси файлы остаются:

| Файл | Статус | Действие |
|---|---|---|
| `constants/Colors.ts` | 0 импортов в prod-коде | Удалить |
| `constants/designTokens.ts` | 1 импорт (тест) | Удалить, обновить тест |
| `constants/airyColors.ts` | 1 импорт (`src/theme.ts`) | Мигрировать `src/theme.ts`, удалить |
| `constants/lightModernDesignTokens.ts` | 0 импортов | Удалить |
| `styles/modernRedesign.ts` | 0 импортов | Удалить |
| `styles/criticalCSS.ts` | Проверить | Если используется только в `+html.tsx` — ок, иначе мигрировать |

**Приоритет:** 🟡 Средний — завершить начатую миграцию

### 3.4. Хуки: раздутые файлы

| Файл | Строк | Проблема |
|---|---|---|
| `hooks/useTravelFormData.ts` | ~900+ | Монолитный хук формы — разбить на под-хуки |
| `hooks/useQuestsApi.ts` | ~600+ | API + адаптеры + типы в одном файле |
| `hooks/useComments.ts` | ~450+ | CRUD + оптимистичные обновления — вынести мутации |
| `hooks/useScrollNavigation.ts` | ~400+ | Сложная логика скролла — разбить |
| `hooks/useTravelWizard.ts` | ~400+ | Wizard state machine — вынести в Zustand store |

**Рекомендация:** Применить паттерн «1 хук = 1 ответственность». Крупные хуки разбить на:
- Хук состояния (Zustand)
- Хук данных (TanStack Query)
- Хук UI-логики

**Приоритет:** 🟡 Средний

### 3.5. API-слой: `src/api/client.ts` — 790 строк

**Проблема:** Монолитный API-клиент содержит:
- HTTP-методы (GET/POST/PUT/PATCH/DELETE)
- Token refresh с race condition protection
- Timeout handling
- Download/upload
- Error handling
- URL normalization

**Рекомендация:**
1. Разбить на модули: `httpClient.ts`, `tokenManager.ts`, `apiError.ts`
2. Рассмотреть переход на **ky** или **ofetch** (легковесные fetch-обёртки с interceptors)
3. Token refresh вынести в отдельный interceptor

**Приоритет:** 🟡 Средний

---

## 4. РЕКОМЕНДАЦИИ по улучшению (nice-to-have)

### 4.1. State management: унификация

Сейчас используются **3 подхода** параллельно:
1. **React Context** — Auth, Favorites, Theme, Filters
2. **Zustand** — mapPanelStore, routeStore, travelSectionsStore
3. **TanStack Query** — серверные данные

**Рекомендация:**
- **Серверное состояние** → TanStack Query (уже так)
- **Глобальное клиентское** → Zustand (перевести Auth, Favorites, Filters)
- **Тема** → оставить Context (мало данных, редко меняется)

Это уменьшит ре-рендеры и упростит тестирование.

**Приоритет:** 🟢 Низкий (постепенно)

### 4.2. Компоненты: `components/MapPage/` — 73 элемента

Самая большая папка компонентов. Содержит всё от UI-примитивов до бизнес-логики карты.

**Рекомендация:** Разбить на подпапки:
```
components/MapPage/
├── Map/          (уже есть)
├── filters/      (фильтры карты)
├── panels/       (боковые панели)
├── controls/     (кнопки управления)
├── markers/      (маркеры и кластеры)
└── shared/       (общие утилиты)
```

**Приоритет:** 🟢 Низкий

### 4.3. `app/+html.tsx` — 29 КБ

**Проблема:** Файл HTML-шаблона содержит inline critical CSS, скрипты аналитики, meta-теги — всё в одном файле.

**Рекомендация:** Вынести:
- Critical CSS → отдельный модуль (частично сделано в `styles/criticalCSS.ts`)
- Analytics scripts → `components/seo/` или `src/utils/analytics.ts`
- Meta-теги → генерировать через `InstantSEO`

**Приоритет:** 🟢 Низкий

### 4.4. `app/_layout.tsx` — 20 КБ

**Проблема:** Корневой layout содержит слишком много логики: провайдеры, шрифты, splash screen, navigation, consent, toast.

**Рекомендация:** Вынести провайдеры в `components/layout/AppProviders.tsx`, оставить в layout только структуру навигации.

**Приоритет:** 🟢 Низкий

### 4.5. Metro config: CORS proxy в dev-сервере

**Проблема:** `metro.config.js` содержит полноценный HTTP-прокси (50+ строк) для API-запросов. Это не задача Metro.

**Рекомендация:** Вынести прокси в отдельный dev-сервер или использовать `scripts/serve-web-build.js` (уже есть) для всех случаев.

**Приоритет:** 🟢 Низкий

### 4.6. Тесты: `__tests__/components/` — 71+ файл в плоской структуре

**Рекомендация:** Зеркалить структуру `components/` внутри `__tests__/components/`:
```
__tests__/components/
├── MapPage/
├── travel/
├── layout/
├── ui/
└── ...
```

**Приоритет:** 🟢 Низкий

---

## 5. Что НЕ трогать (работает хорошо)

| Аспект | Почему оставить |
|---|---|
| **TanStack Query** для серверных данных | Правильный паттерн, хорошо интегрирован |
| **Zustand** для UI-состояния карты/маршрутов | Лёгкий, без boilerplate |
| **Expo Router** (file-based routing) | Стандарт Expo, хорошо работает |
| **`components/ui/`** библиотека | Хороший набор переиспользуемых компонентов |
| **E2E тесты** (Playwright) | Отличное покрытие критических путей |
| **SEO-утилиты** (`InstantSEO`, `buildCanonicalUrl`) | Централизованный подход |
| **Дизайн-токены** (`DESIGN_TOKENS`) | Правильная архитектура, почти завершена миграция |
| **Безопасное хранилище** (`secureStorage`) | Правильный подход к токенам |
| **Image optimization** (`imageOptimization.ts`) | Работает, покрывает все случаи |

---

## 6. План действий (приоритизированный)

### Фаза 1: Чистка ✅ (выполнено 6 фев 2026)
1. ✅ Удалены 6 неиспользуемых зависимостей: `deprecated-react-native-prop-types`, `react-native-multiple-select`, `react-native-flash-message`, `sharp-cli`, `ms`, `json5`
2. ✅ Удалены 7 легаси файлов дизайн-системы: `Colors.ts`, `designTokens.ts`, `airyColors.ts`, `lightModernDesignTokens.ts`, `modernRedesign.ts`, `src/theme.ts`, тест
3. ✅ Объединены `context/` + `contexts/` + `providers/` → единая папка `context/` (5 файлов). Обновлены все 11 импортов + 3 jest.mock пути

### Фаза 2: Консолидация структуры ✅ (выполнено 6 фев 2026)
4. ✅ Удалена папка `src/` — все модули перенесены в корень: `api/`, `services/`, `infrastructure/`, `ui/`, `screens/`, `generators/`, `queryClient.ts`, `queryKeys.ts`. Объединены `hooks/`, `utils/`, `types/`, `config/`. Обновлены 331 импорт + 10 относительных путей.
5. ✅ Исправлены babel алиасы (убраны stale `./src/` overrides) и tsconfig `typeRoots`. Единый алиас `@/ → ./`.
6. Реструктурировать `components/MapPage/` (п. 4.2) — отложено на Фазу 4

### Фаза 3: Рефакторинг крупных модулей (1-2 недели)
7. Разбить `FavoritesContext` на Zustand-сторы (п. 2.3)
8. Разбить крупные хуки (п. 3.4)
9. Модуляризировать API-клиент (п. 3.5)

### Фаза 4: Оптимизация (ongoing)
10. Постепенный перевод Context → Zustand (п. 4.1)
11. Замена `formik` → собственные хуки или React Hook Form (п. 3.1)
12. Вынос логики из `_layout.tsx` и `+html.tsx` (п. 4.3, 4.4)

---

## 7. Метрики для отслеживания

| Метрика | Текущее | Цель |
|---|---|---|
| Зависимостей в `dependencies` | 65 | < 50 |
| Файлов в корне `hooks/` + `src/hooks/` | 55 | Одна папка |
| Файлов в корне `utils/` + `src/utils/` | 72 | Одна папка |
| Легаси дизайн-импортов | ~3 | 0 |
| Максимальный размер хука (строк) | ~900 | < 300 |
| Контекст-провайдеров | 4 (в 3 папках) | ≤ 2 Context + Zustand |

---

*Документ создан на основе анализа кодовой базы, `docs/`, `package.json`, конфигурационных файлов и паттернов импортов.*
