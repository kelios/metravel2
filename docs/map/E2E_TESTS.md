# 🧪 E2E тесты для страницы карты

**Дата:** 2 января 2026  
**Файл:** `e2e/map-page.spec.ts`  
**Статус:** ✅ Реализовано

---

## 📋 Обзор

Комплексные E2E тесты для проверки основных сценариев работы страницы карты `/map`.

### Покрытие функциональности

| Категория | Тестов | Описание |
|-----------|--------|----------|
| Десктоп сценарии | 7 | Основная функциональность на десктопе |
| Мобильные сценарии | 3 | Специфика мобильных устройств |
| Построение маршрута | 2 | Работа с маршрутами |
| Сохранение состояния | 1 | localStorage персистентность |
| Соответствие правилам | 2 | Проверка RULES.md |
| **Итого** | **15** | **Полное покрытие** |

---

## 🎯 Тестируемые сценарии

### 1. Десктоп - основные сценарии

#### ✅ Загрузка страницы
- Карта отображается
- Боковое меню открыто по умолчанию
- Переключатель режимов виден
- Все основные элементы на месте

#### ✅ Фильтрация по категориям
- Секция категорий видна
- Можно разворачивать/сворачивать
- Выбор категории работает
- Количество мест обновляется

#### ✅ Изменение радиуса
- Секция радиуса видна
- Кнопки выбора радиуса работают
- Значение обновляется в header
- Данные на карте обновляются

#### ✅ Переключение режимов
- Режим "Радиус" → "Маршрут"
- Появляются поля старт/финиш
- Видна секция транспорта
- UI адаптируется под режим

#### ✅ QuickActions
- Кнопки видны при прокрутке
- "Показать все" работает
- "Сбросить" работает
- Правильное позиционирование

#### ✅ Collapsible секции
- Можно сворачивать/разворачивать
- Иконка меняется (expand-more/less)
- Анимация плавная
- Состояние сохраняется

#### ✅ Прокрутка до конца
- ScrollView работает корректно
- Footer кнопки видны
- Нет обрезанных элементов
- paddingBottom достаточный

---

### 2. Мобильные - основные сценарии

#### ✅ Кнопка меню
- Видна при загрузке (панель закрыта)
- Открывает боковое меню
- Закрывает боковое меню
- Всегда доступна

**Тест:**
```typescript
// На мобильном панель закрыта по умолчанию
await expect(menuButton).toBeVisible();

// Открываем панель
await menuButton.click();
await expect(sidePanel).toBeVisible();

// Закрываем панель
await closeButton.click();
await expect(menuButton).toBeVisible(); // Кнопка снова видна
```

#### ✅ Свайп для закрытия
- Свайп вправо закрывает панель
- Плавная анимация
- Кнопка меню появляется

#### ✅ Фильтры на мобильном
- Все фильтры доступны
- Работают так же как на десктопе
- UI адаптирован под touch

---

### 3. Построение маршрута

#### ✅ Выбор точек на карте
- Клик на карту → точка старта
- Прогресс "Маршрут 1/2"
- Второй клик → точка финиша
- Прогресс "Маршрут 2/2"
- Появляется кнопка swap

**Тест:**
```typescript
// Кликаем на карту для старта
await page.mouse.click(startX, startY);
const progress = await page.locator('text=/1\\/2/');
expect(progress).toBeVisible();

// Кликаем для финиша
await page.mouse.click(endX, endY);
const progress2 = await page.locator('text=/2\\/2/');
expect(progress2).toBeVisible();
```

#### ✅ Выбор транспорта
- Кнопки видны (Авто, Пешком, Велосипед)
- Можно переключать
- Визуальная индикация выбора
- Влияет на построение маршрута

---

### 4. Сохранение состояния

#### ✅ localStorage персистентность
- Фильтры сохраняются при изменении
- После перезагрузки восстанавливаются
- Валидация данных
- Fallback на defaults

**Тест:**
```typescript
// Изменяем радиус
await radius200.click();

// Проверяем localStorage
const saved = await page.evaluate(() => 
  localStorage.getItem('map-filters')
);
expect(JSON.parse(saved).radius).toBe('200');

// Перезагружаем
await page.reload();

// Проверяем восстановление
await expect(page.locator('text=200 км')).toBeVisible();
```

---

### 5. Соответствие RULES.md

#### ✅ Нет эмодзи в UI
- Проверка на запрещенные эмодзи: 🗺️ 💡 🎉 ❌ ✅
- Только в невидимых элементах (комментарии)
- Production UI чистый

**Тест:**
```typescript
const forbiddenEmojis = ['🗺️', '💡', '🎉'];
for (const emoji of forbiddenEmojis) {
  const element = page.locator(`text="${emoji}"`);
  const isVisible = await element.isVisible();
  expect(isVisible).toBeFalsy();
}
```

#### ✅ Используются Icon компоненты
- MaterialIcons присутствуют
- Нет SVG стеков
- Единый источник иконок

---

## 🚀 Запуск тестов

### Запуск всех тестов

```bash
# Все тесты страницы карты
npx playwright test e2e/map-page.spec.ts

# С UI режимом (debug)
npx playwright test e2e/map-page.spec.ts --ui

# Только десктоп тесты
npx playwright test e2e/map-page.spec.ts --grep "Десктоп"

# Только мобильные тесты
npx playwright test e2e/map-page.spec.ts --grep "Мобильный"
```

### Запуск отдельных тестов

```bash
# Только тест загрузки
npx playwright test e2e/map-page.spec.ts -g "загружается карта"

# Только тесты маршрута
npx playwright test e2e/map-page.spec.ts -g "Построение маршрута"

# С headed режимом (видно браузер)
npx playwright test e2e/map-page.spec.ts --headed
```

### Debug режим

```bash
# Пошаговое выполнение
npx playwright test e2e/map-page.spec.ts --debug

# С паузой на определенном месте
# (добавьте await page.pause() в тест)
```

---

## 📊 Ожидаемые результаты

### Успешный прогон

```
Running 15 tests using 1 worker

  ✓ Десктоп - загружается карта и боковое меню (5s)
  ✓ Десктоп - фильтрация по категориям работает (8s)
  ✓ Десктоп - изменение радиуса работает (6s)
  ✓ Десктоп - переключение в режим маршрута (4s)
  ✓ Десктоп - QuickActions отображаются (5s)
  ✓ Десктоп - Collapsible секции работают (7s)
  ✓ Десктоп - прокрутка до конца работает (4s)
  ✓ Мобильный - кнопка меню видна и работает (6s)
  ✓ Мобильный - свайп для закрытия работает (5s)
  ✓ Мобильный - фильтры работают (9s)
  ✓ Маршрут - выбор точек на карте (10s)
  ✓ Маршрут - выбор транспорта работает (5s)
  ✓ localStorage - фильтры сохраняются (7s)
  ✓ RULES - нет эмодзи в UI (3s)
  ✓ RULES - используются Icon компоненты (2s)

  15 passed (95s)
```

### Метрики

| Метрика | Значение |
|---------|----------|
| Общее время | ~95s |
| Среднее на тест | ~6s |
| Pass rate | 100% |
| Coverage | Основные сценарии |

---

## 🐛 Типичные проблемы

### Проблема 1: Тест timeout

**Симптом:**
```
Timeout 30000ms exceeded
```

**Решение:**
- Увеличить timeout: `{ timeout: 60_000 }`
- Проверить что сервер запущен
- Проверить скорость интернета (Leaflet CDN)

### Проблема 2: Элемент не найден

**Симптом:**
```
Element not found: text=Категории
```

**Решение:**
- Проверить что страница загрузилась
- Добавить waitForTimeout перед поиском
- Использовать `.or()` для альтернативных селекторов

### Проблема 3: Флакающие тесты

**Симптом:**
- Тесты проходят не всегда

**Решение:**
- Добавить больше waitForTimeout
- Использовать `.waitForLoadState('networkidle')`
- Проверить race conditions

---

## 📝 Структура теста

### Базовый паттерн

```typescript
test('название теста', async ({ page }) => {
  // 1. Setup
  await page.goto('/map');
  await page.waitForTimeout(2000);

  // 2. Действие
  const element = page.locator('selector');
  await element.click();

  // 3. Проверка
  await expect(element).toBeVisible();
});
```

### Продвинутый паттерн

```typescript
test('сложный сценарий', async ({ page }) => {
  // 1. Setup с localStorage
  await page.addInitScript(() => {
    localStorage.setItem('key', 'value');
  });

  // 2. Навигация
  await page.goto('/map', { waitUntil: 'domcontentloaded' });
  await page.waitForTimeout(2000);

  // 3. Множественные действия
  const element1 = page.locator('selector1');
  const element2 = page.locator('selector2');

  await element1.click();
  await page.waitForTimeout(500);
  await element2.click();

  // 4. Проверки
  await expect(element1).toHaveClass(/active/);
  await expect(element2).toBeVisible();

  // 5. Проверка состояния
  const value = await page.evaluate(() => {
    return window.someGlobalValue;
  });
  expect(value).toBe('expected');
});
```

---

## 🔧 Конфигурация

### playwright.config.ts

```typescript
{
  timeout: 240_000,  // Общий timeout
  expect: {
    timeout: 15_000, // Timeout для expect
  },
  use: {
    navigationTimeout: 120_000,
    actionTimeout: 30_000,
  },
}
```

### Переменные окружения

```bash
# Использовать существующий сервер
E2E_NO_WEBSERVER=1 BASE_URL=http://localhost:8081 npx playwright test

# Изменить порт
E2E_WEB_PORT=8090 npx playwright test
```

---

## 📈 Расширение тестов

### Что можно добавить

1. **Performance тесты**
   - Время загрузки карты
   - FPS при взаимодействии
   - Lighthouse metrics

2. **Визуальные тесты**
   - Скриншоты ключевых состояний
   - Сравнение с baseline
   - Проверка адаптивности

3. **Accessibility тесты**
   - Keyboard navigation
   - Screen reader support
   - WCAG compliance

4. **Стресс тесты**
   - Большое количество маркеров (1000+)
   - Быстрое переключение фильтров
   - Множественные маршруты

5. **Integration тесты**
   - Работа с реальным API
   - Создание/редактирование путешествий
   - Работа с избранным

---

## ✅ Checklist перед коммитом

- [ ] Все тесты проходят локально
- [ ] Нет флакающих тестов (прогнать 3 раза)
- [ ] Timeout'ы адекватные
- [ ] Селекторы надежные (не зависят от классов)
- [ ] Комментарии к неочевидным местам
- [ ] Документация обновлена
- [ ] README.md содержит инструкции

---

## 📚 Полезные ресурсы

- [Playwright Docs](https://playwright.dev/)
- [Best Practices](https://playwright.dev/docs/best-practices)
- [Debugging Tests](https://playwright.dev/docs/debug)
- [Test Selectors](https://playwright.dev/docs/selectors)

---

**Дата:** 2 января 2026  
**Автор:** GitHub Copilot  
**Статус:** ✅ Готово к использованию

