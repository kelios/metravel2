# 🎨 Быстрый старт: Система тем

## ✅ Что уже сделано

### 1. Исправлены CSS-переменные темной темы
- ✅ Обновлен `app/global.css` - все цвета темной темы теперь используют матовую палитру
- ✅ Заменены яркие синие цвета (#60a5fa) на мягкие серо-зеленые (#8fb5a5)
- ✅ Синхронизированы с `MODERN_MATTE_PALETTE_DARK`

### 2. Создан компонент переключения темы
- ✅ Компонент `components/ThemeToggle.tsx` готов к использованию
- ✅ Поддерживает 3 режима: светлая, темная, авто
- ✅ Адаптивный дизайн для всех устройств

### 3. Документация
- ✅ `THEME_SYSTEM_SPEC.md` - полное техническое задание
- ✅ `THEME_ISSUE_ANALYSIS.md` - анализ проблемы и решение
- ✅ `THEME_QUICK_START.md` - этот файл

## 🚀 Как использовать

### Добавить переключатель темы в UI

```typescript
import ThemeToggle from '@/components/ThemeToggle';

// В меню пользователя или настройках:
<ThemeToggle />

// Компактный вариант без рамки:
<ThemeToggle compact />

// Вертикальный вариант:
<ThemeToggle layout="vertical" />

// Без подписей (только иконки):
<ThemeToggle showLabels={false} />
```

### Использовать динамические цвета в компонентах

```typescript
import { useThemedColors } from '@/hooks/useTheme';
import { useMemo } from 'react';

export function MyComponent() {
  const colors = useThemedColors();
  
  const styles = useMemo(() => StyleSheet.create({
    container: {
      backgroundColor: colors.surface,
      borderColor: colors.border,
    },
    text: {
      color: colors.text,
    },
    button: {
      backgroundColor: colors.primary,
    }
  }), [colors]);
  
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Hello</Text>
    </View>
  );
}
```

### Проверить текущую тему

```typescript
import { useTheme } from '@/hooks/useTheme';

export function MyComponent() {
  const { theme, isDark, toggleTheme } = useTheme();
  
  return (
    <View>
      <Text>Текущая тема: {theme}</Text>
      <Text>Темная? {isDark ? 'Да' : 'Нет'}</Text>
      <Button onPress={toggleTheme}>Переключить</Button>
    </View>
  );
}
```

## 📋 Следующие шаги

### Шаг 1: Добавить переключатель в меню (5 минут)

1. Открыть `components/AccountMenu.tsx`
2. Добавить импорт:
   ```typescript
   import ThemeToggle from '@/components/ThemeToggle';
   ```
3. Добавить компонент в меню:
   ```typescript
   <View style={styles.themeSection}>
     <Text style={styles.sectionTitle}>Тема оформления</Text>
     <ThemeToggle compact />
   </View>
   ```

### Шаг 2: Протестировать (2 минуты)

1. Открыть приложение в браузере
2. Открыть меню пользователя
3. Переключить тему
4. Проверить, что все попапы и модалки показывают правильные цвета

### Шаг 3: Мигрировать критические компоненты (по необходимости)

Если компонент не реагирует на изменение темы:
1. Найти компонент в списке приоритета (см. THEME_SYSTEM_SPEC.md)
2. Применить шаблон миграции (см. ниже)
3. Протестировать в обеих темах

## 🔧 Шаблон миграции компонента

```typescript
// ❌ БЫЛО:
import { DESIGN_TOKENS } from '@/constants/designSystem';

const styles = StyleSheet.create({
  container: {
    backgroundColor: DESIGN_TOKENS.colors.surface,
  }
});

export function MyComponent() {
  return <View style={styles.container}>...</View>;
}

// ✅ СТАЛО:
import { useThemedColors } from '@/hooks/useTheme';
import { useMemo } from 'react';

export function MyComponent() {
  const colors = useThemedColors();
  
  const styles = useMemo(() => StyleSheet.create({
    container: {
      backgroundColor: colors.surface,
    }
  }), [colors]);
  
  return <View style={styles.container}>...</View>;
}
```

## 🎯 Приоритет миграции

### Высокий (требуется сейчас)
- ✅ CSS-переменные - **ГОТОВО**
- 🔄 Модальные окна и диалоги
- 🔄 Навигация (Footer, Header, Menu)

### Средний (на этой неделе)
- 🔄 Карточки путешествий
- 🔄 Формы и инпуты
- 🔄 Списки и таблицы

### Низкий (по мере необходимости)
- 🔄 Специализированные компоненты
- 🔄 Вспомогательные элементы

## 🐛 Известные проблемы и решения

### Проблема: Тема не переключается
**Решение:** Убедитесь, что приложение обернуто в `<ThemeProvider>` в `app/_layout.tsx`

### Проблема: Компонент не реагирует на тему
**Решение:** Замените `DESIGN_TOKENS.colors.*` на `useThemedColors()`

### Проблема: Цвета в CSS не меняются
**Решение:** Проверьте, что используете CSS-переменные `var(--color-*)`

## 📚 Полезные ссылки

- [Полное ТЗ](./THEME_SYSTEM_SPEC.md)
- [Анализ проблемы](./THEME_ISSUE_ANALYSIS.md)
- [Цветовая палитра](./constants/modernMattePalette.ts)
- [Хук useTheme](./hooks/useTheme.ts)

## ✅ Чек-лист проверки

- [x] CSS-переменные обновлены
- [x] ThemeToggle создан
- [x] Документация готова
- [ ] Переключатель добавлен в UI
- [ ] Протестировано на веб
- [ ] Протестировано на мобильных
- [ ] Критические компоненты мигрированы

---

**Дата:** 31 декабря 2025  
**Статус:** ✅ Готово к использованию  
**Время на интеграцию:** ~10 минут

