# Техническое задание: Система темной и светлой темы

## 🎯 Цели проекта

1. **Полная поддержка темной и светлой темы** во всех компонентах приложения
2. **Унификация цветовой палитры** - использование единой системы цветов из `MODERN_MATTE_PALETTE`
3. **Автоматическое переключение тем** с сохранением предпочтений пользователя
4. **Поддержка нативных и веб-платформ** (iOS, Android, Web)

## 📊 Текущее состояние (Анализ проблем)

### Проблема 1: Несоответствие цветов в CSS и TypeScript
**Симптомы:**
- Попапы и модальные окна показывают неправильные цвета в темной теме
- Цвета в `global.css` не совпадают с `MODERN_MATTE_PALETTE_DARK` (ожидаемые акценты: `#8fb5a5`, `#7aa595`)

**Причина:**
- CSS-переменные в `html[data-theme="dark"]` не синхронизированы с `MODERN_MATTE_PALETTE_DARK`
- Использование хардкодных цветов вместо переменных из палитры

### Проблема 2: Отсутствие реактивности темы в компонентах
**Симптомы:**
- Некоторые компоненты не реагируют на изменение темы
- Использование статичных `DESIGN_TOKENS.colors.*` вместо динамических `useThemedColors()`

**Причина:**
- Компоненты используют `DESIGN_TOKENS` напрямую, который не реагирует на изменения темы в React Native
- Нужно использовать хук `useThemedColors()` для динамических цветов

### Проблема 3: Несогласованность в разных частях приложения
**Компоненты с проблемами:**
- `ConfirmDialog` - статичные цвета
- Все модальные окна и попапы
- Карточки путешествий
- Навигационные элементы
- Формы и инпуты

## 🏗️ Архитектура решения

### Слой 1: Цветовая палитра (источник истины)
```
constants/modernMattePalette.ts
├── MODERN_MATTE_PALETTE (светлая тема)
└── MODERN_MATTE_PALETTE_DARK (темная тема)
```

### Слой 2: CSS-переменные (Web)
```
app/global.css
├── :root (светлая тема)
└── html[data-theme="dark"] (темная тема)
```

### Слой 3: Система токенов
```
constants/designSystem.ts
└── DESIGN_TOKENS (использует CSS vars для web, fallback для native)
```

### Слой 4: Контекст темы
```
hooks/useTheme.ts
├── ThemeProvider (управление состоянием темы)
├── useTheme() (текущая тема и методы переключения)
└── useThemedColors() (динамические цвета для React)
```

### Слой 5: Компоненты
```
components/**/*.tsx
└── Используют useThemedColors() для динамических стилей
```

## 📋 План реализации

### Этап 1: Исправление цветовой палитры ✅

**Задачи:**
1. ✅ Синхронизировать CSS-переменные в `global.css` с `MODERN_MATTE_PALETTE_DARK`
2. ✅ Убрать яркие синие цвета, заменить на матовые серо-зеленые
3. ✅ Убедиться в соответствии всех цветовых переменных

**Файлы для изменения:**
- `app/global.css` - обновить все переменные в `html[data-theme="dark"]`

**Результат:**
- Единая цветовая палитра во всех слоях
- Консистентные цвета в светлой и темной теме

---

### Этап 2: Миграция компонентов на динамические цвета

**Категории компонентов:**

#### 2.1. Модальные окна и диалоги (ВЫСОКИЙ ПРИОРИТЕТ)
**Компоненты:**
- `components/ConfirmDialog.tsx`
- `components/export/ExportModal.tsx`
- Все компоненты с `<Portal>` и `<Dialog>`

**Изменения:**
```typescript
// ❌ БЫЛО:
import { DESIGN_TOKENS } from '@/constants/designSystem';
const styles = StyleSheet.create({
  dialog: {
    backgroundColor: DESIGN_TOKENS.colors.surface,
    ...
  }
});

// ✅ СТАЛО:
import { useThemedColors } from '@/hooks/useTheme';
const MyComponent = () => {
  const colors = useThemedColors();
  const styles = useMemo(() => StyleSheet.create({
    dialog: {
      backgroundColor: colors.surface,
      ...
    }
  }), [colors]);
  ...
}
```

#### 2.2. Карточки и списки (СРЕДНИЙ ПРИОРИТЕТ)
**Компоненты:**
- `components/TravelCardCompact.tsx`
- `components/listTravel/**/*.tsx`
- `components/ui/UnifiedTravelCard.tsx`
- `components/ArticleListItem.tsx`

#### 2.3. Формы и инпуты (СРЕДНИЙ ПРИОРИТЕТ)
**Компоненты:**
- `components/TextInputComponent.tsx`
- `components/SelectComponent.tsx`
- `components/CheckboxComponent.tsx`
- `components/MultiSelectField.tsx`
- `components/FormFieldWithValidation.tsx`

#### 2.4. Навигация и меню (ВЫСОКИЙ ПРИОРИТЕТ)
**Компоненты:**
- `components/Footer.tsx`
- `components/FooterDesktop.tsx`
- `components/CustomHeader.tsx`
- `components/AccountMenu.tsx`
- `components/BottomDock.tsx`

#### 2.5. UI-элементы (СРЕДНИЙ ПРИОРИТЕТ)
**Компоненты:**
- `components/ui/Button.tsx`
- `components/ui/Chip.tsx`
- `components/ui/IconButton.tsx`
- `components/ui/FloatingActionButton.tsx`
- `components/ui/Tooltip.tsx`

#### 2.6. Специализированные компоненты (НИЗКИЙ ПРИОРИТЕТ)
**Компоненты:**
- `components/travel/**/*.tsx`
- `components/quests/**/*.tsx`
- `components/gamification/**/*.tsx`
- `components/profile/**/*.tsx`

---

### Этап 3: Обновление страниц и экранов

**Страницы для обновления:**
- `app/(tabs)/**/*.tsx`
- `app/(tabs)/travels/[param].tsx`
- `components/travel/details/TravelDetailsContainer.tsx`
- `components/home/**/*.tsx`
- `components/mainPage/**/*.tsx`

---

### Этап 4: Добавление переключателя темы в UI

**Расположение:**
- Меню пользователя (AccountMenu)
- Настройки профиля
- Футер (опционально)

**Компонент:**
```typescript
// components/ThemeToggle.tsx
import { useTheme } from '@/hooks/useTheme';

export function ThemeToggle() {
  const { theme, setTheme, isDark } = useTheme();
  
  return (
    <View>
      <TouchableOpacity onPress={() => setTheme('light')}>
        <Text>☀️ Светлая</Text>
      </TouchableOpacity>
      <TouchableOpacity onPress={() => setTheme('dark')}>
        <Text>🌙 Темная</Text>
      </TouchableOpacity>
      <TouchableOpacity onPress={() => setTheme('auto')}>
        <Text>🔄 Авто</Text>
      </TouchableOpacity>
    </View>
  );
}
```

---

### Этап 5: Тестирование и документация

**Задачи:**
1. Визуальное тестирование всех компонентов в обеих темах
2. Проверка переключения темы в реальном времени
3. Тестирование на iOS, Android, Web
4. Проверка сохранения предпочтений темы
5. Обновление документации

**Тестовые сценарии:**
- [ ] Переключение темы работает мгновенно
- [ ] Все компоненты корректно отображаются в обеих темах
- [ ] Контрастность текста соответствует WCAG AA (4.5:1)
- [ ] Предпочтения темы сохраняются между сессиями
- [ ] Системная тема (auto) работает корректно
- [ ] Нет мигания/моргания при переключении темы

---

## 🎨 Руководство по миграции компонента

### Шаблон миграции

```typescript
// ❌ СТАРЫЙ КОД (статичные цвета):
import { DESIGN_TOKENS } from '@/constants/designSystem';

export function MyComponent() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Hello</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: DESIGN_TOKENS.colors.surface,
    borderColor: DESIGN_TOKENS.colors.border,
  },
  text: {
    color: DESIGN_TOKENS.colors.text,
  },
});

// ✅ НОВЫЙ КОД (динамические цвета):
import { useThemedColors } from '@/hooks/useTheme';
import { useMemo } from 'react';

export function MyComponent() {
  const colors = useThemedColors();
  
  const styles = useMemo(() => StyleSheet.create({
    container: {
      backgroundColor: colors.surface,
      borderColor: colors.border,
    },
    text: {
      color: colors.text,
    },
  }), [colors]);
  
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Hello</Text>
    </View>
  );
}
```

### Важные правила

1. **Всегда используйте `useThemedColors()`** вместо `DESIGN_TOKENS.colors`
2. **Оборачивайте StyleSheet.create в `useMemo`** для оптимизации
3. **Добавляйте `colors` в зависимости `useMemo`**
4. **Для веба продолжайте использовать CSS-переменные** через `DESIGN_TOKENS`
5. **Тестируйте компонент в обеих темах** после миграции

---

## 📊 Метрики успеха

### Технические метрики
- ✅ 100% компонентов поддерживают темную/светлую тему
- ✅ 0 хардкодных цветов в компонентах
- ✅ Время переключения темы < 100ms
- ✅ Размер бандла не увеличился > 5KB

### UX метрики
- ✅ Все тексты читаемы в обеих темах (WCAG AA)
- ✅ Нет визуальных артефактов при переключении
- ✅ Предпочтения сохраняются между сессиями
- ✅ Системная тема поддерживается

---

## 🔧 Инструменты разработки

### Скрипты для помощи

```bash
# Найти компоненты с хардкодными цветами
grep -r "DESIGN_TOKENS.colors" components/

# Найти компоненты без useThemedColors
grep -L "useThemedColors" components/**/*.tsx

# Проверить синхронизацию CSS и TS
npm run check-theme-sync
```

### Чеклист для разработчика

Перед коммитом нового компонента:
- [ ] Компонент использует `useThemedColors()`
- [ ] Стили обернуты в `useMemo`
- [ ] Проверено в светлой теме
- [ ] Проверено в темной теме
- [ ] Проверена контрастность текста
- [ ] Нет хардкодных цветов

---

## 📚 Дополнительная документация

### Ссылки
- [Спецификация цветовой палитры](./constants/modernMattePalette.ts)
- [Документация useTheme](./hooks/useTheme.ts)
- [WCAG контрастность](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html)

### Примеры
- ✅ [ConfirmDialog.tsx](./components/ConfirmDialog.tsx) - пример после миграции
- ✅ [TravelDetailsContainer.tsx](./components/travel/details/TravelDetailsContainer.tsx) - пример использования

---

## 🚀 Приоритизация задач

### Фаза 1: Критические исправления (1-2 дня)
1. ✅ Исправить CSS-переменные в `global.css`
2. 🔄 Мигрировать модальные окна и диалоги (6 компонентов)
3. 🔄 Мигрировать навигацию (Footer, Header, Menu)

### Фаза 2: Основные компоненты (3-5 дней)
4. 🔄 Мигрировать карточки и списки
5. 🔄 Мигрировать формы и инпуты
6. 🔄 Добавить переключатель темы в UI

### Фаза 3: Остальные компоненты (1-2 недели)
7. 🔄 Мигрировать UI-элементы
8. 🔄 Мигрировать специализированные компоненты
9. 🔄 Обновить страницы и экраны

### Фаза 4: Тестирование и полировка (3-5 дней)
10. 🔄 Полное тестирование на всех платформах
11. 🔄 Исправление багов
12. 🔄 Обновление документации

---

## ✅ Чек-лист завершения

### Функциональность
- [ ] Переключение темы работает на всех платформах
- [ ] Все компоненты корректно отображаются в светлой теме
- [ ] Все компоненты корректно отображаются в темной теме
- [ ] Системная тема (auto) синхронизируется с ОС
- [ ] Предпочтения темы сохраняются в localStorage/AsyncStorage

### Качество кода
- [ ] Нет хардкодных цветов в компонентах
- [ ] Все компоненты используют `useThemedColors()`
- [ ] CSS-переменные синхронизированы с TS-палитрой
- [ ] Код прошел линтинг без ошибок

### Тестирование
- [ ] Визуальное тестирование пройдено
- [ ] Unit-тесты обновлены
- [ ] Проверена контрастность (WCAG AA)
- [ ] Проверена производительность

### Документация
- [ ] README обновлен
- [ ] Добавлены примеры использования
- [ ] Созданы гайды для разработчиков
- [ ] Добавлены комментарии в код

---

**Дата создания:** 31 декабря 2025  
**Версия:** 1.0  
**Статус:** 🔄 В разработке
