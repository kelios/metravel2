# Журнал разбора трения квестов

Последнее разобранное: 2026-09-02, progress_id 416 (прогон 2026-09-02, правки применены)

Постоянный журнал агента `quest-friction-analyst`. Существует ради одного:
следующий прогон должен видеть границу и не разбирать заново то, что уже
разобрано. Строка пишется на **каждое** прохождение, которого коснулись, включая
`чисто` и `данных нет` — молчание о прохождении неотличимо от «не смотрели».

Как читать колонки:

- **вердикт** — ровно один из: `дефект` / `чисто` / `drop-off без попыток` /
  `данных нет`;
- **найдено** — механизм отказа одной строкой с доказательством: `step_id`,
  отклонённый нормализованный ввод, номер попытки;
- **исправлено** — что фактически применено: `PATCH answer_pattern` с DB-id
  шага, `тикет #NNNN`, `передано quest-editor` или `ничего`; предложенное, но не
  применённое — `предложено, не применено`;
- **ссылка** — запись в `docs/PROBLEM_MEMORY.md` либо id тикета.

Анонимные игроки строки `quest_progress` не создают: у них трение видно только в
`quest_answer_attempt`. Такое прохождение идентифицируется префиксом
`session_key` (8 символов) — email в базе Fernet-шифротекст и в журнал не идёт.

Граница следующего прогона берётся так:

```sql
SELECT id, quest_id, updated_at FROM quest_progress WHERE updated_at > '<дата из шапки>';
SELECT id, quest_id, step_key, verdict, occurred_at FROM quest_answer_attempt WHERE occurred_at > '<дата из шапки>';
```

| разобрано | progress_id | квест | дата прохождения | вердикт | найдено | исправлено | ссылка |
|---|---|---|---|---|---|---|---|
| 2026-08-15 | 286 | `mir-castle` | 2026-08-15 | дефект | шаг 160 `church`: шесть фактически верных ответов подряд отклонены (`золотой` поп. 1, `зелёный` поп. 2, `зеленый` поп. 3, `зеленый и золотой` поп. 4, `жолтые` поп. 5, `желтые` поп. 6); словарь принимал только синий/голубой/лазурный, купола перекрашены | PATCH: шаг уведён с цвета на неперекрашиваемый признак; тикеты #1428–#1431 | `QUEST-CONTENT-ROT-001` |
| 2026-08-17 | — (сплошной скан, не прохождение) | вся база: 139 квестов, 1160 шагов | — | дефект | буквальная утечка ответа в `hint` на трёх шагах: 677 `8-rotunda`, 1168 `5-ankeruhr`, 1224 `3-fort` | PATCH `hint` ×3 + постоянный скан `scripts/scan-quest-hint-leak.js` | `QUEST-HINT-LEAK-001`, #1447 |
| 2026-08-18 | 285 | `oshmyany-crossroads` | 2026-08-15 | drop-off без попыток | шаги 466, 468, 469, 470 приняты с первой попытки, ни одного отклонения; на следующем шаге попыток нет | ничего | — |
| 2026-08-18 | 291 | `grodno-royal` | 2026-08-17 | drop-off без попыток | шаги 125, 126, 127, 128, 129 приняты с первой попытки; на шаге 6 из 9 попыток нет | ничего | — |
| 2026-08-18 | 297 | `grodno-teens-time-capsule` | 2026-08-17 | чисто | шаг 1034 `any_text` (`min_length` 15): попытка 1 отклонена по длине, попытка 2 принята через 12 с; сырой ввод у свободного шага не хранится — правило приватности | ничего | — |
| 2026-08-18 | 298 | `grodno-gorodnitsa` | 2026-08-17 | чисто | шаг 558: `столб` отклонён попытками 1 и 2 (интервал 1,07 с, одна и та же строка — двойная отправка), `трость` принята попыткой 3; эталон подтверждён внешним источником | ничего | — |
| 2026-08-18 | 301 | `vitebsk-avangard` | 2026-08-17 | drop-off без попыток | шаги 545, 546, 547 приняты с первой попытки; шаг 4 открыт, попыток по нему нет | ничего | — |
| 2026-08-18 | 303 | `vitebsk-chagall` | 2026-08-17 | данных нет | сохранён только `intro`, ни одной попытки ответа | ничего | — |
| 2026-08-18 | 305 | `yelnya-bog-bells` | 2026-08-17 | дефект | шаг 791 `5-okna-ozyora`, задание «Какого она цвета?»: `коричневого` отклонено поп. 1, `рыжего` поп. 2, `прозрачного` поп. 3, принято `коричневый` поп. 4 — в словаре не было ни одной формы родительного падежа для этих прилагательных | PATCH `answer_pattern` шага 791 (23 → 65 вариантов), локальный `scripts/yelnya-quest-data.js` синхронизирован | #1450 |
| 2026-08-18 | 306 | `polotsk-ancient` | 2026-08-17 | drop-off без попыток | шаги 168, 169 приняты с первой попытки; на шаге `collegium` открыта подсказка, попыток нет | ничего | — |
| 2026-08-18 | 307 | `minsk-loshitsa` | 2026-08-18 | данных нет | прохождение создано staff-аккаунтом, 0 сохранённых ответов, 0 попыток | ничего | — |
| 2026-08-18 | сессия `d2327dd9` (аноним, строки `quest_progress` нет) | `vitebsk-kids-skazki` | 2026-08-17 | дефект | шаг 993 `1-ulichny-kloun`: `баян` поп. 1, `акардион` поп. 2, `аккордеон` поп. 3 отклонены при открытой подсказке, `гармонь` принята поп. 4; квест из 9 шагов брошен на первом | тикет #1453, предложено, не применено (выбор варианта — решение владельца) | `QUEST-ANSWER-GRANULARITY-001` |
| 2026-08-18 | сессия `3031e19c` (аноним, вне окна — привлечено как второй игрок) | `vitebsk-kids-skazki` | 2026-08-11 | дефект | шаг 993: `скрипка` поп. 1 отклонена при открытой подсказке, попыток больше нет ни на одном квесте | тикет #1453 | `QUEST-ANSWER-GRANULARITY-001` |
| 2026-08-18 | сессия `47e8741d` (аноним, android) | `minsk-dvoriki`, `poznan-goats` | 2026-08-17, 2026-08-18 | чисто | по одной принятой с первой попытки: шаг 290 (`ключ грамота`), шаг 201 (`lew`) | ничего | — |
| 2026-08-18 | сессия `088da430` (staff-аккаунт, строк `quest_progress` нет) | `minsk-dvoriki` ×3, `vyaloe-tyshkevich-curse` | 2026-08-17 | чисто | QA-прогон владельца, не игроцкие данные: три повтора квеста 28 по ~15 с на шаг; отклонения `ключ и план` (шаг 290, поп. 1) и `сосна` (шаг 883, поп. 1) сняты с этого же аккаунта не на месте | ничего | #1434 |
| 2026-08-23 | 312 | `vitebsk-teens-street-art-map` | 2026-08-18 | чисто | шаг 1066 принят с первой; шаг 1067 `any_text`: поп. 1 отклонена, поп. 2 принята через 6 с — сырой ввод у свободного шага не хранится (правило приватности) | ничего | — |
| 2026-08-23 | 320 | `zhirovichi-icon-pear` | 2026-08-18 | данных нет | прохождение создано, 0 сохранённых ответов, 0 попыток | ничего | — |
| 2026-08-23 | сессия `21d9a4cd` (аноним) | `minsk-cmok` | 2026-08-18 | чисто | шаг 101 принят с первой попытки | ничего | — |
| 2026-08-23 | сессия `ce501e55` (аноним) | `vitebsk-kids-skazki`, `amsterdam-on-piles` | 2026-08-18 | чисто | шаг 993 (уже переделанный по #1453): `аккордеон` отклонён поп. 1, `собака` принята поп. 2; шаг 516: `пять` отклонено поп. 1, `3` принято поп. 2 — другое число, а не синоним, отказ корректен | ничего | #1453 |
| 2026-08-23 | сессии `1eafd771`, `f0d0d1a7`, `2f612728` (аноним, ответы по 0,6–3,9 с) | `vitebsk-kids-skazki` | 2026-08-18 | чисто | проверочный прогон правки #1453, не игроцкие данные: шаг 993 принимает `собака`/`пес`/`пудель`, шаг 994 их отклоняет | ничего | #1453 |
| 2026-08-23 | сессия `b97e2ca6` (аноним) | `krakow-dragon` | 2026-08-18 | чисто | шаг 109 `орёл` принят с первой попытки | ничего | — |
| 2026-08-23 | сессия `1f783ed7` (аноним) | `minsk-cmok` | 2026-08-19 | чисто | шаги 101 и 102 приняты с первой попытки | ничего | — |
| 2026-08-23 | сессия `28a968cc` (аноним, строки `quest_progress` нет) | `brest-teens-erased-city` | 2026-08-19 | дефект | шаг 1018 `1-garden-arch`: 10 попыток подряд отклонены (`i`, `ъ`, `й`, `ъ`, `ъ`, `ерь`, `ять`, `й`, `ъ`, `y`), подсказка открыта с поп. 4 и не перенаправила — игрок отвечал БУКВОЙ, а задание ждало СЛОВО; шаг не пройден. Плюс словарь не содержал `городской садъ` — надписи на арке буквально (БЕЛТА, 2019: «Городской садъ»). Шаг 1019 пройден поп. 4 (`2019`, `1918`, `1818` → `1019`) | PATCH `story`+`task`+`hint` (улика = слово, а не буква) и `answer_pattern` шага 1018 (3 → 6 вариантов); близнец 1016 `brest-kids-garden-song` — PATCH `answer_pattern`; тикет класса #1536 | `QUEST-DICT-SPELLING-GAP-001`, #1536 |
| 2026-08-23 | 334 (сессия `ccf65279`) | `vitebsk-kids-skazki` | 2026-08-21 | дефект | квест завершён. Шаг 997 `5-san-krispin`: `миска` поп. 1, `тарелка` поп. 2, `крыжка` поп. 3 и 4 отклонены при открытой подсказке, `банка` принята поп. 5 — предмет не опознаётся с места. Шаг 996 `4-teatr-lyalka`: `белорусский театр` отклонён поп. 1 — это дословно надпись на фасаде («Беларускі тэатр «Лялька»»), а задание требовало ВИД театра, «указанный на вывеске», которого там нет. Шаг 995: `амфора` поп. 1, `кувшин` поп. 2 — отказ корректен | шаг 996: PATCH `story`+`task`+`answer_pattern` (задание больше не утверждает, что вид театра написан на вывеске); шаг 997: тикет #1537; координата шага 996: тикет #1538 | #1537, #1538 |
| 2026-08-24 | — (разбор тикета #1537) | `vitebsk-kids-skazki` | 2026-08-21 | дефект закрыт | шаг 997 `5-san-krispin`: предмет опознан по фотографиям — это НЕ банка. Приёмник монет на столике мастера — невысокий круглый бронзовый бортик прямо на столешнице, без ручки и без собственных стенок; закрытая банка с крышкой рядом — отдельный сапожный сосуд, монеты в неё не идут. Отклонённые игроком `миска`/`тарелка`/`крыжка` были добросовестным прочтением, а эталон `банка` — неверным описанием предмета. Источники (три ракурса, объект между 2016 и 2021 не менялся): `https://www.unplaces.ru/Pictures/Belorussia/Vitebsk/0599_SapozhnikVitebsk/SapozhnikVitebsk_Big_Main_05.jpg` (вид сверху на столик, EXIF DateTimeOriginal 2016-10-16; отдаётся только с Referer страницы `https://www.unplaces.ru/Monument.php?id=599`), `https://spadchyna.info/wp-content/uploads/2021/09/Вiцебск.-Вулiца-Талстога-2.-Хутарок-Мiкалаеўскiю-Помнiк-сапожнiку.jpg` (загрузка 2021-09), `https://autotravel.ru/phalbum/91343/123.jpg` (1200×800, крупный план столика) | PATCH `story`+`task`+`hint`+`answer_pattern` шага 997. Легенда больше не ведёт к стоящей рядом банке с крышкой (это отдельный сапожный сосуд): она прямо говорит, что монетку опускают не в неё, а в невысокую вещь на столешнице. Задание переведено на бесспорно наблюдаемый признак: «у неё нет ни ручки, ни высоких стенок — назови одним словом, на что она похожа»; подсказка сравнивает предмет с монеткой и явно отсекает её же («у монетки серединка сплошная, а у этой вещи пустая»), принимаемых слов не содержит (`scan-quest-hint-leak` зелёный); словарь 3 → 24 варианта, все достижимы после нормализации (`кольцо`/`обруч`/`ободок`/`бортик`/`круг`/`колесо`/`бублик`… + BE-формы `кальцо`/`калечка`/`абруч`/`абадок`/`кола`/`круглае`/`круглы`, поскольку на этом шаге игрок уже отвечал по-белорусски); `банка`/`кружка`/`миска`/`тарелка`/`монетка` не принимаются. `scripts/vitebsk-kids-quest-data.js` синхронизирован. Первая редакция завёрнута код-ревью гейтом: подсказка содержала сами принимаемые слова | #1537 |
| 2026-08-23 | 336, 337, 338 | `zheludok-palace`, `grodno-kids-park-orchestra`, `grodno-teens-time-capsule` | 2026-08-21 | данных нет | прохождения созданы, 0 сохранённых ответов, 0 попыток | ничего | — |
| 2026-08-23 | 340 (сессия `87537688`) | `grodno-royal` | 2026-08-21 | чисто | шаг 125 `кирпич` принят с первой попытки при открытой подсказке | ничего | — |
| 2026-08-23 | сессия `39143300` (аноним) | `yelnya-bog-bells` | 2026-08-22 | чисто | шаг 787 `дерево` принят с первой попытки | ничего | — |
| 2026-08-23 | сессия `6855e2e7` (аноним) | `mogilev-stargazer` | 2026-08-22 | чисто | шаг 146 `12` принят с первой попытки | ничего | — |
| 2026-08-23 | сессии `33d91aa1`, `2f4dbe3a`, `36eadfe5` (аноним) | `krakow-dragon` | 2026-08-22 | данных нет | попытки по 66–109 мс с пустым вводом и повторяющимся `attempt_no` 1 — автотест, а не игрок | ничего | — |
| 2026-08-23 | 358 (сессия `1725fe1e`) | `gervyaty-kostel` | 2026-08-23 | чисто | android, локаль `be`, квест завершён. Шаг 721: `георгий победоносец` отклонён поп. 1, `архангел михаил` принят поп. 2 через 10 с — игрок опознал другого святого и исправился сам; остальные шесть шагов приняты с первой попытки | ничего | — |
| 2026-08-23 | сессии `eeab19be`, `65f40aba`, `48ed1cb7`, `b1fe7da1`, `c0092c9c`, `1ff25063` (staff-аккаунт), `f37e13b1`, `86edc5e4` (аноним) | `torun-copernicus`, `vilnius-kids-iron-wolf`, `poznan-goats`, `luxembourg-melusina` | 2026-08-18 — 2026-08-23 | данных нет | QA-прогоны, не игроцкие данные: ответы вида `wrong`/`wrong2`/`wrong3`, одинаковая последовательность `дерево` → `бетон` → `кирпич` на двух разных квестах, ответы соседних шагов, поданные на чужой шаг | ничего | — |
| 2026-08-29 | 364 | `baranovichi-dva-goroda` | 2026-08-24 — 2026-08-26 | дефект | user 170 прошёл МИМО всех четырёх вопросных точек: в `answers` только `intro` и две необязательные (`3-kraevedcheskiy-muzey`, `6-tehnika-otkrytoe-nebo`), в `quest_answer_attempt` за прохождение НЕТ НИ ОДНОЙ строки — то есть на 712, 713, 715, 716 игрок не ошибался, а прошёл мимо. Каким именно пропуском — серверные данные не различают: `skipped` не сохраняется (#1632). `unlocked_index` 7 при `current_index` 1: вернулся 26.08 в начало маршрута. Квест не засчитан | тикет #1633 | — |
| 2026-08-29 | 374 (сессия `5feaf4e6`) | `brest-fortress` | 2026-08-25 | дефект | android, `ru`, квест ЗАВЕРШЁН (единственное засчитанное прохождение окна — единственное, где отвечены все 8 точек). Шаг 136 `holmskie`: девять отказов подряд — `снаряд` поп. 1, `пуля` поп. 2, `снаряд` поп. 3, `оружие` поп. 4 и 5, `ружье` поп. 6, `патрон` поп. 7, `патроны` поп. 8 и 9, принято `выстрелы` поп. 10 (строки 348–357, 09:51:05–09:53:33 UTC). Словарь `["пули","пуль","снаряды","осколки","от пуль","обстрел","выстрелы","пулі"]` не содержит НИ ОДНОЙ формы ед. числа, а задание требует «Напиши одно слово»: `снаряд` и `пуля` — это `снаряды` и `пули` из самого словаря. Подсказка не открыта ни разу за 10 попыток. Шаг 135 `zhazhda`: `котелок` откл. поп. 1, `каска` принята поп. 2 — отказ корректен | тикеты #1630 (шаг), #1631 (класс: скан морфологического покрытия) | `QUEST-DICT-MORPH-GAP-001` |
| 2026-08-29 | сессия `5feaf4e6` (аноним, до логина) | `brest-lantern` | 2026-08-24 | чисто | шаги 539 (`ангел`) и 540 (свободный ответ, 16 символов) приняты с первой попытки | ничего | — |
| 2026-08-29 | 381 (сессия `5feaf4e6`) | `nesvizh-radziwill` | 2026-08-27 | drop-off без попыток | android, user 171. Шаги 164 `farny` и 165 `ratusha` (оба `any_text`) приняты с первой попытки. Точки `castle` (order 1), `gate` (order 6), `benedictine` (order 7) — без ответа и без единой строки в `quest_answer_attempt`. `current_index` = `unlocked_index` = 7: игрок стоит на последней точке маршрута, финала нет, квест не засчитан | тикет #1633 | — |
| 2026-08-29 | 382 (сессия `5feaf4e6`) | `mir-castle` | 2026-08-27 | чисто (контент) + drop-off | шаг 160 `church` (`range` 3..3, «Сколько фигур сидит за столом на мозаике»): `12` откл. поп. 1, `13` откл. поп. 2, `3` принято поп. 3 — интервалы 2,5 и 4,0 с, пересчитать мозаику за это время нельзя; 12 и 13 — числа Тайной вечери, названные по памяти, эталон 3 согласован с посвящением храма (Свято-Троицкая церковь, ветхозаветная Троица — три фигуры за столом). Независимой пересверкой мозаики по фотографиям ЭТОТ прогон эталон не подтверждал. Шаги 157 `castle` (`5`) и 158 `chapel` (`иисус`) приняты с первой. Точки `kostel` (order 4) и `shtetl` (order 7) — без ответа и без попыток, квест не засчитан | тикет #1633 | — |
| 2026-08-29 | сессия `4e5ab5ae` (аноним, web) | `lida-castle`, `grodno-royal` | 2026-08-25 | дефект | НЕ полевые данные: шаг 152 `lida-castle` принят в 10:41:19, шаг 125 `grodno-royal` открыт в 10:42:39 — Лида и Гродно в 90 км, 80 секунд между ними. Из кресла. При этом шаг 125 `kalozha` показал дефект, проверяемый без игрока: `глина` откл. поп. 1, `собачий сыр` (заведомо не всерьёз) поп. 2, дальше сессия обрывается. Подсказка шага — «этот материал не тесали из камня, а формовали и обжигали» — описывает обжиг ГЛИНЫ и одинаково верна для отклоняемого ответа; словарь принимает только `плинфа`/`кирпич` | тикет #1634 (правка `hint`, `answer_pattern` не трогать) | `QUEST-ANSWER-GRANULARITY-001` |
| 2026-08-29 | 377 | `minsk-cmok` | 2026-08-25 | данных нет | user 169: прохождение создано, 0 сохранённых ответов, 0 попыток | ничего | — |
| 2026-08-29 | сессия `49b59318` (аноним, android) | `krakow-dragon` | 2026-08-26 | drop-off | шаг 109 `4-barbakan`: единственная попытка `кк` (2 символа, 6,1 с) отклонена, попыток больше нет ни на одном квесте. Мусорный ввод, дефекта контента не показывает | ничего | — |
| 2026-08-29 | 387 (сессия `2620335c`) | `brest-lantern` | 2026-08-28 | чисто (контент) + drop-off | web, аноним до 16:32, затем user 174. Шаг 537 `1-chasy-fonarey`: `закат` откл. поп. 1 → игрок открыл подсказку → `зажигание фонарей` принято поп. 2 через 28 с: подсказка отработала как задумано. Шаг 540 `4-alleya-fonarey` (`any_text`, min_length 4): поп. 1 отклонена по длине (3 символа), поп. 2 принята (24 символа) — сырой ввод у свободного шага не хранится, правило приватности. Шаги 538, 539 приняты с первой. Точки `5-nikolaevskaya-cerkov` (order 1) и `7-simeonovskiy-sobor` (order 7) — без ответа и без попыток, `current_index` = `unlocked_index` = 7, квест не засчитан | тикет #1633 | — |
| 2026-08-29 | сессии `3c9ff476`, `86edc5e4` (user 1, staff) | `vitebsk-kids-skazki`, `minsk-cmok`, `krakow-dragon` | 2026-08-24 — 2026-08-26 | данных нет | QA-прогоны владельца, не игроцкие данные: шаг 997 принимает `кольцо` (проверка правки #1537), шаг 996 — `кукольный` (проверка правки по #1538), все ответы с первой попытки | ничего | #1537, #1538 |
| 2026-08-29 | — (сплошной замер, не прохождение) | вся база: 744 шага `exact_any` | — | дефект | класс «словарь не покрывает форму слова, которую провоцирует задание»: 116 шагов в 83 квестах содержат вариант во мн. числе без единственного, из них 32 требуют в задании «одно слово». Замер эвристический: `часы`/`куранты` (нет ед. числа) и белорусские прилагательные дают ложные срабатывания, а сам шаг 136 в 116 НЕ попал (эвристика зачла `пуль` за форму ед. числа) — точное число даст только скан | тикет #1631 | `QUEST-DICT-MORPH-GAP-001` |
| 2026-08-29 | — (сплошной замер, не прохождение) | вся таблица `quest_progress` | — | дефект | `SELECT count(*) FILTER (WHERE skipped <> '{}'), count(*) FILTER (WHERE early_finish) FROM quest_progress` → 0 и 0 при 33 строках. Бэкенд поля принимает с #1454 (`quests/models.py:265-267`, `serializers.py:590-613`), клиент их не шлёт: `toQuestProgressServerPayload` в `utils/questProgressMerge.ts` их не собирает, `snapshotFromServerProgress` не читает, `serverFingerprint` не учитывает. Следствие для разбора трения: официальный пропуск точки неотличим от пропуска ссылкой, и оба невидимы в серверных данных | тикет #1632 | — |
| 2026-08-29 | 381, 382, 387 (правка данных, не разбор) | `nesvizh-radziwill`, `mir-castle`, `brest-lantern` | 2026-08-27 — 2026-08-28 | дефект закрыт | зачёт восстановлен вручную по прямому указанию владельца: игроки прошли маршрут до последней точки и не получили финала из-за дефекта движка (#1633). Проставлено `completed=true`, `completed_at=updated_at` строки, `early_finish=true` и `skipped` по точкам без ответа. Пороги политики #1443 при этом соблюдены только у 387 (4 из 6 при пороге 4); у 382 (3 из 5) и 381 (2 из 5) зачёт выше порога и поставлен по решению владельца — при чтении статистики прохождений эти две строки органическими считать нельзя. Первопроходец `mir-castle` и `nesvizh-radziwill` = user 171, проставлен штатным `_record_first_completer` через `manage.py shell`; значок не выдан — его нет в каталоге (тикет #1635). Прохождение 364 `baranovichi-dva-goroda` НЕ засчитано: 0 ответов на 6 вопросных точек, засчитывать нечего — «ноль ответов финалом не считается» закреплено в `useQuestWizardProgress` | UPDATE `quest_progress` ×3, бэкап строк снят до правки | #1633 |
| 2026-08-29 | — (архитектура, не прохождение) | приём ответа во всей базе | — | дефект закрыт | корень класса «словарь не покрывает форму слова, которую провоцирует задание» устранён не сканом, а вторым проходом проверки: `utils/questAnswerMorphology.ts` + `buildAnswerChecker` для `exact`/`exact_any`. Проход срабатывает ТОЛЬКО после провала строгого сравнения, поэтому отменить уже работающий ответ не может. Замер: 212 из 212 дыр «множественное без единственного» закрыто, остаток 0. Контроль ложных приёмов: все 113 исторических отклонённых вводов из `quest_answer_attempt` остаются отклонёнными. Пределы зафиксированы тестами: числительные (`пять` против `пятый`) и числа сравниваются строго, чередование основы (`осколок`/`осколки`, `орла`/`орел`) не покрывается и остаётся на ручном словаре | `utils/questAnswerMorphology.ts`, правка `utils/questAdapters.ts`, 19 тестов | #1631 |
| 2026-08-29 | — (архитектура, не прохождение) | остаточный класс приёма ответа | — | дефект закрыт | чередование основы, которое сравнение хвостов не берёт: 48 шагов из 744 содержали вариант на `-ок`/`-ец` без формы с усечённой основой (`щенок` без `щенка`, `василек`, `петушок`, `слоненок`) — игрок, написавший косвенный падеж, получал отказ. В правило добавлен шаг «беглая гласная суффикса», стало 0 из 48. Цена расширения: дополнительно принимаемых пар 1193 → 1267 (+6%), затронутых шагов 378 → 376. Границы стережёт корпус реальных отказов в репозитории (83 пары, `__tests__/fixtures/questRejectedAnswerCorpus.ts`) — он зелёный. Чувствительность стража измерена: отключение списка окончаний ловит, снижение порога общей части с 3 до 2 — нет | корпус + страж + шаг правила, раздел в `docs/QUEST_ANSWER_INSIGHTS.md` | #1631 |
| 2026-08-29 | — (сплошной аудит, не прохождение) | 1501 шаг, все типы паттернов | — | чисто | проверка «есть ли ещё шаги, которые нельзя пройти»: невалидных `answer_pattern` — 0, пустых словарей `exact_any` — 0, паттернов типа `function` (fail closed) — 0, шагов без кириллического варианта — 4, и все четыре спрашивают год цифрами. Типы: `exact_any` 744, `any` 349, `any_text` 253, `range` 97, `exact` 55, `any_number` 3. Отдельно: 67 шагов с узким `range` в пределах 0..12 — класс «перебираемый диапазон», кулдаун между неверными попытками на них работает (`BRUTE_FORCEABLE_ANSWER_TYPES` покрывает `range`, `exact`, `exact_any`, `approx`) | ничего | — |
| 2026-09-02 | сессия `98daa3bb` (аноним, web) | `oshmyany-crossroads` | 2026-08-30 | drop-off без попыток | шаг 466 `1-sinagoga`: `круглое` принято с первой попытки (58,7 с), строки `quest_progress` нет, попыток больше нет ни на одном квесте | ничего | — |
| 2026-09-02 | сессия `348f33a0` (аноним, web) | `minsk-cmok` | 2026-08-30 | drop-off без попыток | шаги 101 (свободный, 3 символа) и 102 (`свислочь`) приняты с первой попытки за 19 с; дальше попыток нет | ничего | — |
| 2026-09-02 | сессия `c3939d3c` (аноним, web) | `khotomlya-emerald-lakes` | 2026-08-31 | drop-off | ПЕРВЫЙ игрок нового квеста (создан 30.08, контент переписан 31.08 в 17:21:34 UTC — игрок начал отвечать в 17:25:53, через 4 мин 19 с). Шаг 1589 `1-pustoe`: `л` поп. 1, `л` поп. 2, `гидрологический` поп. 3 отклонены (17:25:53, 17:26:32, 17:31:21 UTC), подсказка не открыта, квест брошен на первом шаге. Механизм не установлен: два ввода из трёх односимвольные (мусор), третий — ответ, который до переписывания требовался на ДРУГОМ шаге квеста (`6-shchit`, снят как выдуманная деталь по #1652). Гипотеза «у игрока старый бандл» проверена и отброшена: до правки шаг 1589 был `any_text` `min_length` 12 (`git show 5e7e235c3^:scripts/khotomlya-lakes-quest-data.js`), и 15-символьный `гидрологический` был бы принят, а телеметрия показывает отказ. Отдельно найден проверяемый дефект самого шага: задание зовёт нажать «Пропустить», а движок после #1633 отвечает «Точка отложена — вернитесь к ней, иначе квест не засчитается» | тикет #1720 | #1720 |
| 2026-09-02 | 407 (user 109, staff) | `barkovshchina-spirits` | 2026-08-31 | данных нет | QA-прогон владельца, не игроцкие данные: 8 точек за 9 минут, следом отзыв `quest_review` id 8 с `liked` = `disliked` = `Test`. Квест засчитан. Легаси-счётчик показывает 4 отказа (`point-2` ×2, `point-1-2` ×2), в `quest_answer_attempt` — 0 строк | тикет #1719 (потеря телеметрии) | #1719 |
| 2026-09-02 | сессия `fb9f9958` (аноним, web) | `minsk-cipher` | 2026-09-01 | чисто | шаг 323 `1-vorota` (`range` 10..12): `7` откл. поп. 1, `10` принято поп. 2 через 2 мин 48 с — игрок пересчитал сам. Шаг 324 `2-kostel`: `гавриил` откл. поп. 1 при открытой подсказке, `михаил` принят поп. 2 — игрок опознал другого архангела и исправился, отказ корректен. Дальше попыток нет | ничего | — |
| 2026-09-02 | 413 (user 1, staff) | `khotomlya-emerald-lakes` | 2026-09-02 | данных нет | QA-превью владельца: в `answers` только `intro`, `unlocked_index` 4 при `current_index` 1. Легаси-счётчик показывает 1 отказ на `1-pustoe`, в `quest_answer_attempt` — 0 строк | тикет #1719 | #1719 |
| 2026-09-02 | 414 (сессия `5bc3f761`) | `minsk-cinema` | 2026-09-02 | чисто | web, аноним до 14:45, затем user 176. Квест ЗАВЕРШЁН, первопроходец. Первое прохождение этого квеста вообще (создан 17.07.2026). Шаг 1155 `трамвай`, шаг 1161 `часы` приняты с первой; шаг 1156 `2-artmuseum` (`any_text`, `min_length` 10): поп. 1 отклонена по длине (9 символов), поп. 2 принята (18 символов) через 35 с — сырой ввод у свободного шага не хранится, правило приватности. `skipped` = `{"2-artmuseum": true}` при наличии ответа на него: официальный пропуск далёкой точки, к которой игрок потом вернулся — транспорт `skipped` после #1632 работает | ничего | #1632 |
| 2026-09-02 | 415 (сессия `5bc3f761`) | `minsk-teens-oktyabrskaya` | 2026-09-02 | дефект | web, user 176. Квест ЗАВЕРШЁН, первопроходец; первое прохождение квеста вообще (создан 16.07.2026). Восемь точек из девяти взяты с первой попытки. Шаг 1135 `9-amigos` (`range` 2..4, «Сколько жёлтых персонажей встречает тебя на ней?»): `6` поп. 1, `5` поп. 2, `7` поп. 3, затем при открытой подсказке `5` поп. 4, `7` поп. 5, `8` поп. 6 отклонены (строки 409–414, 18:33:43–18:35:05 UTC), принято `3` поп. 7 — подобрано, а не сосчитано. Игрок был у объекта: координаты шага совпадают с узлом OSM 6746025910 «Мурал «3 Amigos»» (`OSGEMEOS & SPETO`, 2019) с точностью 0,3 м, темп по маршруту пешеходный. Эталон 3 подтверждён двумя источниками (vulicabrasil.com/en/vb-2019, тег OSM) — устарел не он: задание и подсказка не задают ГРАНИЦУ счёта. Кулдаун перебора исправен (интервалы 6→7→18→30→21→21 с) | тикет #1718, правка не применена: `range` расширять нельзя, переформулировка — редакционная территория владельца | `QUEST-ANSWER-GRANULARITY-001`, #1718 |
| 2026-09-02 | 416 (user 3) | `barkovshchina-spirits` | 2026-09-02 | чисто | квест ЗАВЕРШЁН штатно: у квеста 3 ровно 8 шагов, и в `answers` есть все восемь (`intro`, `point-1`, `point-1-2`, `cafe`, `point-2`, `point-2-2`, `point-3`, `point-3-2`), `early_finish` = false, `skipped` = `{}` — не случай 364 и не ручной зачёт 29.08. Развёрнутые свободные ответы. Не полевой проход: 8 точек за 7 минут 5 секунд при маршруте родник → сосновая роща → лесное озеро. Легаси-счётчик показывает 1 отказ (`point-1-2`), в `quest_answer_attempt` — 0 строк, и анонимных строк по квесту 3 за 02.09 тоже нет (во всей таблице по квесту 3 всего две строки, обе от 10.08, сессия `dd42e674`) — вариант «попытки записаны до логина» исключён проверкой. В staff-админке эта строка видна с пустым «заявленным стартом» и источником `progress_created_fallback`. Органический игрок или знакомый — данных для вывода нет: аккаунт не staff, заведён 07.06.2020, за ним одна строка прогресса, ноль отзывов, ноль попыток | тикет #1719 | #1719 |
| 2026-09-02 | — (проверка дефекта #1633, не прохождение) | все прохождения окна | 2026-08-30 — 2026-09-02 | дефект закрыт | `SELECT count(*) FILTER (WHERE completed), count(*), count(*) FILTER (WHERE NOT completed AND current_index = unlocked_index AND current_index > 0) FROM quest_progress WHERE updated_at > '2026-08-28'` → 5 завершённых из 6, зависших на последней точке без зачёта — 0. В прошлом окне таких было 4 из 6. Шестое прохождение (413) — QA-превью владельца на первом шаге. `skipped` больше не пуст: строка 414 несёт `{"2-artmuseum": true}`, поставленный клиентом органически (#1632) | ничего | #1632, #1633 |
| 2026-09-02 | — (проверка морфологии #1631, не прохождение) | все 37 попыток окна | 2026-08-28 — 2026-09-02 | чисто | класс «форма слова» на новых данных не появился. В окне 14 отклонений: 7 числовых (`7` и шесть перебираемых на шаге 1135), 2 по длине свободного ответа, 5 текстовых — `закат`, `л`, `л`, `гидрологический`, `гавриил`. Ни один из пяти не является словоформой принимаемого варианта своего шага, то есть дыр, которые морфология должна была бы закрыть, в окне нет. Ложных приёмов тоже нет: все 10 принятых текстовых вводов окна лежат в словарях своих шагов БУКВАЛЬНО (`зажигание фонарей`, `ангел`, `пуговица`, `круглое`, `свислочь`, `михаил`, `трамвай`, `часы`, `обезьяна`, `ленин`) — второй проход не сработал ни разу, ни в плюс, ни в минус | ничего | #1631 |
| 2026-09-02 | — (сплошной механический аудит, не прохождение) | вся база: 166 квестов, 1612 шагов | — | чисто | класс A (недостижимый вариант словаря): 7087 вариантов `exact`/`exact_any` проверены, недостижимых 0 — включая 111 шагов десяти квестов, добавленных после аудита 29.08. Класс B (тип паттерна против `input_type`): 4 рассогласования, ни одно не блокирует — шаги 269, 271, 261 просят «Введи число» при `input_type` `text` (числовой чекер отрабатывает), шаг 1253 `warsaw-kids-bazyliszek` держит `exact_any` при `input_type` `number`, но `"1"` в словаре есть. Класс D (утечка ответа): `node scripts/scan-quest-hint-leak.js --scopes=step` → 0 находок на 1436 шагах; в полном прогоне 80 находок, все в `intro` (67) и `quest_title` (13), ни одной в десяти новых квестах | ничего | `QUEST-ANSWER-UNREACHABLE-001`, `QUEST-HINT-LEAK-001` |
| 2026-09-02 | — (сплошной замер, не прохождение) | вся база: 1612 шагов | — | дефект | класс «счётный шаг задаёт эталон диапазоном, а не числом»: условие — тип `range`/`any_number` И слово «скольк» в задании; подходит 91 шаг, эталон размазан диапазоном у 70, задан одним числом у 19 (контроль чист — там граница счёта прописана словами). Спорная подгруппа «диапазон при мелком счёте» (`span >= 2`, `max <= 6`) — 42 шага, включая 1135 и уже известный эталон перебора `braslav-mezh-ozyor / 1-uspenskaya-church` (шаг 746) | тикет #1718 со списком 42 шагов и командой замера | `QUEST-ANSWER-GRANULARITY-001`, #1718 |
| 2026-09-02 | — (сплошной замер, не прохождение) | вся таблица `quest_progress` после 06.08.2026 | — | дефект | доставка телеметрии попыток. ПЕРВАЯ РЕДАКЦИЯ ЗАМЕРА БЫЛА ЗАНИЖЕНА: фильтр «легаси-счётчик отказов > 0» не видел прохождений, где игрок ни разу не ошибся. Верный знаменатель — «было что записать» = принятый ответ на шаге с типом НЕ `any` ИЛИ отказ по легаси-счётчику (шаги `any` телеметрия не пишет вовсе, проверено на 415). Результат: 33 строки после запуска телеметрии, у 26 было что записать, доставлено 22, потеряно целиком 4 — 277 `novogrudok-crown` (13.08, 8 принятых ответов), 407 (31.08), 413 (02.09), 416 (02.09). Утверждение «все случаи за последние три дня» снято: класс существует с 13.08. Офлайн исключён для 407 (финал 20:29:29 UTC, отзыв того же аккаунта записан 20:30:14). Гипотеза «событию некуда адресоваться» отброшена: `GET /api/quests/by-quest-id/` отдаёт `id`. Остаётся молчаливый дроп батча на 4xx (`utils/questAnswerTelemetry.ts:252-259`) и недожатая очередь при закрытии вкладки | тикет #1719 обновлён верными числами | #1719 |
| 2026-09-02 | — (детектор класса, не прохождение) | вся таблица `quest_progress`, 38 строк | — | чисто | разбор колонки «источник старта» из staff-админки. `progress_created_fallback` выставляется, когда подзапрос `QuestAnswerAttempt.filter(quest_id=OuterRef, user_id=OuterRef).order_by('occurred_at','pk')[:1]` пуст (`quests/services/progress_timing.py:19-48` бэкенда), и фильтр идёт по `user_id` — анонимные попытки (`user_id IS NULL`) к владельцу прогресса не привязываются никогда. Поэтому пустой «заявленный старт» НЕ равен потере телеметрии. Разбор всех 16 fallback-строк: 5 — прохождения до запуска телеметрии, 7 — игрок не ответил ни на один проверяемый шаг и не ошибался (записывать было нечего), 0 — попытки записаны анонимно до логина (группа реальна по коду, сегодня пуста), 4 — настоящая потеря. Остальные 22 из 38 имеют источник `answer_attempt`. Вывод: детектор годен только с вычетом трёх невиновных групп | ничего | #1719 |
| 2026-09-02 | 277 (user 143, вне окна — привлечено корректировкой замера) | `novogrudok-crown` | 2026-08-13 | дефект | квест завершён, 8 принятых ответов на проверяемых шагах, в `quest_answer_attempt` по квесту 96 — 0 строк за всё время. Контроль: тот же user 143 позже доставлял телеметрию с четырёх других квестов (`grodno-royal` 5 строк, `grodno-teens-time-capsule` 3, `grodno-gorodnitsa` 10, `gervyaty-kostel` 7), и 13.08 доставка работала по продукту в целом (7 строк из 2 сессий). Оговорка: первая доставленная строка самого user 143 датирована 17.08, поэтому для 13.08 нельзя полностью исключить клиент без телеметрии. Трение по прохождению разобрать нельзя — данных о попытках нет | тикет #1719 | #1719 |
| 2026-09-02 | — (новые квесты, не прохождение) | 27 квестов, созданных после 2026-08-18 | — | данных нет | из 27 новых квестов прохождения были только у одного — `khotomlya-emerald-lakes` (158): одна анонимная сессия и одно QA-превью владельца, обе на первом шаге. У остальных 26 — 0 строк `quest_progress` и 0 попыток (у `luxembourg-melusina` 2 попытки, уже разобранные как QA 23.08). Отдельно: `minsk-cinema` (119) и `minsk-teens-oktyabrskaya` (116) созданы в июле и до 02.09 не открывались ни разу — user 176 стал первопроходцем обоих за один вечер | ничего | — |
