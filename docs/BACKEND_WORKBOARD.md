# Backend workboard

Last verified: 2026-06-08 (re-verified active queue against prod `metravel.by`).

Scope: active issues that still require changes in the **backend service** (separate repo / separate service, not this Expo frontend monorepo). Closed items are archived below so the backend team can read the top table as the current queue.

Verification source: read-only probes against the e2e backend from `.env.e2e` (`192.168.50.36`) plus linked frontend evidence in `docs/AGENT_WORKBOARD.md`. Auth-only probes used programmatic login and did not print secrets.

2026-06-08 re-verification note: the e2e backend (`192.168.50.36`, LAN) was not reachable from this run, so the active queue was re-probed against prod `metravel.by` (public, read-only GET, 60-id sweep for collection contracts). Auth-gated items (BE-016 avatar, BE-018 comment user_name) could not be re-verified without owner/editor tokens and keep their last status. Net changes: BE-002, BE-015, BE-017 verified fixed (moved to archive); TASK-20260520-001 image sizes/headers now pass, only AVIF remains (downgraded to P3); BE-011 still reproduces; BE-010 not re-verified.

2026-06-09 security review note: a two-pass architectural/security review of the backend repo (`../metravel-backend`) added BE-022…BE-040 below (tasks `012`–`030`). Pass 1 = security/reliability/perf/deploy across travels/users/maintenance/articles/settings/nginx/docker; pass 2 = messaging/quests/user_points/travel_comments/info/`mtravel_async`/migrations/logging + runtime re-probe. Three findings are runtime-confirmed on prod (read-only, no mutations): `POST /api/upload` → 400 not 401 (endpoint is public — BE-023), `api/chat/` reachable without auth (BE-032), `DEBUG=True` confirmed in `prod/settings.py` (BE-024, traceback-leak runtime check deferred to avoid breaking prod). The rest are code-review findings. Net new: 5×P1 (CRITICAL access/exposure), 9×P2 (HIGH), 5×P3 (MEDIUM). FastAPI `mtravel_async`, `user_points`, and quest write-permissions reviewed and found OK (no task).

Rule: a frontend mitigation is load-bearing until a backend fix is verified and a frontend regression test covers the new backend behaviour. Do not remove frontend guards just because a backend task is archived here.

## Active Backend Tasks

| ID | Task | Priority | Verified status | Frontend mitigation | Backend action |
| --- | --- | --- | --- | --- | --- |
| TASK-20260520-001 | Image server resize params and cache headers | P3 | Mostly fixed; only AVIF remains. Prod probe 2026-06-08 (`/travel-image/645/conversions/...webp`): `w=480&q=60` -> WebP `19,966b`, `w=720&q=60` -> WebP `39,448b`, `w=1200&q=65` -> WebP `48,462b` (all well under 220KB); `Cache-Control: public, max-age=31536000, immutable`; `Vary: Accept`. `Accept: image/avif` still falls back to JPEG (no AVIF). | Frontend travel LCP/mobile image contract is already capped (`w=720` for mobile hero). | Add AVIF for `Accept: image/avif` (currently JPEG fallback); size/cache targets already met. Optional PSI/Lighthouse mobile LCP validation. |
| BE-010 / TASK-20260605-003 | Comment tree requires N+1 sub-thread fetches | P2 | Not re-verified on prod 2026-06-08 (sampled travel 563 comment had `sub_thread:null`; needs a travel with nested replies). Last known: still reproduces (probe 2026-06-05, travel_id=733 comments carry `sub_thread > 0`, replies not embedded). | `getTravelComments` (`api/comments.ts`) BFS-fetches sub-threads. | Return the full comment tree in one request (`?expand=sub_threads` / `?depth=full`) or a flat list with `parent_id`. |
| BE-011 / TASK-20260605-004 | No slug-resolution endpoint; FE brute-forces search on 404 | P2 | Still reproduces. Prod probe 2026-06-08: `GET /api/travels/resolve-slug/?slug=definitely-not-existing-slug` -> `404` and `?slug=otdykh-na-naroch` (existing) -> `404` — resolver endpoint absent. | `findTravelBySlugFallback` runs multiple search variants across pages. | Provide stable immutable slugs or a resolver endpoint so FE can collapse the fan-out to one call. |
| BE-016 / TASK-20260605-007 | Profile avatar URL fresh reproduction | P3 | Needs fresh repro. Earlier prod browser evidence showed an avatar 404 with doubled `/avatar/.../avatar/`; direct 2026-06-05 re-check could not verify old user id because `/api/user/82/profile/` returned `404`, and current `torun` travel payload exposes `userName` but no numeric author id. | `useAvatarUri` caches failed avatar URLs for the session and renders fallback. | Backend/tester should probe a current known account with an uploaded avatar and fix URL generation/storage if raw avatar still returns 404. |
| BE-018 / TASK-20260607-008 | Comment `user_name` is frozen to `auth_user`, ignores renamed `Profile` | P2 | Still reproduces in prod. Probe 2026-06-07 with editor token (user_id=120, profile id=86): `PUT /api/user/120/profile/update/` with `{"first_name":"Редакция","last_name":"metravel"}` -> `200`; subsequent `POST /api/travel-comments/` -> `201` with `"user_name":"Sergey Savran"`. OpenAPI confirms `TravelComment.user_name` is `readOnly` and no `auth_user` write endpoint is documented. | Frontend mass-augment script (`scripts/seo-mass-augment.js`) prefixes editor comments with `"От редакции metravel:"` so the editorial voice opens the text even though the byline reads the old name. | Render `TravelComment.user_name` from the related `Profile.first_name`/`last_name` (with `auth_user` fallback when Profile names are empty), so a profile rename takes effect for both new and existing comments. See `tasks/008-comment-username-from-profile.md`. |
| BE-019 / TASK-20260608-009 | Superuser cannot DELETE another user's comment (403) | P2 | Reproduces in prod. Probe 2026-06-08, owner/superuser account: `DELETE https://metravel.by/api/travel-comments/3/` with `Authorization: Token …` -> `403 Forbidden`; response `Allow` header lists `…DELETE…` (method permitted, user not authorized). Backend profile/login reports `is_superuser=true` for the same user, so the comment viewset's object-level permission is internally inconsistent (author-only, no superuser override). | None needed — FE is correct: sends `Authorization: Token` on DELETE (`api/client.ts:299`), derives the "Удалить (Админ)" affordance from backend `is_superuser` (`components/travel/CommentItem.tsx:39`), and toasts on 403 (`hooks/comments/commentMutations.ts:82`). The e2e "admin deletes any comment" test asserts 204 against a route mock, so it does not catch the live 403. | Add `request.user.is_superuser` (and/or `is_staff`) to the `TravelComment` delete permission so a superuser can delete any comment (204), while keeping author-deletes-own (204) and stranger-forbidden (403). See `tasks/009-comment-delete-superuser-forbidden.md`. |
| BE-020 / TASK-20260609-010 | Prod container memory limits rebalance (Django app sits at ~99% of its 700M cap) | P2 | Confirmed 2026-06-09 via metrics collector: app container creeps 46%→99.7% of its 700M `mem_limit` at idle, while PostGIS uses only 53/512M (over-provisioned). System memory healthy (~60% avg, swap ~0, no OOM). Acute 502s already mitigated (2 GB swap added, on-the-fly AVIF removed, gunicorn `--workers 2 --timeout 60 --max-requests 1000`). | None needed (server-side infra; no frontend contract change). | Raise app `mem_limit` 700m→900m and lower `metravel-gis` 512m→384m in `docker-compose-prod.app.yaml` / `docker-compose-prod.infrastructure.yaml`; redeploy; verify app stays <75% of cap at idle and 24h has no OOM/restart. See `tasks/010-prod-memory-limits-rebalance.md`. |
| BE-021 / TASK-20260609-011 | monitor.sh CPU false-alert spam on the 1-core prod box | P3 | Confirmed 2026-06-09: ~29 flapping 🚨/✅ emails to `metraveldev@gmail.com`; trigger was an instantaneous `mpstat 1 1` reading 100% while 1-min load was only 0.77 (no real saturation); memory in the alert (69%) was healthy. Fix ALREADY applied to live `/home/sx3/monitor.sh` (backup `monitor.sh.bak-20260609`); repo copy diverged (old 2330b vs deployed 5519b). | None (ops/monitoring, no frontend impact). | Land the load-gate fix in repo `monitor.sh` (CPU alert requires `load1 > cores`) and reconcile repo↔prod versions. Reference patch + full file in `D:\metravel\ops\` (`monitor-cpu-loadgate.patch`, `monitor.sh.server`). See `tasks/011-monitor-cpu-alert-load-gate.md`. |

### Security review batch — 2026-06-09 (tasks 012–030)

| ID | Task | Priority | Verified status | Frontend mitigation | Backend action |
| --- | --- | --- | --- | --- | --- |
| BE-022 / TASK-20260609-012 | DRF default permission is `AllowAny` (no `DEFAULT_PERMISSION_CLASSES`) | P1 | Code review 2026-06-09: `metravel/envs/common/settings.py:68-73` sets only auth classes, so any view without explicit `permission_classes` is fully open (DRF default). | None — depends on per-view audit; FE unaffected for already-public reads. | Add `DEFAULT_PERMISSION_CLASSES = ['rest_framework.permissions.IsAuthenticatedOrReadOnly']` + audit every view, keep explicit `AllowAny` only on public list/retrieve. See `tasks/012-drf-default-permission-allowany.md`. |
| BE-023 / TASK-20260609-013 | `api/upload` accepts unauthenticated image overwrite of any travel | P1 | **Runtime-confirmed prod 2026-06-09:** `POST https://metravel.by/api/upload` → `400` (not 401) — endpoint is public; `maintenance/views.py:250-362` declares no auth/permission, overwrites cover/gallery of any travel by numeric id, no `_is_travel_manager` check. Anon defacement vector. | **Breaking-change risk:** verify FE upload calls (`api/imageUploadQueries.ts` / gallery / avatar) always send `Authorization: Token`. | `TokenAuthentication` + `IsAuthenticated` + owner/manager check; return 401 anon / 403 non-owner. See `tasks/013-unauthenticated-image-upload.md`. |
| BE-024 / TASK-20260609-014 | Prod settings hardening: `DEBUG=True`, `ALLOWED_HOSTS=['*']`, hardcoded `SECRET_KEY` | P1 | Config-confirmed: `prod/settings.py:3` `DEBUG=True`, `:18` `ALLOWED_HOSTS=['*']`, `common/settings.py:23` hardcoded `django-insecure-` key (prod inherits). Runtime traceback-leak check deferred (did not force a 500 on prod). | None (server-side); but enables Host-header & signature-forgery attacks (reset/IG OAuth state). | `DEBUG=False`; narrow `ALLOWED_HOSTS` to real domains; `SECRET_KEY=os.getenv(...)` (fail if missing) + rotate key. See `tasks/014-prod-settings-hardening.md`. |
| BE-025 / TASK-20260609-015 | Raw-SQL geo query (`_addresses_in_radius`) — SQLi pattern, bypasses GiST index | P1 | Code review 2026-06-09: `travels/views.py:1403-1411` builds `TravelAddress.objects.raw(f"...{lat}...{lng}...{radius}")`; types validated today but pattern is unsafe and does a seq-scan (ignores `geom` GistIndex `models.py:64`). | None. | Replace with GeoDjango `geom__distance_lte=(Point(lng,lat), D(km=radius))` (as in `path_near` `views.py:1002`); drop `raw()`. See `tasks/015-raw-sql-geo-query-sqli.md`. |
| BE-026 / TASK-20260609-016 | IDOR: user history/favorites readable & deletable by any authed user | P1 | Code review 2026-06-09: `users/views.py` `history:665`, `favorite_travels:701`, `clear_history:684`(DELETE), `clear_favorite:693`(DELETE), `recommended_travels:719` filter by `user__id=pk` but never call `_ensure_self_or_superuser` (exists `:388`). Reads/deletes another user's private data. | **Breaking-change risk:** after fix, foreign `pk` returns 403 — verify FE only requests the current user's id. | Add `_ensure_self_or_superuser(request, pk)` at the start of those 5 actions. See `tasks/016-idor-user-history-favorites.md`. |
| BE-027 / TASK-20260609-017 | Unpublished/draft articles publicly listed & retrievable | P2 | Code review 2026-06-09: `articles/views.py:32` `get_queryset = Article.objects.all()`, `publish`/`status` (`articles/models.py:8,21`) not filtered. | **Breaking-change risk:** drafts will disappear from FE article lists — verify nothing relies on seeing unpublished. | Filter public queryset to `publish=1` (or by `status`). See `tasks/017-unpublished-articles-public.md`. |
| BE-028 / TASK-20260609-018 | No DRF throttling + nginx login rate-limit regex misses real auth URLs | P2 | Code review 2026-06-09: zero `throttle` config; `nginx.conf:471` limits `(login|registration|reset-password|sendpassword)` but real paths are `reset-password-link`/`set-password-after-reset`/`google-login`/`confirm-registration` → fall into generic 30r/s. Brute-force on `confirm_registration:643`. | None. | Add `DEFAULT_THROTTLE_CLASSES/_RATES` + `ScopedRateThrottle` on auth actions; fix nginx regex to actual URLs. See `tasks/018-missing-throttling-auth-endpoints.md`. |
| BE-029 / TASK-20260609-019 | Upsert travel not atomic → partial writes; `[0]` race → 500 | P2 | Code review 2026-06-09: `upsert_travel_service.py:224-239` (`process_request`) no `transaction.atomic`; `:129/:145` `...filter(id=...)[0]` → IndexError/500 on race. | None (write path already builds full payload client-side). | Wrap `process_request` in `transaction.atomic()`; replace `[0]` with `get_object_or_404`/`.first()`. See `tasks/019-upsert-travel-no-transaction.md`. |
| BE-030 / TASK-20260609-020 | View caching disabled in prod (`CACHED_VIEWS=False`) + N+1 in user travel-actions | P2 | Code review 2026-06-09: `prod/settings.py:4` `CACHED_VIEWS=False` → `cache_utils.py:33-37` no-op; heavy `facets`/`travels_for_map` recomputed each request. N+1: `serializers.py:67,128,137` call `.all()`; `users/views.py:668,704,726` use bare `Travel.objects` without `prefetch_related('countries','users')`. | None. | Set `CACHED_VIEWS=True` in prod (Redis already configured) + add `prefetch_related` to user travel-actions. See `tasks/020-cached-views-disabled-n-plus-one.md`. |
| BE-031 / TASK-20260609-021 | Gunicorn tuning + disabled async image service → WSGI workers block on Pillow → 502 | P2 | Code review 2026-06-09: `entrypoint.sh:24-34` 2 workers×2 threads, `docker-compose-prod.app.yaml:18` 700M, no healthcheck; FastAPI async image service commented out (`:42-75`, `nginx.conf:111-116`), so image encoding runs in WSGI workers (`maintenance/views._cached_image_view_impl`). Related to BE-020/tasks 010. | None (server-side infra). | Tune workers (~`2*CPU+1`), add healthcheck, enable async image service for encoding. See `tasks/021-async-image-service-disabled-502.md`. |
| BE-032 / TASK-20260609-022 | Unauthenticated OpenAI proxy `api/chat/` with hardcoded key | P2 | **Runtime-confirmed prod 2026-06-09:** reachable without auth (→400 on body validation). `maintenance/views.py:506-557` public, `client.models.list()` + completion per POST, `api_key="api_key"` placeholder `:535`, `print()` `:537`. Billing-abuse/DoS if a real key is set. | None. | Key from env, `IsAuthenticated` + throttle, drop `models.list()`/`print`; remove endpoint if unused. See `tasks/022-openai-proxy-unauthenticated.md`. |
| BE-033 / TASK-20260609-023 | Instagram long-lived `access_token` stored in plaintext | P2 | Code review 2026-06-09: `travels/models.py:389` `access_token = TextField()`. DB/dump compromise = IG account takeover. | None. | Encrypt at rest (Fernet + env key) or use a secret store; migrate existing rows. See `tasks/023-instagram-token-plaintext.md`. |
| BE-034 / TASK-20260609-024 | N+1 `COUNT(*)` per comment on public comment list | P2 | Code review 2026-06-09 (pass 2): `travel_comments/serializers.py:15` `likes_count=IntegerField(source='likes.count')`; queryset `views.py:150` not annotated; `?expand=sub_threads` `_expand_comments_flat:269-312` recurses → dozens-hundreds of COUNTs per `AllowAny` list (`views.py:155`) = anon DoS vector. | None. | `.annotate(likes_count=Count('likes', distinct=True))` in queryset + replies subquery; `IntegerField(read_only=True)`. See `tasks/024-comment-likes-n-plus-one.md`. |
| BE-035 / TASK-20260609-025 | No `max_length` on comment/message text + synchronous `send_mail` in request cycle | P2 | Code review 2026-06-09 (pass 2): `travel_comments/serializers.py:56,81,85`, `messaging/serializers.py:31`, models unbounded `TextField`. Each create calls `send_mail` synchronously (`travel_comments/views.py:225,368`; `messaging/views.py:309`) — large body × SMTP = timeout/502 risk. | **Breaking-change risk (minor):** FE should also enforce an input length limit so users see validation before submit. | Add `max_length` (~10000) + length validator; move `send_mail` to background. See `tasks/025-comment-message-max-length-async-mail.md`. |
| BE-036 / TASK-20260609-026 | `available_users` returns all profiles, unpaginated, `fields='__all__'` | P3 | Code review 2026-06-09 (pass 2): `messaging/views.py:117-121` serializes `Profile.objects.exclude(...)` with `ProfileSerializer(__all__)` (`users/serializers.py:204-207`) → heavy response, leaks all profile fields (socials, `email_notify_*`), user enumeration. | None. | Slim serializer (id/name/avatar) + pagination + name search. See `tasks/026-available-users-slim-paginated.md`. |
| BE-037 / TASK-20260609-027 | `TravelCommentThreadViewSet.list` — `AllowAny`, dumps all threads unfiltered/unpaginated | P3 | Code review 2026-06-09 (pass 2): `travel_comments/views.py:50-53` `queryset=...all()`, default `list` no `travel_id` filter, exposes service sub-threads (`is_main=False`). | None. | Drop default `list` (keep `main_by_travel`/`retrieve`) or require `travel_id` + `is_main=True` + paginate. See `tasks/027-comment-thread-list-filter-paginate.md`. |
| BE-038 / TASK-20260609-028 | Email links built from request `HTTP_HOST` → phishing/open-redirect | P3 | Code review 2026-06-09 (pass 2): `travel_comments/views.py:403-406`, `messaging/views.py:320-324` use `request.META['HTTP_HOST']` in HTML mail; `ALLOWED_HOSTS=['*']` lets a spoofed Host point links to another domain. Tied to BE-024. | None. | Build links from a fixed `settings.SITE_URL`/canonical host. See `tasks/028-email-link-host-header-injection.md`. |
| BE-039 / TASK-20260609-029 | No `LOGGING` config in the whole project | P3 | Code review 2026-06-09 (pass 2): zero `LOGGING`/`logging.config`; no managed format/level/destination, no sensitive-data filter; `send_mail(fail_silently=False)` errors hit raw stderr. | None. | Add `LOGGING` with `django.security`/`django.request` handlers + `SensitiveDataFilter`, level via env. See `tasks/029-structured-logging-config.md`. |
| BE-040 / TASK-20260609-030 | `QuestProgress` allows duplicate progress (no `UniqueConstraint`) | P3 | Code review 2026-06-09 (pass 2): `quests/views.py:190-212` — direct `POST /api/quest-progress/` can create multiple progresses per quest; uniqueness only in `get_or_create` action, not a model constraint. | None. | Add `UniqueConstraint(['user','quest'])` + migration; use `get_or_create`/`update_or_create` in `create`. See `tasks/030-quest-progress-unique-constraint.md`. |

## Closed / Verified Archive

| ID | Closed on | Verification | Frontend note |
| --- | --- | --- | --- |
| BE-001 | 2026-06-04 | Quest media double-scheme no longer reproduces. Probe: `GET /api/quests/by-quest-id/krakow-dragon/` -> `200`, `doubleSchemeCount=0`, media hosts are valid S3/API URLs. | Keep `normalizeMediaUrl` / `fixMediaUrl` regression coverage; do not remove guards without tests. |
| BE-003 | 2026-06-04 | `PUT /api/travels/upsert/` with `{}` now returns structured `400` validation errors instead of `500`. | Wizard still builds full payload client-side; backend validation is now sane for raw/partial requests. |
| BE-006 | 2026-06-04 | Dedicated e2e auth context is usable and no longer empty for QA smoke: programmatic login -> `200`, token/user id present, favorite-travels probe -> `200` with data rows. | Manual QA can use `.env.e2e` auth flow; still avoid destructive prod actions and do not print secrets. |
| BE-007 | 2026-06-04 | Profile avatar no longer comes back as a direct S3 URL. Probe: profile avatar host is backend (`192.168.50.36`), path `/avatar/profile/...`, no presigned S3 query. | Existing avatar `optimizeImageUrl` calls stay in place; add avatar resize-proxy support later only if perf measurements show it is still needed. |
| BE-004 | 2026-06-05 | Places/map catalog pagination now returns canonical and legacy shapes and honors `perPage`: `page=1&perPage=1000` -> `resultsLen=1000`, `count=1507`, `next` present. | Frontend follow-up completed: `fetchPlacesCatalog` now follows paginated pages instead of only reading the first 1000 rows. |
| BE-005 | 2026-06-05 | Places catalog rows now include explicit country fields: sampled map row had `countryName`, `countryCode`, `country_name`, and `country_code`. | Keep country fallback for old/offline payloads, but it is no longer load-bearing for current backend rows. |
| BE-008 | 2026-06-05 | Public `/api/travels/` and authenticated `/api/messages/?thread_id=1` now return canonical `count`, `next`, `previous`, and `results` in addition to legacy Laravel fields. | Existing normalizers remain compatible; optional future cleanup can remove dead dual-shape reads with tests. |
| BE-009 | 2026-06-05 | Travel list canonical `name`/`url` remained verified: sampled `/api/travels/` rows include `name` and canonical `/travels/{slug}` URL. | Optional future cleanup only. |
| BE-012 | 2026-06-05 | Media URL shape remained clean on sampled travel and place media: no double host or `/api` media prefix observed. | Keep media URL guards unless removed with regression tests. |
| BE-013 | 2026-06-05 | Quest catalog and detail now return numeric coordinates and `country_code`: `/api/quests/` first row has numeric `lat/lng` and `country_code=pl`; `/api/quests/by-quest-id/krakow-dragon/` city and first step also have numeric coordinates and `country_code=pl`. | Coordinate parsing fallback is now defensive; optional cleanup requires tests. |
| BE-014 | 2026-06-05 | Quest bundle structured fields are objects/arrays: `steps` is an array and `intro` is an object for `krakow-dragon`. | JSON-string parse fallback is now defensive; optional cleanup requires tests. |
| BE-017 | 2026-06-08 | Quests now appear in the sitemap. Prod probe: `GET https://metravel.by/sitemap.xml` -> `200`, 415 `<loc>` entries (was 388), 26 `/quests` URLs (was 0). | Frontend quest pages/meta/internal links unchanged; canonical sitemap now lists quests. |
| BE-002 | 2026-06-08 | Comments empty-list contract fixed. Prod sweep over 60 real travel ids: all `200`, 55/60 valid travels with zero comments return `200 []`; `400` only for malformed/nonexistent input (`travel-comments/` with no `travel_id`, `?travel_id=abc`, `?travel_id=99999999` -> `400 "travel not found"`). | FE 400->empty swallow in `api/comments.ts` is now defensive-only (not load-bearing). Happy-path 200->[] already covered by `__tests__/api/comments.test.ts`. Keep the guard per the no-removal rule. |
| BE-015 | 2026-06-08 | `near` empty-list contract fixed. Prod sweep over 60 real travel ids: all `200`, 8/60 valid travels with zero neighbours return `200 []`; `404` reserved for non-existent travel (`/api/travels/99999999/near/` -> `404`). | FE 404->`[]` swallow in `api/map.ts` is now defensive-only. New regression tests lock the verified contract in `__tests__/api/travels.test.ts` (`fetchTravelsNear`: 200 [] -> [], 200 data -> data, 404 -> []). |

## Notes

- The active table is intentionally short. If a backend item is fixed and verified, move it to the archive instead of leaving stale blockers in the queue.
- BE-004 changed an API contract and required a frontend follow-up: `api/places.ts` now loads all paginated catalog pages.
- BE-002 closed 2026-06-08: comments empty-list contract verified via a 60-id prod sweep (all `200`; 55/60 empty -> `200 []`; `400` only for malformed/nonexistent input). FE 400->empty guard kept as defensive-only.
- BE-015 closed 2026-06-08: `near` empty-list contract verified via the same 60-id prod sweep (all `200`; 8/60 empty -> `200 []`; `404` only for non-existent travel). FE 404->`[]` guard kept as defensive-only; new `fetchTravelsNear` regression tests lock the contract.
- BE-016 needs a fresh current-account fixture before it can be closed or escalated as still reproducing. Not re-verifiable from this run (auth required).
- BE-017 closed 2026-06-08: quests now in `sitemap.xml` (415 locs, 26 quest URLs).
- 2026-06-09 security review batch (BE-022…BE-040, tasks `012`–`030`): two-pass review of `../metravel-backend`. Recommended fix order — P1 first (BE-023 open upload & BE-024 DEBUG are runtime-confirmed exposures, then BE-022/025/026), then P2, then P3. Breaking-change rows for FE: BE-023 (upload token), BE-026 (foreign pk → 403), BE-027 (drafts hidden), BE-035 (input length). FastAPI `mtravel_async`, `user_points`, quest write-perms reviewed and OK.
- Backend task details now live in `tasks/` per project rules.
- `TASK-20260520-001` remains in `tasks/` because it has detailed acceptance criteria and validation commands.
