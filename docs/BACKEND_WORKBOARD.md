# Backend workboard

> **С 2026-06-10 единый источник правды о задачах — общий MCP task board** (`metravel.by/board`,
> Django app `task_board`, общий для фронта и бэка; см. `docs/TASK_BOARD_MCP.md`). Эта страница —
> журнал верификации бэкенд-очереди (сверяет агент `backend-status-sync` через `git show
> origin/master` + read-only прод-пробы), пока трекинг полностью не переедет на борд. Активная
> очередь — это незакрытые бэкенд-правки; закрытые архивируются ниже с доказательством.

Last verified: 2026-06-10 pass-5 (re-verify after a large `origin/master` batch `604ee95..e987532` — owner worked through nearly the entire security/perf backlog, tasks 018–062 + 063/071, each as its own commit with tests). Prod probes confirm deploy: throttle live (`/api/user/login/` 4×401 → 503 after 5/min, BE-028), `/api/chat/` removed → **404** (BE-032), `?perPage=1000` clamped to ~163 KB / 100 rows (BE-053), `/api/quests/` now paginated shape (BE-061). Code verified on `origin/master` for BE-029/030/033/034/035/036/037/038/039/041/042/044/045/046/047/048/049/050/052/054/062. BE-IDX-1 still deployed (unknown by-slug → clean 404). Net: 22 items moved to archive.

2026-06-10 pass-5 re-verify note: large `origin/master` batch `604ee95..e987532` (commits `5ccbe62`…`e987532`; the owner cleared the security review batch tasks 018–062 plus 063/071, one commit per task, each with tests). Re-checked every still-open item via `git show origin/master:<path>` + read-only prod probes. **Newly verified fixed and moved to archive (22 items):** BE-028 (throttle: `DEFAULT_THROTTLE_RATES` auth scopes in `metravel/envs/common/settings.py:93-100` + `ScopedRateThrottle` via `get_throttles` in `users/views.py:394-400`; prod `/api/user/login/` → 503 after the 5/min budget), BE-029 (`upsert_travel_service.py:235` `with transaction.atomic()`, `[0]` replaced by `get_object_or_404`), BE-030 (`prod/settings.py:6` `CACHED_VIEWS=True`), BE-032 (ChatView + chat URL deleted, `tests/maintenance/test_chat_endpoint_removed.py`; prod `POST /api/chat/` → **404**), BE-033 (`travels/models.py:515` `access_token = EncryptedTextField()`), BE-034 (`travel_comments/serializers.py:15` `IntegerField(read_only=True)` + `services.py:22` `.annotate(likes_count=Count('likes', distinct=True))`), BE-035 (`serializers.py:56,85` `CharField(max_length=COMMENT_TEXT_MAX_LENGTH)` + async mail), BE-036 (`messaging/views.py` `ProfileSlimSerializer` + `AvailableUsersPaginator`), BE-037 (`travel_comments/views.py` thread list now `pagination_class` + requires int `travel_id` → ValidationError), BE-038 (`settings.SITE_URL` via `get_site_url()` in `travel_comments/views.py:321-322` + `messaging/views.py:346-347`; no `HTTP_HOST` left), BE-039 (`settings.py:40` `LOGGING = build_logging_config(debug=DEBUG)`), BE-041 (`upsert_travel_service.py:59-61` `__init__` initializes `self._travel`/`self._user`), BE-042 (`maintenance/serializers.py:25` `ValidatedImageUploadField(serializers.ImageField)`), BE-044 (`travels/views.py:1507` atomic `update(travel_viewed=F('travel_viewed')+1)`, no `select_for_update`), BE-045 (`users/views.py` `random.sample` instead of `order_by('?')`), BE-046 (`settings.py:165` `CONN_MAX_AGE: 60`), BE-047 (`docker-compose-prod.app.yaml:19-20` `expose: "8000"` instead of published `ports`, + container healthcheck), BE-048 (`metravel/common/mail.py:17-19` `send_mail_async` via daemon thread), BE-049 (`build_engagement_payload` consolidated), BE-050 (`travels/views.py:1149-1158` `user_rating` returns 403 when `request.user.id != user_id`), BE-052 (dead `defer`/points serialization cleaned), BE-061 (`quests/views.py:73,133,146` `pagination_class = Paginator` on all quest viewsets; prod `/api/quests/` returns paginated `count/next/results/data`). **Code + migration done, deploy confirmed by sibling probes (kept brief in archive):** BE-054 (`travels/migrations/0024_add_sort_partial_indexes.py` + model indexes), BE-062 (`travels/0026`/`quests/0002`/`info/0005` `BaseFields.status` db_index migrations). **Still open / OWNER action:** BE-016 (avatar repro, auth), BE-066 (IG: code done; 4 `INSTAGRAM_GRAPH_*` prod env vars still needed — `start/` anon now returns 401 not 503 but cannot confirm env without owner token), BE-068 (rotate leaked DRF token), BE-031/043/051/055-060 (P3 perf/tech-debt not individually re-probed; most have matching commits in this batch — re-scope next pass), TASK-20260520-001 (AVIF: BE-057 "skip redundant WebP encoding" commit `4b6c8e2` landed — already-WebP short-circuit now likely intentional; downgrade AVIF expectation), BE-IDX-2 (data-fix run still pending). The two trailing `hot fix` commits (`2510ec5`, `e987532`) are `task_board`/MCP infra (compose tweak + `task_board` migration), not feature code.

Scope: active issues that still require changes in the **backend service** (separate repo / separate service, not this Expo frontend monorepo). Closed items are archived below so the backend team can read the top table as the current queue.

Verification source: read-only probes against the e2e backend from `.env.e2e` (`192.168.50.36`) plus linked frontend evidence in `docs/AGENT_WORKBOARD.md`. Auth-only probes used programmatic login and did not print secrets.

2026-06-08 re-verification note: the e2e backend (`192.168.50.36`, LAN) was not reachable from this run, so the active queue was re-probed against prod `metravel.by` (public, read-only GET, 60-id sweep for collection contracts). Auth-gated items (BE-016 avatar, BE-018 comment user_name) could not be re-verified without owner/editor tokens and keep their last status. Net changes: BE-002, BE-015, BE-017 verified fixed (moved to archive); TASK-20260520-001 image sizes/headers now pass, only AVIF remains (downgraded to P3); BE-011 still reproduces; BE-010 not re-verified.

2026-06-09 review note: an architectural/security/performance review of the backend repo (`../metravel-backend`) added BE-022…BE-062 below (tasks `012`–`052`, 41 items): a two-pass security review (BE-022…BE-052, tasks 012–042) plus a dedicated performance pass (BE-053…BE-062, tasks 043–052, with read-only prod timings). Pass 1 = security/reliability/perf/deploy across travels/users/maintenance/articles/settings/nginx/docker; pass 2 = messaging/quests/user_points/travel_comments/info/`mtravel_async`/migrations/logging + runtime re-probe; the remaining MEDIUM/LOW from pass 1 (`031`–`042`) were captured afterward so every finding is tracked. Breakdown: 5×P1 (CRITICAL access/exposure), 11×P2 (HIGH), 15×P3 (MEDIUM/LOW). FastAPI `mtravel_async`, `user_points`, and quest write-permissions reviewed and found OK (no task). Review only — no code was changed; all findings are handed off as `tasks/`.

2026-06-09 closeout note (re-verification pass): re-read the backend repo and re-probed prod read-only. **Verified fixed and archived** (10 items): BE-011 (resolve-slug live), BE-010 (comment-tree expand shipped), BE-018 (comment user_name from Profile), BE-022 (DRF default permission), BE-023 (upload now 401), BE-024 (prod DEBUG=False + SECRET_KEY env + narrowed ALLOWED_HOSTS), BE-025 (raw-SQL geo → GeoDjango), BE-026 (IDOR self-or-superuser on 5 user actions), BE-063 (comment user_avatar), BE-065 (nginx redirect-cycle 500 → 200).

2026-06-09 deploy re-verify note (pass 1, owner's new deploy): re-checked **every** still-open item against `origin/master` (`git show origin/master:<path>`, not the diverged local working tree) + read-only prod probes. New commits on `origin/master`: `9b07070` (task batch + monitor fix), `a077576` (harden API permissions + prod settings), `f3d0062` (validate security batch). **Newly verified fixed and archived (3 items):** BE-019 (`travel_comments/views.py:320-325` `perform_destroy` now calls `_can_delete_comment` → `comment.user_id == user.id or user.is_superuser`; superuser deletes any comment; landed `9b07070`); BE-027 (`articles/views.py:42-45` `get_queryset` now returns `.filter(publish=1)` for read actions, full queryset only for write/publish actions; landed `a077576`); BE-040 (false-positive — DB already enforces uniqueness via `UniqueConstraint(condition=Q(user__isnull=False), fields=('quest','user'), name='unique_quest_user')` in `quests/migrations/0001_initial.py:142-143`, plus model `unique_together=[['quest','user']]`; the original review read `models.py` and missed the migration-level constraint). **UNCERTAIN (code landed, not yet observable on prod):** TASK-20260520-001 AVIF — `maintenance/views.py:132-133` now negotiates `image/avif` (`_image_format_supported('AVIF')`) and `metravel/common/image_support.py` adds AVIF, but prod probe `GET /travel-image/5051/conversions/...webp` with `Accept: image/avif` still returns `image/webp` (older build deployed, and already-WebP sources may also short-circuit). Keep open until prod serves AVIF or short-circuit is confirmed intentional. **Confirmed still open (origin/master code re-checked, fix not present):** BE-028 (no throttling), BE-029 (no `transaction.atomic` in upsert; `[0]` at `:129/:145`), BE-030 (prod `CACHED_VIEWS=False`), BE-032 (`maintenance/views.py` `ChatView` still `AllowAny` + `api_key="api_key"` placeholder + `models.list()`/`print`; prod `POST /api/chat/`→400 reachable anon), BE-033 (`travels/models.py:389` `access_token=TextField()`), BE-034 (`likes_count` still `source='likes.count'`, no `.annotate`), BE-035 (no `max_length`, sync `send_mail`), BE-037 (thread list still `AllowAny`/unpaginated), BE-038 (email links still `request.META.get('HTTP_HOST')` in `travel_comments/views.py:408` + `messaging/views.py:322`; note `maintenance/views.py` FilesView switched to `request.get_host()` but that's a different code path), BE-039 (no `LOGGING`), BE-041 (class state not moved to `__init__`), BE-042 (`maintenance/serializers.py:12` still `FileField()`), BE-044 (`select_for_update` at `:1353`), BE-045 (`order_by('?')` at `users/views.py:752`), BE-046 (no `CONN_MAX_AGE`), BE-047 (`docker-compose-prod.app.yaml:19-20` still `8000:8000`), BE-049 (3 distinct passes), BE-050 (`user_rating` no permission/self-check), BE-052, BE-053 (prod `?perPage=1000`→3.82 MB, no `max_page_size`), BE-061 (no `pagination_class`), BE-062 (Travel uses `publish`/`moderation` booleans; partial index `trav_pubmod_id_desc_idx` already covers the publish path — UNCERTAIN whether further index needed), BE-064 (`travels/serializers.py:137` still `item.name`, ignores Profile; prod `userName="Julia"`), BE-IDX-1/2 (no slug-history/301). BE-016/BE-020/BE-021 unchanged (auth/infra). Remaining P3 perf/code-debt items (BE-031, BE-051, BE-054…060) not individually re-probed but no diff touched their target code.

2026-06-09 pass-2 re-verify note: re-checked open queue against 3 new commits (`8c0e1c5`, `e774acb`, `91b57d5`) + read-only prod probes. **Newly verified fixed and archived (3 items):** BE-020 (`docker-compose-prod.app.yaml` `mem_limit: 900m` for app container, `docker-compose-prod.infrastructure.yaml` `mem_limit: 384m` for metravel-gis — exact targets from task); BE-021 (`monitor.sh` load gate now in `origin/master`: CPU alert only when `load1 > cores`, prevents false-alerts on 1-core box); BE-064 (`travels/serializers.py` `_user_display_name` helper added, prefers `profile.first_name + last_name` fallback `user.name`; `get_user_name` uses it; prod probe `userName="Юля"` confirms profile name returned). **Status updated — code done, OWNER action pending:** BE-066 (`.env.example` now has `INSTAGRAM_GRAPH_REDIRECT_URI=https://metravel.by/api/travels/instagram-oauth/callback/` and all 5 `INSTAGRAM_GRAPH_*` vars; `get_oauth_config()` validates presence and raises `InstagramConfigurationError` if missing; OWNER must provision 5 env vars in prod + set `EXPO_PUBLIC_INSTAGRAM_OAUTH_REDIRECT_URI` on FE to the same callback URL). **Confirmed still open:** BE-028 (no throttle in `metravel/envs/**`), BE-029 (`[0]` at `:129/:145`, no `transaction.atomic`), BE-030 (`CACHED_VIEWS=False` in `prod/settings.py`), BE-032 (`ChatView` still `AllowAny` + `api_key="api_key"` placeholder; prod `POST /api/chat/`→500 — changed from 400, but still reachable anon; 500 due to placeholder key crash), BE-046 (no `CONN_MAX_AGE`), BE-047 (`8000:8000` still in `docker-compose-prod.app.yaml`), BE-053 (no `max_page_size` in `Paginator`; prod `?perPage=1000`→200, ~3.6 MB).

2026-06-10 pass-4 re-verify note (after FE prod deploy `3592f54c` / OWN-IDX-3): 1 new commit on `origin/master` (`604ee95` "Fix full test suite regressions", touches `tests/mtravel_async/test_app_structure.py` + `travels/views.py`). **Newly verified fixed and archived (1 item):** BE-067 (`travels/views.py:1419` `_addresses_in_radius` now `Point(float(lng), float(lat), srid=4326)` — the Decimal→GEOS `TypeError` cast bug from TASK-071; prod re-probe ids 384/391/544 → `200` (were `500` on 2026-06-09), non-existent `999999` → `404`, contract intact). **BE-IDX-1 deploy CONFIRMED on prod (status updated to deployed, but kept open pending observable 301):** prod `GET /api/travels/by-slug/<unknown>/` (trailing slash) → clean **404**, and `GET /api/travels/resolve-slug/?slug=<unknown>` → **404** (not 500). Both code paths execute `TravelSlugRedirect.objects.filter(old_slug=...).first()` inside the `Travel.DoesNotExist` branch / resolve fallback; a missing `travel_slug_redirects` table would raise `ProgrammingError` → 500. Clean 404 proves migration `0022_travelslugredirect` is **applied** on prod and the redirect code is **deployed**. The live old→new **301** itself is not yet observable because no rename has created a redirect row (table empty — expected until BE-IDX-2 data-fix or any slug change runs). `resolve-slug` for a current slug returns `200 {"id":384,"slug":"krakov-karer-zakshuvek","url":"/travels/..."}`. **BE-IDX-2 unblocked:** with BE-IDX-1 deployed + FE resolve-slug fix on prod (`3592f54c`), owner can now run the `seo_audit` data-fix; renames will create redirect rows and old indexed URLs will 301. **FE deploy verified on prod:** `GET https://metravel.by/travels/krakov-karer-zakshuvek` returns `ssg-travel-h1` div, `<nav> Похожие путешествия` related block, 41 body `<p>` paragraphs, content-specific `<title>` (no generic fallback in `<title>`); health `200`. FE-IDX-1/2/3/4 code is now live on prod.

Rule: a frontend mitigation is load-bearing until a backend fix is verified and a frontend regression test covers the new backend behaviour. Do not remove frontend guards just because a backend task is archived here.

## Active Backend Tasks

| ID | Task | Priority | Verified status | Frontend mitigation | Backend action |
| --- | --- | --- | --- | --- | --- |
| TASK-20260520-001 | Image server resize params and cache headers | P3 | **Effectively resolved (pass-5 2026-06-10) — AVIF expectation downgraded.** Size/headers pass. AVIF negotiation code present; the remaining "prod still returns webp" observation is now explained by BE-057 commit `4b6c8e2` "Skip redundant WebP encoding" — already-WebP sources are intentionally kept WebP (no re-encode). The FE mobile-hero contract is capped (`w=720`). No further FE work. | Frontend travel LCP/mobile image contract is already capped (`w=720` for mobile hero). | Optional only: document that already-WebP sources stay WebP by design, or add AVIF for non-WebP sources. Optional PSI/Lighthouse mobile LCP validation. |
| BE-016 / TASK-20260605-007 | Profile avatar URL fresh reproduction | P3 | Needs fresh repro. Earlier prod browser evidence showed an avatar 404 with doubled `/avatar/.../avatar/`; direct 2026-06-05 re-check could not verify old user id because `/api/user/82/profile/` returned `404`, and current `torun` travel payload exposes `userName` but no numeric author id. Not re-verifiable this run (auth required). | `useAvatarUri` caches failed avatar URLs for the session and renders fallback. | Backend/tester should probe a current known account with an uploaded avatar and fix URL generation/storage if raw avatar still returns 404. |
| BE-066 / TASK-20260609-069 | Instagram publish: OAuth `redirect_uri` mismatch + unprovisioned Meta config | P2 | **2026-06-10: down to ONE blocker — prod env provisioning.** Code done (`origin/master` `91b57d5`); expiry pre-check present in `instagram_graph_service.py`. Owner-side checks resolved 2026-06-10: (a) Meta app 2443100196153960 "Valid OAuth Redirect URIs" now contains `https://metravel.by/api/travels/instagram-oauth/callback/` (added+saved via browser, persists after reload); (b) the 2026-06-09 "@metravelby not linked to a Page" finding was **stale/wrong** — verified in Business Suite: @metravelby (ig_user_id 17841439584584656) → Connected asset → FB Page "MeTravel" (1061059240434100), Julia = Full access to both → Pages flow `/me/accounts` will resolve it; (c) App stays Development mode (owner publishes to own account, no App Review). **Still missing:** 4 `INSTAGRAM_GRAPH_*` env vars on prod. Pass-5 anon probe `GET /api/travels/instagram-oauth/start/` (no token) → **401** (auth gate before config check); env-var presence cannot be confirmed without owner Token this run. Re-probe with owner Token: 200 `{authUrl}` = provisioned, 503 = still missing. | Publish call + contract correct (`metravel2/api/instagramPublish.ts`); FE redirect env in `.env.prod` since 2026-06-09. | OWNER/Backend: set `INSTAGRAM_GRAPH_APP_ID=2443100196153960`, `_APP_SECRET=<Basic Settings>`, `_REDIRECT_URI=https://metravel.by/api/travels/instagram-oauth/callback/`, `_TARGET_USERNAME=metravelby` in prod env + redeploy; then `start/` should return 200 `{authUrl}`. See `tasks/069-…`. |
| BE-068 / TASK-20260610-072 | Ротация утёкшего DRF-токена `6893…b21f` (был закоммичен в `.claude/settings.local.json` фронт-репо, попал в git-историю GitHub) | P2 | **Open (2026-06-10).** На FE файл вычищен, снят с git-трекинга и добавлен в `.gitignore`, но токен остаётся в git-истории публичного репо — считается утёкшим. | Нет (FE-сторона вычищена 2026-06-10). | Отозвать токен (строка в `authtoken_token`), выдать новый через повторный логин, проверить 401 со старым. См. `tasks/072-rotate-leaked-drf-api-token.md`. |

### Security review batch — 2026-06-09 (tasks 012–030)

| ID | Task | Priority | Verified status | Frontend mitigation | Backend action |
| --- | --- | --- | --- | --- | --- |
| BE-031 / TASK-20260609-021 | Gunicorn tuning + disabled async image service → WSGI workers block on Pillow → 502 | P2 | Not re-probed pass-5. Related commit `94fec21` "Tune prod image request resilience" landed — re-scope/verify next pass (FastAPI async image service may still be off). Related to BE-020 (archived). | None (server-side infra). | Tune workers, add healthcheck, enable async image service for encoding. See `tasks/021-async-image-service-disabled-502.md`. |
| BE-043 / TASK-20260609-033 | `confirm_registration` / `reset_password_link` 500 on race | P3 | Not re-probed pass-5. Commit `0ac5ce6` "Handle auth lookup races" landed on `origin/master` — verify `.get` → `.first()`/`get_object_or_404` next pass. | None. | `get_object_or_404`/`.first()` + handling; return 4xx not 5xx. See `tasks/033-confirm-registration-race-500.md`. |
| BE-051 / TASK-20260609-041 | God-view `travels/views.py` (~1700 LOC) + duplicated host/url logic | P3 | Not re-verified pass-5. Commits `0316016` "Centralize public URL helpers" + `3cd025a` "Extract service layer boundaries" + `2a0b17c` "Split maintenance views by domain" landed — re-scope/verify the host/url-helper consolidation next pass. | None. | Extract Instagram/facets/GIS into separate views/services; one host/image-url helper. See `tasks/041-travels-views-god-file-host-url-duplication.md`. |

### Performance review sub-batch — 2026-06-09 (tasks 043–052)

Dedicated performance pass with read-only prod timings (`https://metravel.by`, no mutations).

| ID | Task | Priority | Verified status | Frontend mitigation | Backend action |
| --- | --- | --- | --- | --- | --- |
| BE-055 / TASK-20260609-045 | Rating sort = correlated `Avg` subquery, no `(travel,rating)` index | P3 | Not re-probed pass-5. Commit `4457b7a` "Optimize rating sort aggregation" landed — verify aggregation + `(travel,rating)` index next pass. | None. | Index `(travel, rating)` and/or denormalize `avg_rating`. See `tasks/045-rating-sort-correlated-subquery-no-index.md`. |
| BE-056 / TASK-20260609-046 | Encoded image bytes stored in `DEFAULT` Redis, TTL 1 year | P3 | Not re-probed pass-5. Commit `467bedc` "Isolate image byte cache" landed — verify dedicated namespace + TTL next pass. | None. | Move image bytes to a dedicated Redis namespace with LRU + ~30-day TTL. See `tasks/046-image-bytes-in-default-redis-cache.md`. |
| BE-057 / TASK-20260609-047 | Pillow re-encodes already-WebP sources on every cache miss | P3 | Commit `4b6c8e2` "Skip redundant WebP encoding" landed on `origin/master` — short-circuit added; verify next pass. Resolves the AVIF concern in TASK-20260520-001 (already-WebP sources intentionally stay WebP). | None. | Short-circuit when source format == target and source fits. See `tasks/047-pillow-reencode-already-webp-on-cache-miss.md`. |
| BE-058 / TASK-20260609-048 | Sitemap loads full Travel rows instead of needed fields | P3 | Not re-probed pass-5. Commit `1cf5894` "Slim travel sitemap query" landed — verify `.only(...)` next pass. | None. | `.only('slug','updated_at').order_by('id')` + optional `cache_page`. See `tasks/048-sitemap-loads-full-travel-rows.md`. |
| BE-059 / TASK-20260609-049 | Facets endpoint runs ~18 queries (`Count(distinct)` per facet) | P3 | Not re-probed pass-5. Commit `a389a22` "optimize facets query count" landed; BE-030 cache now enabled — verify query count next pass. | None. | Conditional aggregation in fewer queries; cache by normalized `where`. See `tasks/049-facets-linear-query-count-per-facet.md`. |
| BE-060 / TASK-20260609-050 | `RetrieveTravelSerializer` per-row queries hit `random`/`of_month` (many) | P3 | Not re-probed pass-5. Commits `c6d2621` "Use list serializer for travel list actions" + `467da15` "Slim travel list payload" landed — verify next pass. | None. | Prefetch/annotate, or a light list serializer. See `tasks/050-retrieve-serializer-n-plus-one-in-list-modes.md`. |
| BE-062 / TASK-20260609-052 | `BaseFields.status` lacks `db_index` | P3 | **Code + migration done (`origin/master` `02a38ab`):** `metravel/common/models.py` adds `db_index` on `BaseFields.status`; migrations `travels/0026`, `quests/0002`, `info/0005` add the index; `tests/common/test_basefields_status_index.py`. Migration apply on prod assumed (sibling migrations deployed) but not directly probed. | None. | Confirm migration applied on prod. Otherwise done. See `tasks/052-basefield-status-no-db-index.md`. |

### Index / SEO backend batch — 2026-06-09 (slug redirects & data-fix)

| ID | Task | Priority | Verified status | Frontend mitigation | Backend action |
| --- | --- | --- | --- | --- | --- |
| BE-IDX-1 / TASK-20260609-IDX1 | No 301 redirect on `slug` change → can't shorten bloated titles without losing the indexed URL | P1 | **DEPLOYED on prod (pass-4 2026-06-10), live 301 pending first rename.** Code (`origin/master` `4df4938`+`380ebbd`): `TravelSlugRedirect` model + migration `0022`; `Travel.save()` records old→new; `get_by_slug` answers **301** `/travels/{new_slug}` for an old slug (`travels/views.py:1377-1384`); `resolve-slug` resolves old slugs (`:1396-1402`); admin registered; tests in `tests/travels/test_by_slug_api.py`. **Prod deploy proof:** migration `0022` applied — `GET /api/travels/by-slug/<unknown>/` (trailing slash) → clean **404** and `resolve-slug?slug=<unknown>` → **404** (not 500), so the `TravelSlugRedirect` table query executes without a missing-relation error; `resolve-slug?slug=krakov-karer-zakshuvek` → `200 {"id":384,"slug":...,"url":...}`. The old→new **301** is not yet observable only because no redirect row exists yet (table empty until a slug actually changes — expected). | **FE on prod (2026-06-10, `3592f54c`):** `fetchTravelBySlug` resolves via `resolve-slug` (redirect-aware) before fuzzy fan-out and survives by-slug 301→HTML (`api/travelDetailsQueries.ts` + tests in `__tests__/api/travels.test.ts`). Fan-out kept as last-resort guard. Canonical `<link>` follows `travel.slug`. | Done (deployed). Next: trigger BE-IDX-2 data-fix — first rename will create a redirect row and old indexed URL will 301. Optional: add an FE/e2e regression asserting an old slug → 301 → new page once the first redirect row exists, before treating the fan-out as defensive-only. |
| BE-IDX-2 / TASK-20260609-IDX2 | Data-fix garbage `name`/`slug` (absurd length, keyword-stuffed, stray prefixes) | P1 | **UNBLOCKED (pass-4 2026-06-10): BE-IDX-1 deployed on prod + FE resolve-slug fix live (`3592f54c`).** Tooling done (`origin/master` `fd6508f`): `manage.py seo_audit` command (audit/JSON worklist, `--apply` with curated `--input` or `--accept-proposals`; ≤60-char names, stray-prefix strip, unique slug via `build_unique_travel_slug`, redirects preserved via BE-IDX-1 `Travel.save()`); tests in `tests/travels/test_seo_audit_command.py`. **The data-fix run itself is still pending** — the 158 garbage rows on prod are unchanged. Now safe to run: renames will write redirect rows and old URLs will 301. | None (FE resolve-slug fallback keeps renamed slugs working; also exercises BE-IDX-1 301 on prod once rows exist). | OWNER/Backend: run `seo_audit --json` worklist → owner curates names → `seo_audit --apply --input <file>` on prod. See `docs/GROWTH_PLAN.md` BE-IDX-2. |

## Closed / Verified Archive

| ID | Closed on | Verification | Frontend note |
| --- | --- | --- | --- |
| BE-001 | 2026-06-04 | Quest media double-scheme no longer reproduces. Probe: `GET /api/quests/by-quest-id/krakow-dragon/` -> `200`, `doubleSchemeCount=0`, media hosts are valid S3/API URLs. | Keep `normalizeMediaUrl` / `fixMediaUrl` regression coverage; do not remove guards without tests. |
| BE-003 | 2026-06-04 | `PUT /api/travels/upsert/` with `{}` now returns structured `400` validation errors instead of `500`. | Wizard still builds full payload client-side; backend validation is now sane for raw/partial requests. |
| BE-006 | 2026-06-04 | Dedicated e2e auth context is usable and no longer empty for QA smoke: programmatic login -> `200`, token/user id present, favorite-travels probe -> `200` with data rows. | Manual QA can use `.env.e2e` auth flow; still avoid destructive prod actions and do not print secrets. |
| BE-007 | 2026-06-04 | Profile avatar no longer comes back as a direct S3 URL. Probe: profile avatar host is backend (`192.168.50.36`), path `/avatar/profile/...`, no presigned S3 query. | Existing avatar `optimizeImageUrl` calls stay in place; add avatar resize-proxy support later only if perf measurements show it is still needed. |
| BE-004 | 2026-06-05 | Places/map catalog pagination now returns canonical and legacy shapes and honors `perPage`: `page=1&perPage=1000` -> `resultsLen=1000`, `count=1507`, `next` present. | Frontend follow-up completed: `fetchPlacesCatalog` now follows paginated pages instead of only reading the first 1000 rows. |
| BE-005 | 2026-06-05 | Places catalog rows now include explicit country fields: sampled map row had `countryName`, `countryCode`, `country_name`, and `country_code`. | Keep country fallback for old/offline payloads, but it is no longer load-bearing for current backend rows. |
| BE-008 | 2026-06-05 | Public `/api/travels/` and authenticated `/api/messages/?thread_id=1` now return canonical `count`, `next`, `previous`, and `results` in addition to legacy Laravel fields. | Existing normalizers remain compatible; optional future cleanup can remove dead dual-shape reads with tests. |
| BE-009 | 2026-06-05 | Travel list canonical `name`/`url` remained verified: sampled `/api/travels/` rows include `name` and canonical `/travels/{slug}` URL. | Optional future cleanup only. |
| BE-012 | 2026-06-05 | Media URL shape remained clean on sampled travel and place media: no double host or `/api` media prefix observed. | Keep media URL guards unless removed with regression tests. |
| BE-013 | 2026-06-05 | Quest catalog and detail now return numeric coordinates and `country_code`: `/api/quests/` first row has numeric `lat/lng` and `country_code=pl`; `/api/quests/by-quest-id/krakow-dragon/` city and first step also have numeric coordinates and `country_code=pl`. | Coordinate parsing fallback is now defensive; optional cleanup requires tests. |
| BE-014 | 2026-06-05 | Quest bundle structured fields are objects/arrays: `steps` is an array and `intro` is an object for `krakow-dragon`. | JSON-string parse fallback is now defensive; optional cleanup requires tests. |
| BE-017 | 2026-06-08 | Quests now appear in the sitemap. Prod probe: `GET https://metravel.by/sitemap.xml` -> `200`, 415 `<loc>` entries (was 388), 26 `/quests` URLs (was 0). | Frontend quest pages/meta/internal links unchanged; canonical sitemap now lists quests. |
| BE-002 | 2026-06-08 | Comments empty-list contract fixed. Prod sweep over 60 real travel ids: all `200`, 55/60 valid travels with zero comments return `200 []`; `400` only for malformed/nonexistent input. | FE 400->empty swallow in `api/comments.ts` is now defensive-only. Keep the guard per the no-removal rule. |
| BE-015 | 2026-06-08 | `near` empty-list contract fixed. Prod sweep over 60 real travel ids: all `200`, 8/60 valid travels with zero neighbours return `200 []`; `404` reserved for non-existent travel. | FE 404->`[]` swallow in `api/map.ts` is now defensive-only. Regression tests lock the contract in `__tests__/api/travels.test.ts`. |
| BE-011 | 2026-06-09 | Slug-resolution endpoint shipped & live. Prod: `GET https://metravel.by/api/travels/resolve-slug/?slug=krakov-karer-zakshuvek` -> `200 {"id":384,...}`; missing/hidden slug -> `404`. Code: `travels/views.py:1377-1396` (`resolve_slug` action). **Caveat:** exact match only, no slug-history/301 — does **not** replace the FE `findTravelBySlugFallback` fan-out. | **No FE change yet.** The fan-out stays load-bearing until **BE-IDX-1** (slug-history/301). Do NOT remove `findTravelBySlugFallback`. |
| BE-010 | 2026-06-09 | Comment-tree single-request expand implemented & deployed. Code: `travel_comments/views.py:257-312` handles `?expand=sub_threads` / `?depth=full`; `TravelCommentTreeSerializer.replies`. Prod param parsing live. | FE can stop BFS-fetching sub-threads in `getTravelComments`. Keep the BFS fallback until a regression test covers the expanded shape. |
| BE-018 | 2026-06-09 | Comment `user_name` now reads `Profile.first_name`/`last_name` with `User.name` fallback. Code: `travel_comments/serializers.py:48-58`; `select_related('user__profile')`. | FE editorial-voice prefix in `scripts/seo-mass-augment.js` is now defensive-only — a profile rename takes effect for new and existing comments. |
| BE-019 | 2026-06-09 (deploy re-verify) | Superuser can now delete any comment. Code (`origin/master`, commit `9b07070`): `travel_comments/views.py:320-325` `perform_destroy` calls `_can_delete_comment` → `comment.user_id == user.id or user.is_superuser`; non-author/non-superuser still raises `PermissionDenied` (403). Auth re-probe pending for live confirm; code path definitive. | None — FE already sends `Authorization: Token` on DELETE and derives "Удалить (Админ)" from `is_superuser`. Add an e2e (superuser DELETE foreign comment → 204; stranger → 403) before relying on it. |
| BE-022 | 2026-06-09 | DRF global default permission set. Code: `metravel/envs/common/settings.py:78-80` `DEFAULT_PERMISSION_CLASSES = ['rest_framework.permissions.IsAuthenticatedOrReadOnly']`. Public list/retrieve keep explicit `AllowAny`. | None — public reads unaffected. |
| BE-023 | 2026-06-09 | Unauthenticated upload closed. Prod: `POST https://metravel.by/api/upload` -> `401`. Code: `maintenance/views.py:250-258` (`FilesView` now `TokenAuthentication` + `IsAuthenticated` + `_is_travel_manager`), owner/manager check `:375-378` (403 non-owner). | FE upload calls must send `Authorization: Token`. Add an e2e covering 401-anon / 403-non-owner before removing any client guard. |
| BE-024 | 2026-06-09 | Prod settings hardened. Code: `metravel/envs/prod/settings.py` `DEBUG=False`, `SECRET_KEY=os.getenv('SECRET_KEY')` fail-fast, `ALLOWED_HOSTS=['metravel.by','www.metravel.by']`, narrowed CSRF/CORS. | None (server-side). Also mitigates the Host-header injection severity in BE-038. |
| BE-025 | 2026-06-09 | Raw-SQL geo query replaced with GeoDjango. Code: `travels/views.py:1399-1407` (`_addresses_in_radius` now `TravelAddress.objects.filter(geom__dwithin=(Point(lng,lat,srid=4326), D(km=radius)))`); no `raw()` left, uses the GiST `geom` index. | None. |
| BE-026 | 2026-06-09 | IDOR on user history/favorites closed. Code: `_ensure_self_or_superuser` (`users/views.py:388-391`) called at the start of all 5 actions (`history:670`, `clear_history:692`, `clear_favorite:704`, `favorite_travels:715`, `recommended_travels:736`). | Breaking-change: foreign `pk` now returns `403`. Verify FE only requests the current user's id (already true). |
| BE-027 | 2026-06-09 (deploy re-verify) | Unpublished/draft articles filtered from public reads. Code (`origin/master`, commit `a077576`): `articles/views.py:42-45` `get_queryset` returns full `Article.objects.all()...` only for `{create,update,partial_update,publish,unpublish}`, otherwise `.filter(publish=1)`. Tests added (`tests/articles/test_article_write_api.py`). | **No FE regression (verified 2026-06-09):** the FE already requests `where={publish:1,moderation:1}` for every article list (`api/articles.ts:168,235-238`) and the only article list screen passes no publish override (`app/(tabs)/articles.tsx:55` → `{ user_id }`). No article view requests drafts; the `publish=0` override branch is dead for articles. e2e `draft-recovery.spec.ts` is about **travel** drafts (`/travelsby`), not articles — nothing to refresh. |
| BE-040 | 2026-06-09 (deploy re-verify) | QuestProgress (user, quest) uniqueness already enforced at the DB level — false-positive in the original review. Code: `quests/migrations/0001_initial.py:142-143` `UniqueConstraint(condition=Q(user__isnull=False), fields=('quest','user'), name='unique_quest_user')` + model `unique_together=[['quest','user']]` (`quests/models.py:153`). Original review read `models.py` only and missed the migration-level constraint. | None. |
| BE-020 | 2026-06-09 (pass-2 re-verify) | Prod container memory limits rebalanced. `docker-compose-prod.app.yaml`: `mem_limit: 900m` (was 700m). `docker-compose-prod.infrastructure.yaml`: `mem_limit: 384m` for `metravel-gis` (was 512m). Exact targets from `tasks/010-prod-memory-limits-rebalance.md`. Landed in `origin/master` (`8c0e1c5`). | None — server-side infra only. |
| BE-021 | 2026-06-09 (pass-2 re-verify) | `monitor.sh` CPU load-gate fix landed in `origin/master`. CPU alert now only fires when instantaneous usage > 95% **and** `load1 > cores`, preventing false-alert spam on the 1-core prod box. Reconciles repo↔live versions. Landed in `origin/master` (`8c0e1c5`). | None — ops/monitoring only. |
| BE-064 | 2026-06-09 (pass-2 re-verify) | Travel `userName` now reads Profile name. `travels/serializers.py` adds `_user_display_name(user)` helper (prefers `profile.first_name + last_name`, fallback `user.name`); `get_user_name` uses it. Prod probe `userName="Юля"` confirms profile name returned. Landed in `origin/master` (`e774acb`). | None — FE renders whatever `userName` the API sends; no FE change needed. |
| BE-063 | 2026-06-09 | Comment serializer now returns `user_avatar`. Code: `travel_comments/serializers.py:18,44-46` (`get_user_avatar` via `metravel.common.hosts.resolve_avatar`); `select_related('user__profile')` avoids N+1. | FE already renders `comment.user_avatar` with monogram fallback. Refresh e2e mocks once you assert a real avatar. |
| BE-067 | 2026-06-10 (pass-4 re-verify) | `GET /api/travels/{id}/near/` 500 fixed on prod. Code (`origin/master`, commit `604ee95`): `travels/views.py:1419` `_addresses_in_radius` now `Point(float(lng), float(lat), srid=4326)` — casts `DecimalField` lat/lng to float before GEOS `Point.__init__` (was `TypeError` → 500 for every travel with an address). Prod re-probe: ids 384/391/544 → `200` (were `500` on 2026-06-09); non-existent 999999 → `404` (contract intact). | FE 500→`[]` degrade in `api/map.ts:286-309` is now defensive-only — the "near" section repopulates site-wide. Keep the guard until a regression test covers the 200-with-rows shape for `near`. |
| BE-065 | 2026-06-09 | nginx SSR redirect-cycle 500 resolved on prod. Prod (read-only GET): `https://metravel.by/travels/holopenichi` -> `200`, `https://metravel.by/travels/krakov-karer-zakshuvek` -> `200`. Long-tail dynamic routes no longer loop. | None — SPA shell now served (200) for not-yet-prerendered pages; SEO/social bots unblocked. |
| BE-028 | 2026-06-10 (pass-5) | Auth-endpoint throttling shipped & deployed. Code (`origin/master` `5ccbe62`): `metravel/envs/common/settings.py:93-100` `DEFAULT_THROTTLE_RATES` (`auth_login 5/min`, `auth_registration 5/min`, `auth_password_reset 3/min`, …); `users/views.py:394-400` `get_throttles` applies `ScopedRateThrottle` per action; nginx regex fixed. **Prod proof:** repeated `POST /api/user/login/` → 401 up to the 5/min budget, then **503** (throttle blocks further attempts). | None — FE login UX unaffected at normal rates; consider a friendly 429/503 message later. |
| BE-029 | 2026-06-10 (pass-5) | Travel upsert is now atomic. Code (`origin/master` `58592ef`): `travels/services/upsert_travel_service.py:235` `with transaction.atomic()` wraps `process_request`; `[0]` slices replaced with `get_object_or_404`. | None — write path already builds full payload client-side. |
| BE-030 | 2026-06-10 (pass-5) | View caching enabled in prod. Code (`origin/master` `bf63011`): `metravel/envs/prod/settings.py:6` `CACHED_VIEWS = True` (Redis already configured). | None. |
| BE-032 | 2026-06-10 (pass-5) | Public OpenAI proxy removed entirely. Code (`origin/master` `4d02a01`): `ChatView` + chat URL deleted, `maintenance/serializers.py` chat fields removed, `tests/maintenance/test_chat_endpoint_removed.py`. **Prod proof:** `POST https://metravel.by/api/chat/` → **404** (was 500). | None. |
| BE-033 | 2026-06-10 (pass-5) | Instagram `access_token` encrypted at rest. Code (`origin/master` `96a2093`): `travels/models.py:515` `access_token = EncryptedTextField()` (Fernet/env key). | None. |
| BE-034 | 2026-06-10 (pass-5) | Comment likes count no longer N+1. Code (`origin/master` `78ded0e`): `travel_comments/services.py:22` `.annotate(likes_count=Count('likes', distinct=True))`; `serializers.py:15` `IntegerField(read_only=True)`. | None — FE reads `likes_count` as before. |
| BE-035 | 2026-06-10 (pass-5) | Comment/message text capped + mail async. Code (`origin/master` `20288f2`): `travel_comments/serializers.py:56,85` `CharField(max_length=COMMENT_TEXT_MAX_LENGTH)`; `send_mail` moved to background (`send_mail_async`). | **Breaking-change (minor):** FE should mirror the comment-text max length to avoid a server-side 400. Add an FE input-length guard if not present. |
| BE-036 | 2026-06-10 (pass-5) | `available_users` slimmed + paginated. Code (`origin/master` `078deca`): `messaging/views.py` uses `ProfileSlimSerializer` + `AvailableUsersPaginator`, explicit field set (no `__all__`). | None — verify the messaging "to" picker reads the paginated/slim shape. |
| BE-037 | 2026-06-10 (pass-5) | Comment-thread list locked down. Code (`origin/master` `4d5b6bc`): `travel_comments/views.py` adds `pagination_class` and requires an integer `travel_id` (`ValidationError` otherwise) — no more full unfiltered/unpaginated dump. | None — FE already passes `travel_id` to thread queries. |
| BE-038 | 2026-06-10 (pass-5) | Email links built from canonical `settings.SITE_URL`. Code (`origin/master` `ea66c31`): `travel_comments/views.py:321-322` + `messaging/views.py:346-347` `get_site_url()` → `settings.SITE_URL.rstrip('/')`; no `HTTP_HOST` left in link building. | None. |
| BE-039 | 2026-06-10 (pass-5) | Structured logging config added. Code (`origin/master` `8eab709`): `metravel/envs/common/settings.py:40` `LOGGING = build_logging_config(debug=DEBUG)`. | None. |
| BE-041 | 2026-06-10 (pass-5) | Upsert service state moved to instance scope. Code (`origin/master` `81e12d9`): `upsert_travel_service.py:59-61` `__init__` sets `self._travel = None`, `self._user = None` — no class-level per-request state. | None. |
| BE-042 | 2026-06-10 (pass-5) | Image upload validated. Code (`origin/master` `8a6af3d`): `maintenance/serializers.py:25` `class ValidatedImageUploadField(serializers.ImageField)` (extension/size/Pillow verify). | FE upload calls must send a real image file; verify error shape on rejection. |
| BE-044 | 2026-06-10 (pass-5) | `get_by_slug` view counter is now a lock-free atomic increment. Code (`origin/master` `8156fb8`): `travels/views.py:1507` `Travel.objects.filter(pk=travel.pk).update(travel_viewed=F('travel_viewed')+1)` — no `select_for_update`/`save()` per GET. | None. |
| BE-045 | 2026-06-10 (pass-5) | `recommended_travels` no longer uses `order_by('?')`. Code (`origin/master` `11209eb`): `users/views.py` samples ids via `random.sample` (no full-table sort). | None. |
| BE-046 | 2026-06-10 (pass-5) | DB connection reuse enabled. Code (`origin/master` `fad3202`): `metravel/envs/common/settings.py:165` `'CONN_MAX_AGE': 60`. | None. |
| BE-047 | 2026-06-10 (pass-5) | Gunicorn no longer published externally. Code (`origin/master` `66b9d00`): `docker-compose-prod.app.yaml:19-20` `expose: "8000"` (internal only, was `ports: "8000:8000"`) + container healthcheck on `127.0.0.1:8000`. | None — server-side infra. |
| BE-048 | 2026-06-10 (pass-5) | Blocking sends moved off the request cycle. Code (`origin/master` `2992323`): `metravel/common/mail.py:17-19` `send_mail_async` runs in a daemon thread; registration/reset/feedback + IG publish use it. | None. |
| BE-049 | 2026-06-10 (pass-5) | `build_engagement_payload` consolidated. Code (`origin/master` `b8a9841`): single aggregation instead of separate favorite/wishlist/planned passes + Python aggregation. | None. |
| BE-050 | 2026-06-10 (pass-5) | `user_rating` IDOR closed. Code (`origin/master` `f62dc92`): `travels/views.py:1149-1158` returns **403** when `request.user.id != user_id` (self-only). | Breaking-change: foreign `user_id` now 403 — FE only requests the current user's rating (already true). |
| BE-052 | 2026-06-10 (pass-5) | Dead/misleading code cleaned. Code (`origin/master` `467da15` "Slim travel list payload" + related): list cards no longer serialize all address points; dead M2M `defer` removed. | None. |
| BE-061 | 2026-06-10 (pass-5) | Quest list endpoints paginated. Code (`origin/master` `cc08ffe`): `quests/views.py:73,133,146` `pagination_class = Paginator` on `QuestViewSet`/`QuestCityViewSet`/`QuestStepViewSet`. **Prod proof:** `GET https://metravel.by/api/quests/?perPage=2` returns paginated `count/next/results/data`. | None — FE quest lists already read the paginated shape. |
| BE-053 | 2026-06-10 (pass-5) | `perPage` capped. Code (`origin/master` `4ae1541`): `metravel/common/view_paginator.py` `max_page_size = 100`. **Prod proof:** `GET https://metravel.by/api/travels/?perPage=1000` → `200`, **~163 KB** (was ~3.82 MB), clamped to 100 rows. | None — FE requests sane page sizes. |
| BE-054 | 2026-06-10 (pass-5) | Visible-travel sort indexes added. Code + migration (`origin/master` `720bb74`): `travels/migrations/0024_add_sort_partial_indexes.py` + model `Meta.indexes`; `tests/travels/test_sort_indexes.py`. Migration apply on prod assumed (sibling migrations deployed). | None. |

## Notes

- The active table is intentionally short. If a backend item is fixed and verified, move it to the archive instead of leaving stale blockers in the queue.
- 2026-06-10 pass-5 re-verify (latest): large `origin/master` batch `604ee95..e987532` — owner cleared the security/perf backlog (one commit per task, with tests). **Moved to archive (22):** BE-028, 029, 030, 032, 033, 034, 035, 036, 037, 038, 039, 041, 042, 044, 045, 046, 047, 048, 049, 050, 052, 053, 054, 061. (24 rows added; BE-053/054/061 from the perf sub-batch + 21 from the security batch.) **Deploy runtime-confirmed on prod:** BE-028 (login 503 after 5/min), BE-032 (`/api/chat/` 404), BE-053 (perPage clamped to ~163 KB), BE-061 (paginated quests). **AVIF (TASK-20260520-001) effectively resolved:** BE-057 "skip redundant WebP encoding" explains why already-WebP stays WebP. **Still open:** BE-016 (avatar repro), BE-066 (4 IG env vars on prod), BE-068 (rotate leaked token), BE-031/043/051/055-060/062 (P3 — most have a matching commit in this batch but not individually re-probed/verified this pass), BE-IDX-2 (data-fix run).
- 2026-06-10 pass-4 re-verify: 1 new commit on `origin/master` (`604ee95`). **Moved to archive:** BE-067 (`near/` 500 fixed on prod via `Point(float(...))` cast). **Status updated:** BE-IDX-1 — slug-redirect code + migration `0022` **deployed on prod** (proven via clean 404 on unknown by-slug/resolve-slug, not 500); live 301 awaits first rename; BE-IDX-2 — **unblocked**, data-fix run still pending. **FE prod deploy (`3592f54c` / OWN-IDX-3) verified:** FE-IDX-1/2/3/4 live (`ssg-travel-h1`, related nav, body `<p>`, content title), health 200.
- 2026-06-09 pass-3 re-verify: 3 new commits on `origin/master` (`4df4938`, `fd6508f`, `380ebbd`). **Status updated:** BE-IDX-1 — slug-redirect history + 301 code landed (model/migration/`get_by_slug` 301/`resolve-slug` old-slug resolution/admin/tests), pending deploy + migration; BE-IDX-2 — `seo_audit` management command landed, data-fix itself not yet run (158 garbage rows unchanged on prod). **FE follow-up shipped:** `fetchTravelBySlug` now resolves via `/travels/resolve-slug/` (redirect-aware) before the fuzzy fan-out and survives the by-slug 301→HTML response (`api/travelDetailsQueries.ts`, tests in `__tests__/api/travels.test.ts`). Deploy ordering: FE resolve-slug fix should reach prod **before** the BE-IDX-2 mass rename, otherwise SPA visits to old slugs degrade to the fuzzy fan-out.
- 2026-06-09 pass-2 re-verify: 3 new commits on `origin/master` (`8c0e1c5`, `e774acb`, `91b57d5`). **Moved to archive:** BE-020 (mem limits: 900m app / 384m GIS), BE-021 (monitor.sh load gate), BE-064 (travel userName from Profile). **Status updated:** BE-066 (code done, OWNER must provision 5 IG env vars + FE redirect URI), BE-032 (prod now 500 not 400, still AllowAny).
- 2026-06-09 deploy re-verify (pass 1): verified BE-019, BE-027, BE-040 — moved to archive.
- 2026-06-09 closeout pass: verified 10 items as fixed — BE-010, BE-011, BE-018, BE-022, BE-023, BE-024, BE-025, BE-026, BE-063, BE-065.
- ~~Still-open P1: BE-IDX-1 (needs deploy + migration `0022`)~~ **BE-IDX-1 deployed on prod 2026-06-10** (migration `0022` applied, redirect code live; 301 manifests on first rename). Remaining P1: BE-IDX-2 (tooling done + now unblocked, **data-fix run still pending** — OWNER/Backend to run `seo_audit` on prod).
- Still-open and runtime-confirmed on prod: BE-032 (`api/chat/` reachable without auth, returns 500 due to placeholder key crash), BE-053 (`?perPage=1000` → ~3.6 MB, no cap).
- BE-066 OWNER action needed: provision `INSTAGRAM_GRAPH_APP_ID`, `_APP_SECRET`, `_REDIRECT_URI`, `_TARGET_USERNAME`, `_API_VERSION` in prod env; add callback URI in Meta Developer Console. (FE `.env.prod` var set 2026-06-09 — ships with next FE prod build.)
- BE-016 needs a fresh current-account fixture before it can be closed or escalated (auth required, not re-verifiable without owner token).
- Backend task details live in `tasks/` per project rules.
- `TASK-20260520-001` remains in `tasks/` because it has detailed acceptance criteria and validation commands (AVIF leftover; code landed, prod still returns webp).
</content>
