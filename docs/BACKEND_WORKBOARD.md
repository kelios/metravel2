# Backend workboard

Last verified: 2026-06-04.

Scope: active issues that still require changes in the **backend service** (separate repo / separate service, not this Expo frontend monorepo). Closed items are archived below so the backend team can read the top table as the current queue.

Verification source: read-only probes against the e2e backend from `.env.e2e` (`192.168.50.36`) plus linked frontend evidence in `docs/AGENT_WORKBOARD.md`. Auth-only probes used programmatic login and did not print secrets.

Rule: a frontend mitigation is load-bearing until a backend fix is verified and a frontend regression test covers the new backend behaviour. Do not remove frontend guards just because a backend task is archived here.

## Active Backend Tasks

| ID | Task | Priority | Verified status | Frontend mitigation | Backend action |
| --- | --- | --- | --- | --- | --- |
| BE-002 | Comments endpoint returns an error for some travels with no comments | P2 | Still reproduces partially. Probe 2026-06-04: `GET /api/travel-comments/?travel_id=391` -> `200` array-like, but `GET /api/travel-comments/?travel_id=563` -> `400`. | Frontend treats the 400 as "no comments" and renders the empty state; comments read path avoids the older `main-thread` 404 probe. | Return `200` + empty list/page for any valid travel with zero comments. Reserve `400` for malformed `travel_id`. |
| BE-004 | Places/map catalog endpoint does not honor server-side pagination | P2 | Still reproduces. Probe 2026-06-04: `GET /api/travels/search_travels_for_map/?page=1&perPage=1000&where={lat,lng,radius,publish,moderation}` -> `200`, but returns `1505` rows, no `count`, no `next`; `perPage=1000` is not enforced. | Frontend fetches the full payload, then reveals cards incrementally with `visibleCount`; this reduces DOM but not network/parse cost. | Enforce pagination/cursor contract or provide bbox/viewport-scoped catalog fetch. Coordinate with frontend before changing `api/places.ts` / `api/map.ts`. |
| BE-005 | Places catalog lacks explicit country/name quality guarantees | P3 | Still relevant as backend data/API quality. Probe 2026-06-04: all sampled map-catalog rows lacked an explicit country field; frontend can derive many countries from address text, but the backend contract still does not provide normalized country data. | Frontend derives a country from address text and falls back to neutral labels; UI should not show raw broken data. | Backfill/enforce non-empty normalized country and place name fields, or document and expose the canonical fields the frontend should use. |
| TASK-20260520-001 | Image server resize params and cache headers | P2 | Partially fixed, still active. Probe 2026-06-04 on `metravel.by/gallery/...detail_hd.jpg?w=`: `w=480` -> `51,362b` WebP, `w=720` -> `112,680b` WebP, `w=1200` -> `241,698b` WebP; single `Cache-Control: public, max-age=31536000, immutable`; `Vary: Accept`. Size/header basics are fixed, but target WebP budget for `w=1200` (`<=220KB`) is still missed. | Frontend already sends responsive image params; performance still depends on backend byte budget. | Finish image encoder/quality tuning so all size AC in `tasks/001-image-server-resize-cache.md` pass; then run PSI/Lighthouse mobile LCP check. |
| BE-008 | Pagination uses Laravel paginator shape, not `{results,count,next,previous}` | P2 | **Reproduces.** Probe 2026-06-04 (e2e `192.168.50.36`): `GET /api/messages/?thread_id=1` and `GET /api/travels/` both return `200` with `{total,next_page_url,prev_page_url,data,per_page,current_page,to,path}` — never `{results,count,next,previous}`. Shape is non-standard but **consistent**, so FE `count??total / next??next_page_url / results??data` always hits the Laravel branch; the `count/next/results` reads are dead defensive code. | `fetchMessages` (`api/messages.ts:168-177`) normalizes both shapes into `{count,next,previous,results}`. | Either adopt `{results,count,next,previous}` project-wide, or document the Laravel shape as canonical so FE can drop the dead dual-key branch (pairs with T-075). |
| BE-009 | Travel object canonical `name` / `url` | P4 | **Not reproduced.** Probe 2026-06-04: scanned 30 `/api/travels/` rows — `name` always present, `title` absent (0), `url` always canonical `/travels/{slug}` (0 missing, 0 non-canonical). Backend contract is already canonical here. | `normalizeTravelListItem` (`api/travelsNormalize.ts:165-198`) collapses `name`/`title` and rewrites `url` — defensive/dead on this backend. | No backend change required. FE simplification (T-076) can proceed as safe dead-code cleanup with a regression test; not blocked on backend. |
| BE-010 | Comment tree requires N+1 sub-thread fetches | P2 | **Reproduces.** Probe 2026-06-04: `GET /api/travel-comments/?travel_id=733` -> `200` with 4 comments, **2 carrying `sub_thread > 0`**, so FE must issue extra sub-thread fetches to assemble the conversation. (`563`/`3` -> `400` = BE-002; `391` -> `200` empty.) | `getTravelComments` (`api/comments.ts:113-147`) BFS-fetches sub-threads in batches of 5. | Return the full comment tree in one request (`?expand=sub_threads` / `?depth=full`) or a flat list with `parent_id`. Then FE removes the BFS loop (pairs with T-077). |
| BE-011 | No slug-resolution endpoint; FE brute-forces search on 404 | P2 | **Reproduces.** Probe 2026-06-04: `GET /api/travels/by-slug/<bad>/` -> `404`; `GET /api/travels/resolve-slug/?slug=<bad>` -> `404` (endpoint does not exist). FE search fan-out is load-bearing. | `findTravelBySlugFallback` (`api/travelDetailsQueries.ts:310-348`) runs ~15 query variants across pages, scoring client-side (4000ms budget); builds on closed T-072. | Provide a stable slug contract (slugs don't drift across deploys) and/or a `GET /travels/resolve-slug/?slug=` fuzzy endpoint. Then FE collapses the fan-out to one call and lowers the timeout (pairs with T-078). |
| BE-012 | Media URL shape (double host / `/api` prefix / S3 signatures) | P4 | **Not reproduced.** Probe 2026-06-04: sampled `/api/travels/` thumbs are clean single-host `http://host/travel-image/.../*.webp` — no double host, no `/api` prefix, no `X-Amz-Signature`. Consistent with closed BE-001. | `normalizeAbsoluteMediaUrl` (`utils/mediaUrl.ts:39-85`) repairs double-host URLs and strips `X-Amz-*` — defensive/dead on this backend. | No backend change required. Keep the FE guard + a regression test per the repo "don't remove guards without tests" rule; T-079 is optional cleanup, not blocked on backend. |
| BE-013 | Quest/city coordinates as strings; `country_code` missing | P3 | **Reproduces.** Probe 2026-06-04: `GET /api/quests/by-quest-id/krakow-dragon/` -> `200`; node `lat`/`lng` come back as **strings**, `country_code` is **undefined**. Overlaps BE-005 (country quality). | `coordNum` parses string coords (can yield NaN); `getCountryCodeByCoords` (`utils/geoCountry.ts`) backfills country from lat/lng (`utils/questAdapters.ts:22-28,202-212`). | Return coordinates as numbers (server-validated lat in [-90,90], lng in [-180,180]) and always return `country_code`. Then FE removes `coordNum` parsing and the bounding-box table (pairs with T-080). |
| BE-014 | Quest bundle structured fields as JSON strings | P4 | **Not reproduced.** Probe 2026-06-04: same quest -> `steps` and `intro` are already JSON objects (`typeof === "object"`), not strings. | `normalizeQuestBundle` (`api/quests.ts:88-122`) `JSON.parse`-s `steps`/`intro` inside try/catch — defensive/dead on this backend. | No backend change required. FE parse layer can be removed with a regression test (T-081); not blocked on backend. |
| BE-015 | Collection endpoints return 404 instead of `200` + empty list | P3 | **Reproduces.** Probe 2026-06-04: `GET /api/travels/563/near/` -> `404` but `GET /api/travels/391/near/` -> `200` — inconsistent for two valid travels. Same error class as BE-002. (Draft `__draft_placeholder__` handling not separately probed.) | FE swallows the 404 as `[]` (`api/map.ts:286-309`) and injects placeholders for blank draft fields (`utils/travelFormNormalization.ts:132-150`). | For valid resources with no rows return `200` + `[]`/`{results:[]}`; reserve 404 for non-existent resources. Then FE removes the 404->empty swallow (pairs with T-082). |
| BE-016 | Profile `avatar` URL points to a missing file -> avatar 404s (doubled `/avatar/.../avatar/` path segment) | P2 | **Reproduces in prod.** Browser probe 2026-06-05 on `metravel.by/travels/torun` (user `Julia`, id 82): `GET /api/user/82/profile/` -> `200` with **non-empty** `avatar`, but the image `GET https://metravel.by/avatar/profile/82/avatar/47c230f….webp` -> **404 (Not Found)** (also `?v=0` and `?v=1`). The **raw** URL with no `w/h/q/fit` resize params still 404s, so the FE `optimizeImageUrl` proxy rewrite is **not** the cause — the file simply does not exist at the served path. Note the suspicious **doubled** `…/profile/82/avatar/…` segment, suggesting the stored path and the path the API returns diverge. Follows the closed BE-007 (avatar moved off direct S3 to backend `/avatar/profile/...`). | `useAvatarUri` (`hooks/useAvatarUri.ts`) now caches failed avatar URLs for the session: after the first 404 it stops re-requesting the same base URL (no more `v=0`/`v=1` retry storm across components) and renders the fallback icon. Regression tests in `__tests__/hooks/useAvatarUri.test.tsx`. | Make `profile.avatar` resolve to a file that actually exists: (1) reconcile the stored avatar path with the URL returned by the API (investigate the doubled `avatar/.../avatar/` segment in URL generation); (2) verify avatar files were not lost in the storage move that closed BE-007 — everything else returns `200`, only avatar files 404; (3) once fixed, confirm `GET /avatar/profile/<id>/...` -> `200` for an account with an uploaded avatar. |

## Closed / Verified Archive

| ID | Closed on | Verification | Frontend note |
| --- | --- | --- | --- |
| BE-001 | 2026-06-04 | Quest media double-scheme no longer reproduces. Probe: `GET /api/quests/by-quest-id/krakow-dragon/` -> `200`, `doubleSchemeCount=0`, media hosts are valid S3/API URLs. | Keep `normalizeMediaUrl` / `fixMediaUrl` regression coverage; do not remove guards without tests. |
| BE-003 | 2026-06-04 | `PUT /api/travels/upsert/` with `{}` now returns structured `400` validation errors instead of `500`. | Wizard still builds full payload client-side; backend validation is now sane for raw/partial requests. |
| BE-006 | 2026-06-04 | Dedicated e2e auth context is usable and no longer empty for QA smoke: programmatic login -> `200`, token/user id present, favorite-travels probe -> `200` with data rows. | Manual QA can use `.env.e2e` auth flow; still avoid destructive prod actions and do not print secrets. |
| BE-007 | 2026-06-04 | Profile avatar no longer comes back as a direct S3 URL. Probe: profile avatar host is backend (`192.168.50.36`), path `/avatar/profile/...`, no presigned S3 query. | Existing avatar `optimizeImageUrl` calls stay in place; add avatar resize-proxy support later only if perf measurements show it is still needed. |

## Notes

- The active table is intentionally short. If a backend item is fixed and verified, move it to the archive instead of leaving stale blockers in the queue.
- BE-004 changes an API contract and must be coordinated with the frontend before landing frontend incremental-fetch changes.
- BE-002 is now narrower than the original finding: one no-comments travel returns `200`, another still returns `400`.
- BE-016 was found via a prod browser probe on 2026-06-05 (Network/Console), not the e2e backend; the FE failed-URL cache is a UX guard against the 404 retry storm, not a fix — keep it until BE-016 is verified fixed.
- `TASK-20260520-001` remains in `tasks/` because it has detailed acceptance criteria and validation commands.
