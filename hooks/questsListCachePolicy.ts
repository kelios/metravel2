// Политика кеша списка квестов, вынесенная листовым модулем (без единого импорта).
//
// #1393: константы жили в `hooks/useQuestsApi.ts`, и любой потребитель, которому
// нужны были только времена кеша, тянул за собой весь слой данных квестов —
// `utils/questAdapters` → `utils/geoCountry` → таблицу контуров стран
// `utils/geoCountryOutlines` (47 КБ raw). Из универсальных крошек в шапке это
// ставило таблицу контуров тегом <script> на 960 из 967 маршрутов, включая
// главную, карту, профиль и статьи.
//
// Значения менять только вместе: список кешируется под одним ключом
// `queryKeys.quests()`, чтобы экран квестов, промо-блок главной, три мета-хука
// детали (rating/completion/pioneer) и крошка города дедуплицировались в ОДИН
// запрос `/quests/`. Разные staleTime под одним ключом дедупликацию ломают.
export const QUESTS_LIST_STALE_TIME = 30 * 60 * 1000
export const QUESTS_LIST_GC_TIME = 60 * 60 * 1000
