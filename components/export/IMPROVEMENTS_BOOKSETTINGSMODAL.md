# Улучшения BookSettingsModal

## 📋 Обзор улучшений

Модальное окно настроек PDF было значительно улучшено с фокусом на:
- ✅ Доступность (accessibility)
- ✅ UX и визуальная обратная связь
- ✅ Валидация данных
- ✅ Сохранение настроек
- ✅ Производительность

---

## 🎯 Основные улучшения

### 1. **Доступность (A11y)**

#### Focus Trap
```typescript
import { useFocusTrap } from '@/hooks/useFocusTrap';

useFocusTrap(dialogRef, {
  enabled: visible && Platform.OS === 'web',
  initialFocus: firstFocusableRef,
});
```
- Фокус остается внутри модального окна
- Автоматический фокус на кнопке "Отмена" при открытии
- Корректная навигация по Tab/Shift+Tab

#### ARIA атрибуты
```typescript
<div
  role="dialog"
  aria-modal="true"
  aria-labelledby="modal-title"
  aria-live="polite"
  aria-invalid={hasErrors}
/>
```
- Корректные ARIA роли и атрибуты
- Поддержка screen readers
- Live regions для ошибок валидации

#### Клавиатурная навигация
- ✅ Escape закрывает модалку (с подтверждением при несохраненных изменениях)
- ✅ Tab навигация по всем интерактивным элементам
- ✅ Видимые focus стили на всех кнопках и полях

---

### 2. **Валидация данных**

#### Автоматическая валидация
```typescript
useEffect(() => {
  const errors: string[] = [];
  
  if (!settings.title || settings.title.trim().length === 0) {
    errors.push('Укажите название книги');
  }
  
  if (settings.title.length > 100) {
    errors.push('Название книги не должно превышать 100 символов');
  }
  
  // ... другие проверки
  
  setValidationErrors(errors);
}, [settings]);
```

#### Типы валидации
- ✅ Обязательные поля (название книги)
- ✅ Ограничение длины (100 символов для заголовка, 150 для подзаголовка)
- ✅ Логическая валидация (чек-листы должны быть выбраны, если включены)
- ✅ Визуальная индикация ошибок (красные рамки, сообщения)

#### Блокировка действий при ошибках
```typescript
<button
  onClick={handleSave}
  disabled={isSaving || validationErrors.length > 0}
>
  Сохранить PDF
</button>
```

---

### 3. **Отслеживание изменений**

#### Индикатор несохраненных изменений
```typescript
const [hasUnsavedChanges, setHasUnsavedChanges] = useState(false);

// Показываем бейдж "Не сохранено"
{hasUnsavedChanges && (
  <span style={{...}}>• Не сохранено</span>
)}
```

#### Предупреждение при закрытии
```typescript
const handleClose = useCallback(() => {
  if (hasUnsavedChanges && Platform.OS === 'web') {
    const confirmed = window.confirm(
      'У вас есть несохраненные изменения. Вы уверены, что хотите закрыть настройки?'
    );
    if (!confirmed) return;
  }
  onClose();
}, [hasUnsavedChanges, onClose]);
```

---

### 4. **Сохранение настроек**

#### LocalStorage persistence
```typescript
// Сохранение
if (Platform.OS === 'web' && typeof localStorage !== 'undefined') {
  try {
    localStorage.setItem('metravel_pdf_settings', JSON.stringify(settings));
  } catch (e) {
    console.warn('Failed to save settings to localStorage:', e);
  }
}

// Загрузка
useEffect(() => {
  let savedSettings: Partial<BookSettings> | undefined;
  if (Platform.OS === 'web' && typeof localStorage !== 'undefined' && !defaultSettings) {
    try {
      const stored = localStorage.getItem('metravel_pdf_settings');
      if (stored) {
        savedSettings = JSON.parse(stored) as Partial<BookSettings>;
      }
    } catch (e) {
      console.warn('Failed to load settings from localStorage:', e);
    }
  }
  
  setSettings(buildInitialSettings(savedSettings || defaultSettings, userName));
}, [defaultSettings, userName]);
```

**Преимущества:**
- ✅ Настройки сохраняются между сессиями
- ✅ Пользователю не нужно каждый раз настраивать заново
- ✅ Graceful fallback при ошибках

---

### 5. **Индикаторы загрузки**

#### Состояние загрузки
```typescript
const [isSaving, setIsSaving] = useState(false);

// В кнопке
{isSaving ? (
  <>
    <span style={{ animation: 'spin 1s linear infinite' }}>⏳</span>
    Создание...
  </>
) : (
  <>📄 Сохранить PDF</>
)}
```

**Визуальная обратная связь:**
- ✅ Анимированная иконка при сохранении
- ✅ Блокировка кнопок во время операции
- ✅ Изменение opacity на disabled элементах

---

### 6. **Улучшенный UX**

#### Счетчики символов
```typescript
<div style={{ fontSize: '11px', color: MODAL_COLORS.textMuted, marginTop: '4px', textAlign: 'right' }}>
  {settings.title.length}/100 символов
</div>
```

#### Информационные подсказки
```typescript
// Подсказка о количестве путешествий
<div style={{...}}>
  <span>📚</span>
  <div>
    <div>Выбрано путешествий: <span>{travelCount}</span></div>
    <div>{travelCount === 1 
      ? 'Будет создана книга с одним путешествием' 
      : `Будет создана книга с ${travelCount} путешествиями`}
    </div>
  </div>
</div>
```

#### Визуальная обратная связь при взаимодействии
- ✅ Hover эффекты на всех интерактивных элементах
- ✅ Smooth transitions (0.2s cubic-bezier)
- ✅ Transform на hover (translateY(-1px))
- ✅ Изменение box-shadow при hover/focus

#### Улучшенные чекбоксы
```typescript
<label style={{
  padding: '12px',
  borderRadius: '12px',
  backgroundColor: settings.includeToc ? MODAL_COLORS.primarySoft : 'transparent'
}}>
  <input type="checkbox" checked={settings.includeToc} />
  <div>
    <div>Включить оглавление</div>
    <div style={{ fontSize: '12px' }}>С миниатюрами и номерами страниц</div>
  </div>
</label>
```

---

### 7. **Оптимизация производительности**

#### useCallback для обработчиков
```typescript
const handlePresetSelect = useCallback((preset: BookPreset) => {
  // ...
}, [settings.title, settings.subtitle]);

const handleThemeSelect = useCallback((theme: PdfThemeName) => {
  // ...
}, []);

const handleSettingsChange = useCallback((updates: Partial<BookSettings>) => {
  // ...
}, []);
```

**Преимущества:**
- ✅ Предотвращение лишних ререндеров
- ✅ Стабильные ссылки на функции
- ✅ Оптимизация для дочерних компонентов

---

## 📊 Визуальные улучшения

### До улучшений:
- ❌ Нет индикации несохраненных изменений
- ❌ Нет валидации в реальном времени
- ❌ Нет счетчиков символов
- ❌ Простые информационные блоки
- ❌ Нет анимаций и плавных переходов

### После улучшений:
- ✅ Бейдж "Не сохранено" в заголовке
- ✅ Блок с ошибками валидации (красный)
- ✅ Счетчики символов для текстовых полей
- ✅ Улучшенный информационный блок с иконками
- ✅ Плавные анимации и transitions
- ✅ Hover эффекты на всех элементах
- ✅ Анимированная иконка загрузки

---

## 🎨 Стилизация

### Цветовая схема
```typescript
const MODAL_COLORS = {
  overlay: 'var(--color-overlay)',
  surface: 'var(--color-surface)',
  text: 'var(--color-text)',
  textMuted: 'var(--color-textMuted)',
  border: 'var(--color-border)',
  primary: 'var(--color-primary)',
  primarySoft: 'var(--color-primarySoft)',
  focus: 'var(--color-focus)',
  // ... и другие
};
```

### Тени
```typescript
const MODAL_SHADOWS = {
  light: 'var(--shadow-light)',
  medium: 'var(--shadow-medium)',
  heavy: 'var(--shadow-heavy)',
  modal: 'var(--shadow-modal)',
  soft: 'var(--shadow-soft)',
};
```

### Адаптивность
```typescript
// Адаптивные размеры
const isMobile = window.innerWidth <= METRICS.breakpoints.tablet;

padding: isMobile ? '20px' : '28px',
fontSize: isMobile ? '20px' : '24px',
```

---

## 🧪 Тестирование

### Ручное тестирование

**Валидация:**
1. Оставить поле "Название книги" пустым → должна появиться ошибка
2. Ввести более 100 символов → должна появиться ошибка
3. Включить чек-листы, но не выбрать ни один → должна появиться ошибка
4. При наличии ошибок кнопки "Превью" и "Сохранить PDF" должны быть disabled

**Несохраненные изменения:**
1. Изменить любую настройку → должен появиться бейдж "Не сохранено"
2. Попытаться закрыть модалку → должно появиться предупреждение
3. Сохранить настройки → бейдж должен исчезнуть

**LocalStorage:**
1. Настроить PDF и сохранить
2. Закрыть браузер
3. Открыть заново → настройки должны сохраниться

**Доступность:**
1. Открыть модалку → фокус должен быть на кнопке "Отмена"
2. Tab → фокус должен перемещаться по элементам внутри модалки
3. Escape → модалка должна закрыться (с подтверждением, если есть изменения)
4. Клик вне модалки → должна закрыться (с подтверждением)

---

## 📈 Метрики улучшений

### Доступность
- **До:** Базовая поддержка (role, aria-modal)
- **После:** Полная поддержка (focus trap, ARIA атрибуты, клавиатурная навигация)

### UX
- **До:** Минимальная обратная связь
- **После:** Полная визуальная обратная связь (валидация, индикаторы, подсказки)

### Производительность
- **До:** Все обработчики создавались заново при каждом рендере
- **После:** Мемоизация с useCallback

### Надежность
- **До:** Нет валидации, можно отправить некорректные данные
- **После:** Полная валидация, блокировка при ошибках

---

## 🚀 Будущие улучшения

### В планах:
- [ ] Живой превью настроек в реальном времени
- [ ] Анимированные переходы между секциями
- [ ] Drag & drop для изменения порядка путешествий
- [ ] Предпросмотр обложки книги
- [ ] Экспорт/импорт настроек в файл
- [ ] История изменений (undo/redo)
- [ ] Темная тема для модалки
- [ ] Поддержка мобильных устройств (native модалка)

---

## 📚 Связанные файлы

- `BookSettingsModal.tsx` - основной компонент
- `PresetSelector.tsx` - выбор пресетов
- `ThemePreview.tsx` - предпросмотр тем
- `GalleryLayoutSelector.tsx` - выбор раскладки галереи
- `useFocusTrap.ts` - хук для focus trap
- `/src/types/pdf-presets.ts` - типы пресетов
- `/src/types/pdf-gallery.ts` - типы галереи

---

## 💡 Best Practices

### 1. Всегда используйте валидацию
```typescript
// ✅ ХОРОШО
if (validationErrors.length > 0) {
  alert(`Пожалуйста, исправьте ошибки:\n\n${validationErrors.join('\n')}`);
  return;
}
```

### 2. Мемоизируйте обработчики
```typescript
// ✅ ХОРОШО
const handleChange = useCallback((updates: Partial<BookSettings>) => {
  setSettings((prev) => ({ ...prev, ...updates }));
  setHasUnsavedChanges(true);
}, []);
```

### 3. Используйте ARIA атрибуты
```typescript
// ✅ ХОРОШО
<input
  aria-required="true"
  aria-invalid={hasError}
  aria-describedby="error-message"
/>
```

### 4. Предупреждайте о потере данных
```typescript
// ✅ ХОРОШО
if (hasUnsavedChanges) {
  const confirmed = window.confirm('У вас есть несохраненные изменения...');
  if (!confirmed) return;
}
```

---

**Дата обновления:** 3 января 2026
**Версия:** 2.0
**Автор:** GitHub Copilot

