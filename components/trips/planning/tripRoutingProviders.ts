// components/trips/planning/tripRoutingProviders.ts
// Служебные значения `RoutingState.provider`, которыми конструктор поездки
// помечает собственное живое превью маршрута (#1490). Отдельный модуль без
// зависимостей: их читает и логика превью, и презентационные подписи, а тянуть
// ради двух строк парсер треков в граф карточек поездок незачем.

/** Маршрут по дорогам от движка /map, ещё не сохранённый на бэке. */
export const PREVIEW_PROVIDER = 'preview';

/** Движок честно не смог построить дорогу — показана прямая линия. */
export const PREVIEW_DIRECT_PROVIDER = 'direct';

/** Общественный/смешанный транспорт: автопрокладки для него нет вовсе. */
export const PREVIEW_SCHEMATIC_PROVIDER = 'schematic';
