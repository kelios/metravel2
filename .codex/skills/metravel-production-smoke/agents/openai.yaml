interface:
  display_name: "Metravel Production Smoke"
  short_description: "Read-only production health checks."
  default_prompt: "Use $metravel-production-smoke to check metravel.by production health with safe GET/browser probes and report concrete regressions."
