interface:
  display_name: "Metravel Production Smoke"
  short_description: "Read-only production health checks."
  default_prompt: "Use $metravel-production-smoke to check metravel.by with read-only GET/browser probes, and when performance is in scope report route-level request/API counts, bytes, oversized media and before/after-scroll behavior instead of treating 200 or a visible page as success."
