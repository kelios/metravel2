interface:
  display_name: "Metravel iOS Release Operator"
  short_description: "Build and operate TestFlight/App Store releases."
  default_prompt: "Use $metravel-ios-release-operator to prepare the exact authorized iPhone release gate, audit both source config and the compiled signed IPA before upload, fail closed on identity/minimum-OS/purpose-string/signing/privacy drift, and keep signed build, TestFlight upload, App Review submission, and storefront release as separate approvals."
