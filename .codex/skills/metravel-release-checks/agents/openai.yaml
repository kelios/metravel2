interface:
  display_name: "Metravel Release Checks"
  short_description: "Pick the right metravel validation flow."
  default_prompt: "Use $metravel-release-checks to choose and run the right metravel checks for this change, check the operation gate before release/build/test commands, fix real failures in scope, and report any unrelated blocker explicitly. For deploy execution, rollback, Windows/Codex wrapper use, or emergency scripts/fix-prod.sh recovery, hand off to $metravel-devops-agent instead of inventing a deploy command."
