interface:
  display_name: "Metravel Google Play Operator"
  short_description: "Local Android build and safe Play Production release"
  default_prompt: "Use $metravel-google-play-operator to build Android locally without EAS, require google-services.json, dry-run the authorized Play tracks, and commit production and/or alpha+internal only with explicit owner authority."
