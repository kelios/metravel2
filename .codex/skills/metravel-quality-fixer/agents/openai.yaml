interface:
  display_name: "Metravel Quality Fixer"
  short_description: "Run lint, Jest, and Playwright end to end and fix the failures."
  default_prompt: "Use $metravel-quality-fixer to run the requested metravel quality gates, fix real failures in scope, rerun the affected checks, and report any unrelated blocker explicitly."

