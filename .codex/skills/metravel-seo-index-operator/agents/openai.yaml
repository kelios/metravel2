interface:
  display_name: "Metravel SEO Index Operator"
  short_description: "Run SEO checks and scoped GSC browser actions."
  default_prompt: "Use $metravel-seo-index-operator to gather indexing evidence and execute explicitly scoped GSC URL actions through an authenticated browser."
