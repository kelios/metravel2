interface:
  display_name: "Metravel iOS Developer"
  short_description: "Reserved future-iOS route; explicit opt-in only"
  default_prompt: "Use $metravel-ios-developer only after the user explicitly reactivates iOS work; otherwise keep iOS out of QA, Done gates, and verify-pending requirements."
