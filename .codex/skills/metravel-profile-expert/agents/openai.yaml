interface:
  display_name: "Metravel Profile Expert"
  short_description: "Own profile, settings, subscriptions, and profile IA"
  default_prompt: "Use $metravel-profile-expert for profile and settings work while preserving identity, tabs, counters, subscriptions, auth boundaries, existing UI primitives, centralized links, and verified mobile/desktop behavior."
