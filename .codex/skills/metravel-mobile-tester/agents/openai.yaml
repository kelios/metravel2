interface:
  display_name: "Metravel Mobile Tester"
  short_description: "Compare mobile web and Android with evidence"
  default_prompt: "Use $metravel-mobile-tester to compare the same flow, state, and affected locales on mobile web and a local USB Android build, requiring paired evidence whenever either surface changes."
