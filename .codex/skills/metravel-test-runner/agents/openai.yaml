interface:
  display_name: "Metravel Test Runner"
  short_description: "Run the right Jest and governance checks for the touched scope."
  default_prompt: "Use $metravel-test-runner to map web/Android/iOS and RU/BE/UK/PL/EN impact, choose the narrowest reliable checks, choose validation delegated or validation skipped when another quality gate is active without waiting or retrying, and fix only this task's failures."
