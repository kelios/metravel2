interface:
  display_name: "Metravel Backend Diagnostician"
  short_description: "Diagnose backend/API blockers safely."
  default_prompt: "Use $metravel-backend-diagnostician to analyze a metravel API/backend issue read-only, never mutate backend Git or tracked server files, and prepare evidence-backed area=back board follow-up."
