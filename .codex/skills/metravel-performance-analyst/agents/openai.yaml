interface:
  display_name: "Metravel Performance Analyst"
  short_description: "Measure Lighthouse, bundles, and performance budgets"
  default_prompt: "Use $metravel-performance-analyst to measure page-wide requests, API fan-out, bytes, media variants and budgets on the same live production URL before and after deploy; distinguish preview from production, include a negative probe, and keep unverified fixes in review/testing."
