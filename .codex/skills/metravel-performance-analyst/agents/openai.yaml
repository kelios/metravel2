interface:
  display_name: "Metravel Performance Analyst"
  short_description: "Measure Lighthouse, bundles, and performance budgets"
  default_prompt: "Use $metravel-performance-analyst to measure metravel from a production build or real URL, compare budgets, and when travel hero code is touched verify both swipe behavior and performance before passing it."
