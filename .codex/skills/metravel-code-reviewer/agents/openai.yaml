interface:
  display_name: "Metravel Code Reviewer and Fixer"
  short_description: "Review and repair every code diff before handoff"
  default_prompt: "Use $metravel-code-reviewer to review and fix the complete task diff, verify affected-target and localization impact, require native evidence only for native-specific observable scope, then re-review and revalidate."
