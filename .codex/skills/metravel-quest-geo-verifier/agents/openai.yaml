interface:
  display_name: "Metravel Quest Geo Verifier"
  short_description: "Verify quest coordinates against real-world objects"
  default_prompt: "Use $metravel-quest-geo-verifier to verify quest coordinates read-only with local/API data and OSM/Nominatim evidence, prefer the project geocheck script, and return findings and suggested patches without writing production content."
